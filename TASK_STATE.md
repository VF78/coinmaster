# Nautilus MVP — coordinator checkpoint

Current: P5.2/P6 Nautilus paper hardening. The canonical deployed paper node is native Sandbox-only on the P5 branch; strict runtime sidecar, warmup, reconciliation, venue-specific funding freshness, and paper controls are verified. `live_order_capability=false`; no live promotion is authorized. Historical P0–P4 entries below remain evidence, not current status.

P2.2 research job lifecycle final P1 race correction 2026-09-20: local-only `b021779` prevents an unowned manager from freeing a canonical lease while its child is still alive. A cross-manager cancel proves the persisted owner token plus birth-bound process identity, terminates the exact group with bounded TERM→KILL, confirms it has vanished, then and only then records `CANCELED` and releases the lease. Identity/termination uncertainty remains `CANCEL_REQUESTED` with conflict evidence and retains the lease; an active leased research terminal update requires an explicit verified-death release path. Existing launch permit, compact SHA-verified job artifact, stdout cap, observer reaping, and atomic idempotency protections remain. Two real ControlStore/manager regressions verify successful remote cancel→child death→successor start, and mismatch→lease-held→successor refusal. Focused API tests: 37 passed; full runtime suite: 111 passed. No native 24-month execution, VPS/service deployment, paper command/order, or live action occurred.

P2.2 runtime-only deployment 2026-09-20: VPS canonical checkout now runs source `caa4cdb378595cd4db669d8429f37df5ad409e42`; only `coinmaster-runtime.service` restarted (`2304656` → `2334804`) after the checked runtime SPA build/copy. Paper service stayed active on PID `2315259`, `paper`/`RECONCILED`/`live_order_capability=false`, flat positions/orders. Online-safe runtime-control backup: `/var/lib/coinmaster-runtime/drills/20260920T050156Z/control.sqlite`, SHA-256 `dd765abbf3184a839e7bd8603643f288ca4dabb87c59f29e5fdcc86b0fbed1a9`. Authenticated loopback verified default save, preflight (`allowed=false`), catalog (7 entries), a persisted `MISSING_1M_MANIFEST` research `BLOCKED` before spawn (`pid=null`, zero leases) and harmless terminal cancel; unauthenticated config access remained `401`, SPA asset `200`. No paper command/order, backtest, data copy, private/live exchange, proxy/firewall/TLS, or paper restart occurred.

P2 configuration-honesty 2026-09-20: local-only commit `4643038` prevents phantom paper controls. `StrategyConfig` preserves the exact original v0 envelope but rejects any non-v0 value for unsupported initial allocation/total, zero-wave/sigma switches, optional stops/loss cap, future SOL margin earmark, `insufficient_margin=clip`, reserve transfer/trigger, and restart/post-liquidation policy with stable `UNSUPPORTED_PAPER_CONFIG:<field>`. The Strategy UI removes those edit paths and labels saved candidate data as a draft that does not alter the running `corrected-v0` paper service; venue is preflight-only. Domain Candidate fields remain stored-draft inputs pending a separately authorized immutable-config activation path, while account/reserve/stop policies still need native execution and recovery semantics. Focused `test_api.py` + `test_runtime_sidecar.py`: 18 passed; TypeScript check, UI invariant (10), and both production builds passed. No 24-month backtest, VPS/service restart, paper command/order, or live action occurred.

Critical P4/P5 autonomous recovery correction 2026-09-20: deployed code `961b23a` removes the HTTP-poll dependency; follow-up `90cbea5` adds the native-Sandbox crash harness and was deployed only through `coinmaster-paper.service`. The paper worker owns a monotonic single-thread polling loop; HTTP health only reads state. Every native order submit first writes a SQLite-WAL durable intent. Sandbox group/cache restoration across processes is unsupported by design: any durable open position/order or submit-before-ACK record yields `MANAGE_ONLY` and blocks increases, preserving the original durable snapshot instead of accepting a fresh empty cache. The journal connection is guarded for concurrent poll/status access. Local full suite: 78 passed, including autonomous no-HTTP snapshot advancement and a real native-Sandbox SIGKILL harness: BTC opens and reduces from 1.0 to 0.6, SOL is durably journaled and submitted before ACK, then the same DB restarts into `MANAGE_ONLY_PENDING_INTENT` with duplicate submission blocked. VPS restarts touched only `coinmaster-paper.service` (PID `2306169` → `2311653` → `2315259`); runtime PID remained `2304656`. With no worker HTTP polling for 150 seconds after the follow-up deploy, journal snapshot advanced `1789876876038435190` → `1789877026037009612`. One authenticated runtime read then confirmed `READY`, all four BTC/SOL Bybit/Hyperliquid feeds `READY`, `RECONCILED`, flat positions/orders, `mode=paper`, and `live_order_capability=false`. No runtime command/order, private execution, proxy, firewall, or live activation occurred. Monitor public-feed freshness and autonomous snapshot age; real uncertain durable exposure must stay `MANAGE_ONLY` until separately authorized recovery design.

P0/P3 Hyperliquid public-data pass 2026-09-20: added `coinmaster.research.hyperliquid_data`, a resumable, no-secret, no-S3-billing `POST https://api.hyperliquid.xyz/info` importer. Every raw response is content-addressed and records exact request body/capture time; current `meta` + `metaAndAssetCtxs` are retained as immutable profile evidence only (`historical_profile_applicability=UNKNOWN_CURRENT_META_ONLY`). Fresh ignored manifest `runtime/var/data/hyperliquid/manifest.json` SHA-256 `b7feb312…`, raw-manifest SHA-256 `e371c412…`, captures BTC/SOL `candleSnapshot` 1d `[2022-09-02,2026-09-01)` and `fundingHistory` `[2024-09-01,2026-09-01)`, with Parquet hashes and request/raw provenance. Both assets: daily 1,460/1,460, zero gaps/duplicates/off-grid, but BTC 177 and SOL 183 zero-volume-or-trade rows explicitly `PROXY_PRICES_NONTRADING`; funding 17,520/17,520 hourly buckets, zero missing/duplicate buckets, raw timestamp offsets preserved (17,292 non-zero offsets per asset) and no funding zeros created. Every funding row explicitly says settlement oracle `UNKNOWN_FREE_REST_HAS_NO_SETTLEMENT_ORACLE`. Manifest blocks any faithful/comparable 24m/1m Hyperliquid run with `REST_MAX_5000_NO_24M_BBO` and `AWS_REQUESTER_PAYS_INVENTORY_REQUIRED`; no HL backtest/second simulator exists or is authorized. Narrow importer test and full runtime suite: 74 passed. No AWS billing, VPS, Project, paper, or live action.

P3 reporting-v2 fixed stress validation 2026-09-20: no trading rule or candidate parameter changed. The `7.5` control was reused from the immutable reporting-v2 canonical manifest (settled `125198.28909680`, interval cash `122240.20258750`) and exactly four predeclared fresh-engine/fresh-journal full four-stream runs were made: fee `1.25x`, fee `1.5x`, 2m latency + 5bps adverse symmetric spread, and 5m + 10bps. Canonical native latency is now a queued-intent readiness delay to the subsequent actual 1m quote; terminal boundary settlement remains separately named. Ignored immutable validation artifact `runtime/var/data/runs/native-validation-canonical-reporting-v2-stress.json`, SHA-256 `fd84369d…`, CSV SHA-256 `685d9a2a…`, has status `VALIDATION_SEEN_NOT_OOS + NOT_FAITHFUL_DIAGNOSTIC`, source/control/candidate/policy/code hashes and complete reports. Respective settled TOTAL / interval cash / cash DD percent / fees / funding / fills: `96302.13189164`/`94197.69629864`/`0.8020757563766053931282251948`/`262173.59904786`/2441/478; `74920.70977513`/`73417.64945413`/`0.8361362556553298945861553811`/`279934.52061225`/2441/478; `65052.96695239`/`63930.41990119`/`0.8510822699755522327990084671`/`179113.85344960`/2444/475; `40097.56604124`/`39654.95484204`/`0.8987094062998082819696779217`/`140868.43998060`/2444/475. All four are terminal-flat with zero native rejections, pre-submit gate blocks, and liquidations; each has one explicitly separated terminal-boundary fill and 24 validated cash months. No stress result selects or promotes a candidate. No VPS/Project/paper/live action; historical fee/tier applicability, BBO/liquidity, settlement marks, and intraminute liquidation fidelity remain blockers.

P3 canonical reporting correction 2026-09-20: reports produced at `dfd6f4c` are retained as immutable local audit evidence but are `SUPERSEDED_REPORTING_SCHEMA`, not economics controls, because they hardcoded liquidation zero, mixed native cash with unlabelled marked-equity terminology, used initial-seed DD percent, and hid an exact end-boundary terminal fill as `end-1µs`. `WaveOverlayStrategy` now exports authoritative liquidation state (trigger timestamp, native marked equity, tier MM, per-leg close-fill timestamp/value, flat lockout), terminal lifecycle, and pre-submit tier/margin gate blocks separately from native rejected orders. Baseline reporting now separates native cash-account series from sparse paired-mark checkpoints using the same native account/cache/CustomData state (marked monthly returns remain explicitly UNKNOWN), uses standard cash peak-to-trough DD percent, and asserts actual fill timestamps. Chosen convention: the exact `2026-09-01T00:00:00Z` terminal close belongs only to named `post_boundary_settlement` and is excluded from the 24 interval cash-return rows. Fresh ignored v0 artifact `runtime/var/data/runs/native-diagnostic-v0-canonical-reporting-v2.json`, SHA-256 `5f9408b6…`, is flat with 478 fills, zero native rejects/pre-submit blocks/liquidations, one separately reported terminal BTC fill, settled ACTIVE/RESERVE/TOTAL `63325.93791898`/`0`/`63325.93791898`; interval cash before settlement is `61535.30022708`. Cash DD is `321153.22428978` / `0.9067596926568427229058997829` (2025-03-03 → 2026-03-27); it remains diagnostic only. Fresh four-point canonical artifact `native-optimizer-canonical-reporting-v2.json` SHA-256 `aa546527…` / CSV `8a75e3db…` supersedes the prior pass for reporting only: `7.5` and `8.25` are terminal-flat, zero-liquidation candidates (`125198.28909680`, `90843.78155857`); `9.75` and `10.5` each have an authoritative BTC liquidation/flat lockout and were cut off after that native batch (so excluded, despite their partial terminal cash `13296.92456772`/`14092.01598100`). Best only within these unvalidated diagnostic assumptions is `7.5`, delta `+61872.35117782` vs settled v0. A real native callback/cache-order regression verifies re-entry is released only after the position callback and gets the following quote timestamp in the tier gate. Full runtime suite: 73 passed. No trading rule/parameter/VPS/Project/paper/live action; fee/tier history, BBO/liquidity, settlement-mark, and intraminute liquidation fidelity remain blockers.

P3 bounded canonical re-entry/size pass 2026-09-20: the full-1m v0 artifact at checkpoint `54b585b` was preserved unchanged (`native-diagnostic-report.json`, SHA-256 `27399a634…`). Its apparent post-2024-09-13 flatline was a real strategy-adapter defect, not a trading-rule result: after a group close the queued re-entry was checked against the stale daily-bar timestamp, while the latest native mark was the next minute, so the fail-closed tier gate rejected it. `WaveOverlayStrategy` now applies the contemporaneous quote event time to that gate and reconciles pending group-flat cache state; a targeted domain test proves the next daily decision re-enters. Correct lifecycle made the former repeated profile construction in `TierMarginPolicy` material; caching the immutable captured Bybit profile reduced a seven-day canonical smoke from 13.706s to 1.493s with the same 40,334 events/five fills, without changing model semantics. The one corrected full v0 rerun is separately stored as ignored `runtime/var/data/runs/native-diagnostic-v0-canonical-report.json`, SHA-256 `0ed0ae6b…`: exact 1,051,200 rows/source, 105 batches/4,206,260 events, 478 fills, native fees `219559.24998770`, 2,441 funding postings (signed amount explicitly UNKNOWN), zero rejections/liquidations/open positions, ACTIVE/RESERVE/TOTAL `63325.93791898`/`0`/`63325.93791898`, ROI `5.332593791898`, DD `321153.22428978`/`32.115322428978`. It remains `NOT_FAITHFUL_DIAGNOSTIC`/non-ranking. One authorized canonical-only four-point BTC multiplier pass (`7.5, 8.25, 9.75, 10.5`; no legacy sparse/daily route) is recorded in ignored `native-optimizer-canonical-reentry-btc.json` SHA-256 `86ccdaad…` and CSV SHA-256 `65531ad7…`, with candidate/config/data/policy/strategy hashes, complete reports and reconciliation checks. All rows are terminal-flat with zero liquidations/rejections (therefore no liquidation early cutoff triggered): terminal totals `125198.28909680`, `90843.78155857`, `13296.92456772`, `14092.01598100`; within these unvalidated assumptions, `7.5` is best, delta `+61872.35117782` vs corrected v0. No candidate is live-rankable. Historical fee/tier applicability, BBO/liquidity, settlement-mark, and intraminute liquidation fidelity remain blockers; no VPS, Project, paper, or live action.

P3 bounded performance sub-pass 2 2026-09-20: `PerpetualFundingModule` now consumes funding in stable `(timestamp,event_id)` cursor order, so repeated streaming runs do not rescan prior events; durable journal IDs still prevent reposting. `BybitTierMarginModule.observed` is bounded to the most recent 128 diagnostics rather than growing with minute processing. Existing native funding/replay, custom-mark, margin and quote-queue integration tests plus the full runtime suite pass (65). Dedicated synthetic both-leg liquidation/stream-boundary sequence fixtures and the weekly full-1m runner remain required; no full run, optimizer, paper/VPS/live action.

P3 bounded performance sub-pass 2b 2026-09-20: added one compact native `BacktestEngine` liquidation sequence using non-matching `VenueMark`/`DailySignalBar` inputs and quote-only execution. It proves a signal cannot fill before its BTC quote; paired healthy marks do not arm; a same-minute adverse BTC/SOL mark pair arms once at `equity <= tier MM`; BTC and SOL reduce-only closes wait for, and fill on, their respective next quotes with no cross-leg stale fill; all orders terminate filled, positions reconcile closed, audit records trigger/equity/MM/final fill, a later signal cannot re-enter, and a duplicate breach pair cannot liquidate twice. No production code changed. Targeted native tests: 17 passed; full runtime suite: 66 passed. Full-1m/weekly streaming proof remains required; no optimizer, paper, VPS, or live action.

P3 bounded performance sub-pass 3 2026-09-20: canonical `run_native_diagnostic` now uses one native engine and lazy four-Parquet 1m merge, with an explicit 730d feature-only `seed_bars` warmup. The normal 24m interval rejects anything other than exactly 1,051,200 rows per BTC/SOL execution/mark source; each selected minute is gap/duplicate/boundary checked, then emitted in deterministic bounded weekly batches as homogeneous `BYBIT_MARK` CustomData, optional non-matching daily signals at UTC close, and quote-close `QuoteTick`s (all available at minute open+60s). Each batch is `sort_data` → `run(streaming=True)` → `clear_data`; finalization calls `end()` once. The stream report records processed rows, batches/events, maximum batch size, cursor bound, and peak RSS. The former sparse minute route is explicitly rejected and cannot be selected by optimizer/validation. Tests cover lazy exact-once merge, duplicate/boundary rejection, one-shot versus two batch account/fill/flat parity, one `on_start` per engine, post-`clear_data` subscriptions, same-timestamp ordering, and bounded batches. A real two-day seeded smoke used 2,880 rows/source in two batches (11,524 events), 2 fills, terminal TOTAL `10845.64812400`, flat, peak RSS `421265408`; no 24m run, optimizer, paper, VPS, or live action. Targeted tests passed; full runtime suite: 70 passed.

P3 canonical full-1m v0 2026-09-20: one full exact [2024-09-01,2026-09-01) weekly streaming diagnostic was completed after the sub-pass-3 infrastructure. A report-schema omission of explicit episodes/transfers/funding-by-instrument fields required one allowed code-only correction and one final rerun; execution/config/data/policy assumptions were unchanged. Final artifact `runtime/var/data/runs/native-diagnostic-report.json` SHA-256 `27399a6345934cb36a87b9aae1db7a49bf237a67795bbd5a399d21fdd78135a0`; fills/orders SHA-256 `8318aebb5c6893cf528ad89b04cbbd0b084cae982cf39dbd87b3d2e1bfc347be` / `51adde1c5f1d6d39012c568d8c55bb3ed2feb2abb97b91d8c6cd68562a1aa193`. Config/data/code/policy hashes: `c3e2e2b2…` / `7ab5ae2c…` / `655f2bd8…` / `535e2dfe…`. It consumed exactly 1,051,200 rows per source in 105 batches (4,206,260 events; max 10,080 minutes/40,334 events; peak RSS 454,836,224; wall 95.6975s), with one engine/journal/end, 730d seed, default Candidate BTC 9.0/original z, native 0.001 fixture fees, and prior-minute funding. Final result is `NOT_FAITHFUL_DIAGNOSTIC`/non-ranking: 9 fills/orders, fees `402.00062660`, zero rejects/liquidations/transfers/open positions, 30 applied BTC funding postings (signed cash UNKNOWN), ACTIVE `11937.30014030`, RESERVE `0`, TOTAL `11937.30014030`, ROI `0.19373001403`, DD `634.30501120`/`0.06343050112` (start/trough 2024-09-13), terminal flat, 24 monthly returns. Episodes and realized/unrealized remain explicitly UNKNOWN; 1m-close/BBO-liquidity, historical fee/tier applicability, exact funding settlement marks, and liquidation fidelity remain blockers. Report invariants and independent hashes verified; full suite 70 passed. No optimizer, VPS, paper, or live action.

P3 bounded full-quote sub-pass 1 2026-09-20: `WaveOverlayStrategy` now retains O(1) latest BTC/SOL `VenueMark` and tier marks, queues daily domain intents, and releases each only on its own next native quote (including group-close legs). It has an in-strategy paired-mark liquidation arm: breach calculation uses native account marked equity against validated tier MM; opening orders/intents are cancelled/suppressed, native reduce-only closes wait for each own quote, and the domain locks only after cache-flat confirmation. Compact trigger/fill audit is retained. Existing native integration asserts bounded mark/tier state and empty post-run queue; targeted integration and full suite passed (65). This is not the full-1m proof yet: weekly streaming ingestion, deterministic liquidating sequences, and a new exact v0 run remain mandatory before any optimizer. No optimizer/paper/VPS/live action.

P3 canonical causal validation 2026-09-20: `DailySignalBar` CustomData is now the only daily signal path for baseline/optimizer research; daily-bar matching is rejected before any engine/data load. `native-validation-causal-v1-v3.json/.csv/.partial.json` is linked to sensitivity parent `c1513a6c…`; its fresh canonical 24m control was saved as `native-diagnostic-report.json` SHA-256 `29cfc001…`. It proves `control_matches_parent_best=false`: all prior causal-v1 optimizer/refinement2/sensitivity artifacts are explicitly `SUPERSEDED_DAILY_BAR_MATCHING`, preserved only as historical evidence, and must not be ranked or used as optimizer controls. V3 runs use explicit start/end/`10000` seed, >=730-day feature warmup, fresh account/journal, versioned latency/spread/native-fee policies, and nonmatching marks/signals. Status is `VALIDATION_SEEN_NOT_OOS` plus `NOT_FAITHFUL_DIAGNOSTIC`/non-live-ranking. Canonical 24m control: TOTAL `509682.23612836`, ROI `49.968223612836`, DD `13274.10044339`, fees `39815.92731810`, 295 fills, 1,397 funding, zero rejects/liquidations, flat. Seen-data windows: 2024-09→2025-09 TOTAL `20991.48353019` (ROI `1.099148353019`, DD `573.76718348`, fees `985.03947940`, 63 fills, 278 funding) and 2025-09→2026-09 TOTAL `23904.96102199` (ROI `1.390496102199`, DD `274.11091575`, fees `1204.08672240`, 61 fills, 272 funding); both fresh-seed with 12 monthly rows. Fee assumptions: 1.25x TOTAL `469685.57154051`, fees `46865.73272034`; 1.5x TOTAL `432134.04121394`, fees `52905.05748915`. Conservative execution assumptions: 2m/5bps TOTAL `447298.98854997`, fees `36484.65717530`; 5m/10bps TOTAL `390158.06882374`, fees `33145.12013520`. This is seen-data validation only, not OOS or historical fidelity; fee/tier applicability, settlement marks, BBO/liquidity and intraminute liquidation remain blockers. No optimizer, paper/VPS/live action.

P3 causal-v1 bounded sensitivity 2026-09-20: created linked ignored `native-optimizer-causal-v1-sensitivity.json/.csv/.partial.json` from refinement2 parent SHA-256 `f8b64192…`, with inherited baseline config/data/code/policy provenance and fresh native engine/journal per attempt. The 15-row one-axis-only pass held BTC multiplier `2.4` and included its exact control, uniform SOL entry-z shifts `{-0.125,-0.0625,+0.0625,+0.125}`, and the authorized two-value variants of relative/beta/z-history/wave-history/SOL-holding days; no axes were combined or expanded. Best non-control is SOL entry-z shift `-0.125`: terminal ACTIVE/TOTAL `14371.58946324`, ROI `0.437158946324`, DD `3993.80938214` (`0.399380938214`), fees `3576.52692440`, 269 fills, 1,391 funding events, zero rejects/liquidations, terminal flat. One fresh rerun matched terminal/count/cost/funding, flatness, and 24 monthly returns. All rows remain `NOT_FAITHFUL_DIAGNOSTIC`/non-ranking for live; historical fee/tier applicability, exact settlement marks, BBO/liquidity, and intraminute liquidation are still blockers. No paper/VPS/live change.

P3 causal-v1 refinement2 2026-09-20: preserved the parent `native-optimizer-causal-v1.json` and created linked ignored `native-optimizer-causal-v1-refinement2.json/.csv/.partial.json`, with parent SHA-256 `e60bef74…`, inherited full baseline provenance, and seven only new BTC multipliers `{2.3,2.4,2.5,2.6,2.7,2.8,2.9}` inside the explicitly established `[2.2,3.0]` bracket. No silent extension or other parameter change. Combined artifact retains 13 parent + 7 new rows, each with 24 monthly returns; all are `NOT_FAITHFUL_DIAGNOSTIC`/non-live-ranking. Best is interior `2.4`: terminal ACTIVE/TOTAL `13661.36361686`, ROI `0.366136361686`, DD `3993.80938214` (`0.399380938214`), fees `3479.33434980`, 265 fills, 1,385 funding events, zero rejects/liquidations, terminal flat. Independent fresh `2.4` rerun verified terminal/count/cost/funding, flatness, and 24 months. Historical fee/tier applicability, exact settlement marks, BBO/liquidity, and intraminute liquidation remain blockers; no paper/VPS/live change.

P3 causal-v1 optimizer pass 2026-09-20: a distinct ignored local artifact set (`native-optimizer-causal-v1.json/.csv/.partial.json`) used the exact post-boundary baseline report SHA-256 `77c728b4…`, preserving full baseline config/data/code/policy provenance, seed `0`, and one fresh native engine/funding journal per candidate. Coarse BTC-notional-only axis was `{0.5,0.75,1.0,1.5,2.0,3.0,4.5,6.48,9.0}` with `9.0` as v0 control; all other strategy parameters were unchanged. Coarse best `2.0` was interior, so exactly one ±5/±10% refinement ran; its best was the upper refinement endpoint `2.2` and the range was not extended. Best terminal ACTIVE/TOTAL `13652.42627773` (ROI `0.365242627773`, delta `+9774.92634932` vs v0 `3877.49992841`, DD `3657.04667313`, fees `3215.25765010`, 265 fills, 1,385 funding events, zero rejects/liquidations, terminal flat). Fresh independent best rerun matched all terminal/count/cost/funding fields. All 13 rows retain 24 monthly returns and are `NOT_FAITHFUL_DIAGNOSTIC`, non-ranking for live; historical fee/tier applicability, settlement marks, BBO/liquidity, and intraminute liquidation remain blockers. No venue comparison or paper/live change.

P3 report-boundary correction 2026-09-20: exact corrected-v0 diagnostic reporting now restricts exported equity and drawdown to `[2024-09-01, 2026-09-01)`, seeded at `10000` on `2024-09-01`. The terminal close booked at the exclusive `2026-09-01` boundary is represented as the final August endpoint solely for month-end reporting. One exact rerun: 24 rows (`2024-09`…`2026-08`), August TOTAL `3877.49992841` equals terminal TOTAL exactly; compounded monthly returns equal `terminal/10000`; DD start `2024-09-01T00:00:00+00:00`, trough `2026-03-28 00:00:00+00:00`. Production report assertions and targeted tests enforce row count, terminal reconciliation, Decimal compounding, and DD interval bounds. No strategy/execution change and no optimizer run; result remains `NOT_FAITHFUL_DIAGNOSTIC`/non-ranking.

P3 acceptance checkpoint 2026-09-20: `native_baseline` now obtains matching quotes from the first actual closed, gap-free Bybit 1m execution candle after each daily decision and uses paired 1m marks as non-matching CustomData; it no longer manufactures daily close ±0.05 quotes. `ExecutionPolicy` is versioned/hashed and explicitly marks fees, spread/slippage/liquidity, and liquidation as unknown/unvalidated rather than inventing historical facts. Targeted baseline/native integration tests: 6 passed. One exact corrected-v0 run completed with data hash `7ab5ae…`, config hash `4ecbc5…`, policy hash `03c59f…`, but is `NOT_FAITHFUL_DIAGNOSTIC`, 0 fills, TOTAL 10000: no optimizer was run. Blocker: full §5 acceptance reporting, validated venue profile/cost/liquidation facts, and a causal strategy lifecycle with nonzero native executions still require implementation/evidence; do not rank or promote this result.

P3 causal ordering correction 2026-09-20: 0 fills came from a session mismatch: the 1m mark was timestamped after the daily close, so the Strategy never paired daily BTC/SOL bars with marks and never submitted. The causal fix uses the last closed 1m mark at the UTC daily close for the decision, then the first closed 1m execution candle strictly after submit for matching. No synthetic bid/ask spread was restored: equal bid/ask is an explicitly labelled 1m-close proxy with unknown BBO/slippage/liquidity. Exact corrected-v0 diagnostic: 265 native fills, 1,385 modelled funding events, native fees 6254.82187750, terminal ACTIVE/TOTAL 3877.49992841, terminal flat; data/config/policy hashes unchanged above. It remains NOT_FAITHFUL_DIAGNOSTIC/non-ranking because fees, BBO/liquidity/slippage, historical profile applicability, settlement marks, and liquidation are unvalidated. Targeted relevant tests: 6 passed; no optimizer.

P3 §5 reporting checkpoint 2026-09-20: full immutable config/provenance, code/data/config/policy hashes, explicit fixture-fee applicability, equity checkpoints, drawdown fields, funding counts, and fills/orders CSV artifacts with SHA-256 are now exported. Exact v0 report verifies 24 carry-forward calendar returns `2024-09` through `2026-08`; fills/orders artifacts each have 265 rows and hashes. Drawdown amount 8922.48446936, start/trough timestamps present, recovery explicitly UNKNOWN. Targeted report tests: 4 passed. Episodes and realized/unrealized remain explicit UNKNOWN because native/domain has no authoritative export; signed funding remains UNKNOWN because durable audit stores post-account totals, not cash deltas. Still NOT_FAITHFUL_DIAGNOSTIC; no optimizer.
P3 update 2026-09-20: replaced the unsupported test-only MID mark bars in the one native strategy and diagnostic runner with explicit `CustomData` `VenueMark` streams. Nautilus 1.231 unwraps the wrapper before strategy delivery; marks are subscribed through a dedicated data client and are never quote/trade/book input, hence never matching input. A native test holds executable quotes fixed while custom mark 100→200 changes marked equity by 100 and native tier IM/MM upward without another fill. Strategy increases now use the same captured-profile tier policy and fail closed on absent/stale marks or unavailable selected leverage; reductions remain `reduce_only`. Current public tier snapshot and selected BTC 40x/SOL 20x are diagnostic assumptions, not historical applicability evidence; historical fees remain unverified. The diagnostic interval is fixed to [2024-09-01, 2026-09-01) with [2022-09-02, 2024-09-01) feature-only warmup. Final daily diagnostic uses a per-run durable funding journal (not reused across fresh native accounts), funding strictly inside its captured 1m mark interval, and terminal `reduce_only` close. Its local ignored artifact `runtime/var/data/runs/native-diagnostic-report.json` says `NOT_FAITHFUL_DIAGNOSTIC`/non-ranking: 425 fills, 2,270 funding posts, native fees 297142.38547490 USDT, ACTIVE 272442.22075162, RESERVE 0, TOTAL 272442.22075162, 0 open positions. These are a model diagnostic, not ROI evidence, because current profile/fees and minute settlement/liquidation assumptions are not historically verified.
Decision: new isolated `runtime/` package, CPython >=3.12,<3.13, `nautilus_trader==1.231.0`; legacy execution is not imported.
Implemented: immutable public Bybit BTCUSDT/SOLUSDT and Hyperliquid meta snapshots, SHA-256 manifest, explicit account/historical-data unknowns, and hash-validation test.
Evidence: `runtime/var/venue-manifest.json`; raw payloads have real UTC capture timestamps. Bybit has 8h funding interval and public tiers; HL payload establishes BTC 40x/SOL 20x public maxima only.
P1 evidence: `runtime/coinmaster/research/native_fixture.py` uses one native BacktestEngine with BTC/SOL perpetuals; 5 native fills, partial BTC reduce, SOL add and final closes reconcile to hand-calculated `15632.05 USDT` after fees. A separate tier lifecycle opens and reduce-only closes BTC and SOL; native reports are closed, open orders are empty, and both native IM/MM entries clear.
Margin: native model gives BTC 80k/40 + SOL 80k/20 = 6k IM; an 1,100-SOL increase is fail-closed pre-submit against native free account balance. Reservations retain parent IM through partial fill until confirmed cancel. The Bybit module accepts only manifest-hash-validated full BTC/SOL 35-tier profiles plus explicit marks; selected leverage is required and forms an IM floor. Unchanged quotes with mark 75k→80k cross the BTC 300k boundary, ending in native IM 3,200/MM 1,090. Missing, stale, or out-of-range mark/tier data rejects increases and never leaves stale/default native margin; reductions still close.
Funding: a native FundingRateUpdate alone does not change account money in this pin. `PerpetualFundingModule` is a small supported SimulationModule using only `exchange.adjust_account`; it requires a durable SQLite journal, writes the stable event ID/rate/settlement mark/native total audit after each native posting, and skips that ID on a reopened-journal replay. Signed fixture postings give `14346.303 USDT`; production events fail closed without confirmed venue settlement mark.
Journal: SQLite WAL records funding IDs/audit and rejects transfers that change TOTAL; it validates native ACTIVE+RESERVE snapshots and has no matching/PnL calculation. It is not claimed crash-atomic across `adjust_account` and the journal insert; venue restart reconciliation remains P4.
Tested: `UV_CACHE_DIR=/private/tmp/coinmaster-uv-cache uv lock --check`; CPython 3.12.13; `nautilus=1.231.0`; adapters expose funding plus `QueryAccount`/`AccountState`; `.venv/bin/python -m pytest -q` (14 passed; Pandas deprecation warnings).
Assumed: public maxima are not account eligibility; real importer must normalize a stable venue funding ID and confirmed settlement mark.
VPS read-only inventory: `coinmaster.service` inactive; Docker active with unrelated gateway/postgres/VPN containers; no production action taken.
Coverage: required interval is [2024-09-01, 2026-09-01), warmup starts 2022-09-02. Bybit boundary probes exist for BTC/SOL mark+funding, but gap-free coverage is UNVERIFIED. HL has funding and nonzero-volume candles at research start; its warmup funding is empty and candle provenance remains unverified, not a standalone funding blocker while flat.
Blockers: account fee/margin/risk tiers, limits, complete price/funding history and USDT↔USDC conversion cannot be verified without further data collection/account evidence; no keys were requested or read.
Correction used: declared Hatch wheel package explicitly after editable build initially could not discover `coinmaster/`.
P2: local-only authenticated FastAPI `/api/v1` and SQLite-WAL control store are implemented with immutable complete baseline configs, decimal-string money, profile/preflight warnings, run status/cancel/report, terminal ACTIVE/RESERVE/TOTAL, and idempotent no-runtime commands. React now exposes only Strategy/Research/Runtime through this API; Strategy now exposes the complete candidate controls, with CSV arrays committed only on blur and nullable stop/loss fields explicitly toggled to `null`. Fixture reports are labelled `SYNTHETIC_P1_FIXTURE` and not rankable, while real baseline requests are BLOCKED pending P3 strategy/data evidence. Browser loopback smoke verified authenticated immutable save, saved-config selection, Bybit preview (`IM 5530.5000000`, `MM 2555.5000000` under the synthetic request), synthetic fixture completion/report (`15632.05000000`), and the explicit real-backtest BLOCKED state; listeners and temporary SQLite data were removed after the check.
P3 checkpoint: `coinmaster.domain.wave_overlay` now has causal EMA/beta/relative/z calculation, completed-wave linear quantiles, and a fill/cancel-driven episode state that emits intents but never substitutes fills. It covers liquidation lockout, original-quantity BTC levels, persistent SOL rights, frozen sigma and staged SOL exits. Same-day phases are fill/terminal driven: BTC reductions earn SOL rights only on actual fill, then terminal state permits SOL additions; cancelled zero-fill levels retry no earlier than the next decision. `coinmaster.strategy.wave_overlay` is the one native Strategy adapter: it submits native orders only from domain intents and reports confirmed native fills/cancels back to state. SOL additions are quote-notional until causally converted to native SOL quantity; reductions/group closes use reconciled native quantities and `reduce_only`. The adapter pairs four execution/mark bars by exact UTC session, sets daily open/close from native event time and availability from the latest native init time (rejecting a bar that arrived before its close), uses account cash plus position PnL marked by paired mark bars at decision time, and closes a group only after native cache is flat. The native P1 funding/margin fixture remains the only completed native money lifecycle; liquidation model, execution costs, and baseline report are not yet implemented.
P3 data: read-only Bybit raw snapshots and Parquet are local under ignored `runtime/var/data`; content-addressed raw manifest SHA-256 is `5f64e9100f31604bcdae34aa89ffc308a6bc7e5798f38baa07de7edcfb096fff`. For [2022-09-02, 2026-09-01), BTC and SOL each have 1,460 daily kline and mark bars with no reported daily gaps. All actual funding rows are retained: BTC has 4,380 events with 4,379 observed 8h intervals; SOL has 4,740 events with 4,259 observed 8h and 480 observed 2h intervals, plus 360 timestamps off the assumed fixed 8h grid. This is irregular-vs-assumed-8h evidence only, not proof of an official schedule change. Daily mark does not prove every funding settlement mark or intraday liquidation execution, so any current baseline would be labelled NOT_FAITHFUL_DIAGNOSTIC. Current tiers/account fees remain UNKNOWN and are not inferred as historical facts. Hyperliquid comparison remains conditional on requester-pays inventory/billing evidence; no credentials or bulk archive download used.
P3 1m data: `coinmaster.research.bybit_minute` stores raw content-addressed pages plus resumable cursor state and reports only completed ranges. One-day BTC/SOL execution+mark smoke has zero gaps. Original PID `71759` was verified absent on 2026-09-20; after confirming no other importer, a single managed resume handle completed only `SOLUSDT/mark` from page 600. All four streams now complete at 1,052 pages; final manifest has zero gaps/duplicates and hashes BTC execution `9c48e97c…`, BTC mark `5ba0ba60…`, SOL execution `fd5392f1…`, SOL mark `ceeb158d…`. Minute closes are explicitly coarse for intraminute liquidity/liquidation and funding settlement marks.
P3 native integration: `tests/test_wave_native_integration.py` runs `WaveOverlayStrategy` in one `BacktestEngine` with four causal daily streams (LAST plus native-compatible MID test stand-in for production mark), native quote fills, and native account report. A sub-step SOL conversion is floored to the native increment and fail-closed. `python -m coinmaster.research.native_baseline` is the only baseline entrypoint and exits `BLOCKED`/2 until the final 1m manifest is complete, gap-free, and hashed; it currently reports `MISSING_1M_MANIFEST`, thus no ROI is calculated or labelled.
Tests: CPython 3.12.13 / Nautilus 1.231.0; `compileall`, `pytest -q` 37 passed; `UV_CACHE_DIR=/private/tmp/coinmaster-uv-cache uv lock --check` passed. Next: source-verify historical fee/profile applicability and funding settlement marks, validate intraminute liquidation/execution, then promote only a faithful baseline toward optimizer input.

P3 optimizer 2026-09-20: `coinmaster.research.native_optimizer` runs a bounded deterministic native-only grid (seed 0) through a fresh `BacktestEngine` and funding journal per candidate; it never uses a second PnL/matching simulator. v0 control records prior report hash `a68062578e4ce3080314164a6cbe5fb3b3afd1f10c2f4907b8e57639a9a74f25`, current code/data/profile hashes, full search space and all result rows. Artifacts are ignored local `runtime/var/data/runs/native-optimizer-report.json` and `native-optimizer-results.csv`. Under the explicit current-profile assumption only, v0 TOTAL 272442.22075162; best `btc_notional_minus_10pct` TOTAL 316359.71901860, delta +43917.49826698. Other totals: +10% 217194.22367804; z−0.125 247505.88503623; z+0.125 171145.24218523. All candidates are terminal-flat, but all remain NOT_FAITHFUL_DIAGNOSTIC/non-ranking for live.

P3 correctness correction 2026-09-20: prior optimizer ranking is superseded but retained. Corrected domain now uses exact prior calendar z windows including invalid values, blocks SOL new risk on invalid current beta, stages reductions→adds→exits (timeout after an eligible add), carries regime-vs-trail close reason through confirmed group-flat reconciliation, enforces max gross/marked ACTIVE and caps SOL parent notional. Fresh-journal corrected controls artifact `runtime/var/data/runs/native-optimizer-corrected-controls.json`: v0 TOTAL 20148.28857331 / fees 80252.95064140 / fills 474 / funding 2444; former best BTC −10% TOTAL 32118.08614457 / fees 85118.48342670 / fills 474 / funding 2444. Both terminal-flat and NOT_FAITHFUL_DIAGNOSTIC; do not treat earlier 272442/316359 rankings as canonical.

P3 corrected refinement complete 2026-09-20: no trading-rule changes after `6dc6e30`; one resumable fresh-engine/journal pass over BTC multiplier {6.48, 7.29, 7.695, 8.10, 8.505, 8.91} plus rerun v0. Canonical ignored artifacts: `native-optimizer-corrected-refinement.json` SHA-256 `b984c61e2da113d38619250f85a1f1fb6508bec484e019ebb0d9b3b2ada7b345`, CSV SHA-256 `41cde9d0b898386cef1221ef85facf5a0843d2fd5eec1138b0311f9c634a52e2`. Best within explicit assumptions is BTC multiplier 6.48: TOTAL 61769.73800273, delta +41621.44942942 vs corrected v0 20148.28857331; fees 86977.81041630, 474 fills, 2444 funding, zero rejections/open positions. An independent fresh 6.48 rerun reproduced the same totals/counts. Stop P3 optimizer here; still NOT_FAITHFUL_DIAGNOSTIC/non-live-ranking.

P4 partial 2026-09-20: added local-only `coinmaster.ops.paper.PaperRuntime`: SQLite WAL one-owner lock, durable idempotent pause/flatten paper commands, snapshots and stale/unreconciled fail-closed health. It never loads credentials or sends live orders. Full local suite 38 passed. VPS read-only inventory: Python 3.12.3, no existing CoinMaster repo/unit/secrets, ports 80/443/22 and unrelated loopback services; 12G free disk. Pushed `e341e90` and cloned only feature branch to mandated `/root/.openclaw/workspace/coinmaster/coinmaster`; `/opt` and existing services untouched. Paper service NOT started: remote has no `uv`, and no public-adapter-backed persistent worker yet; do not install runtime or claim paper smoke without a separately approved/provisioned pinned runtime.
Safety: no legacy engines, deployments, VPS changes, orders, transfers, or live activation were run.

P4 native-node implementation 2026-09-20: `coinmaster.ops.native_paper_node` now builds exactly one Nautilus `TradingNode`: public `BybitDataClient` BTCUSDT/SOLUSDT and read-only public `HyperliquidDataClient` BTC/SOL, with the sole execution route `SandboxLiveExecClientFactory` / `SandboxExecutionClient` on BYBIT. No Bybit/Hyperliquid execution factory is imported or registered. It scrubs BYBIT/HYPERLIQUID private-key/secret/wallet environment variables before adapter construction and asserts the factory/config allowlist at startup; health always reports `live_order_capability=false`. Public instruments are provider-loaded into the native cache before Sandbox connects. The 730-day public daily manifest is a hard warmup gate; missing/stale quote/mark/funding state produces `DATA_STALE/PAUSED`, blocking increases. Funding observations are explicitly `UNPOSTED_REQUIRES_CONFIRMED_SETTLEMENT_MARK`; no sandbox auto-funding assumption. `PaperRuntime` now de-duplicates native order/fill/funding/account/position identities across restart. Local isolated native Sandbox TradingNode self-test has five native BTC/SOL fills, reduce-only closes, native account report, and zero open positions; it uses no service journal or venue endpoint. Local worker smoke exercised the real provider path and correctly stayed `DATA_STALE/PAUSED` when provider initialization returned `ValueError`; process health remained available. Local full suite: 42 passed. Next: commit/push this exact implementation, deploy the existing loopback-only paper unit to VPS, and verify a VPS isolated self-test plus health/journal/service state; no live action.

P4 deployment evidence 2026-09-20: VPS canonical checkout is `989f2486ad63a9a1bb6c3087cbb2ae048adf2ed9` on `codex/nautilus-btc-sol-mvp`; only `coinmaster-paper.service` was restarted. Its prior HTTP worker ignored SIGTERM, so systemd restart initially remained in `stop-sigterm`; the specific paper-unit process was force-stopped and the same unit started cleanly. Final systemd state is `active/running`, listener is loopback-only `127.0.0.1:18181`, and `/health` reports `TradingNode`, `BybitDataClient` + `HyperliquidDataClient`, only `SandboxExecutionClient` / `SandboxLiveExecClientFactory`, `live_order_capability=false`, and four READY native BTC/SOL quote+mark+funding feed states. An isolated VPS native sandbox self-test (no service journal) reported 5 fills, 0 open positions, and native account report present. Durable `/var/lib/coinmaster-paper/paper.sqlite` has `paper_lock`, `paper_commands`, `paper_events`, and `paper_snapshot` with WAL/SHM files; no Bybit/Hyperliquid execution factory source matches. The worker is intentionally `DATA_STALE/PAUSED`, `safe_for_increase=false`: no validated >=730-day manifest exists on VPS. No live orders, transfers, production service/container/proxy/firewall changes, or credentials were used/read. Remaining blocker: provision a manifest-backed public historical warmup in the paper runtime, then validate an actual paper strategy lifecycle/restart reconciliation; current observer intentionally submits no entries.

P4 integration checkpoint 2026-09-20: local implementation registers the existing `WaveOverlayStrategy` alongside health-only `FeedObserver` in the one BYBIT sandbox node. It hydrates 1,460 verified BTC/SOL daily bars without replaying orders; public Bybit daily bars and `MarkPriceUpdate` drive UTC-close causal decisions, with marks kept out of matching. Default config is explicit `corrected-v0` (hash reported); `research-6.48` requires explicit selection. Paper snapshots now reconcile restored sandbox state, journal native strategy events, and normalize funding into a replay-safe **modelled/unposted** ledger: Nautilus 1.231 `SandboxExecutionClient` has no supported live cash-adjustment hook. Local full suite: 48 passed. Next: commit/push, deploy only `coinmaster-paper.service`, and verify graceful restart, loopback health, warmup, strategy registration, journal/reconciliation; no live execution.

P5 runtime sidecar 2026-09-20: added a separately loopback-bound, authenticated FastAPI runtime projection on `127.0.0.1:18182` and static built SPA serving. It reads the paper SQLite journal read-only and relays only durable `pause-new-entries`, `resume-new-entries`, and `flatten-paper` commands to the existing paper worker using a distinct relay token. The OpenAPI contract has strict named Pydantic responses for runtime state, decimal-string balances, strategy, feeds, events, funding, commands, and cursors; generated frontend types are deterministic. The worker refuses any non-paper mode or execution client other than native `SandboxExecutionClient`; flatten remains a no-op while flat. Local tests: runtime suite 52 passed; TypeScript check and web build passed. Local built-UI smoke renders the authenticated Runtime tab with PAPER badge, UNKNOWN balances when no worker is connected, modelled/unposted funding state, and fail-closed worker warnings. No live/private execution or credentials are enabled.

P5 deployment 2026-09-20: commits `1f5f9ab` + `05e51da` are pushed on `codex/nautilus-btc-sol-mvp` and VPS canonical checkout is `05e51da`. The static built SPA is isolated at `/var/lib/coinmaster-runtime/web` (hashes verified) and `coinmaster-runtime.service` is enabled/active on `127.0.0.1:18182`, serving only that path. `/api/v1/runtime` is 401 unauthenticated and authenticated state is paper / `DATA_STALE/PAUSED` with modelled-unposted funding; root serves the app. `/etc/coinmaster-paper.env` is 0600 with newly generated distinct runtime/relay tokens, never printed. Critical safe blocker: the pre-existing running `coinmaster-paper.service` did not have a relay token in its inherited environment, so command forwarding returns fail-closed 503 until that service is deliberately restarted to read it. Per scope, it was not restarted: sidecar restart proved paper remains `active` with unchanged MainPID. No proxy/firewall/TLS/other service or live execution changed. SSH access: `ssh -L 18182:127.0.0.1:18182 root@46.225.163.123`.

P5 controls completion 2026-09-20: explicit follow-up authorization allowed a controlled restart of only `coinmaster-paper.service` to load its new relay token. MainPID changed `2215457` → `2223630` as expected for that one restart; it then became `active`, worker health `READY`, `mode=paper`, `live_order_capability=false`, execution client allowlist only `SandboxExecutionClient`. Authenticated loopback sidecar calls `pause-new-entries`, `resume-new-entries`, and `flatten-paper` each returned `ACCEPTED`/`mode=paper`. Flatten was a verified flat no-op: runtime API reports `positions=[]`, `orders=[]`. No proxy/firewall/TLS/other service or live/private capability was changed. Final source/deploy checkpoint pending commit after this record.

P5 funding freshness correction 2026-09-20: exact stale root cause was one `120s` transport TTL incorrectly applied to quote, mark, and scheduled funding alike. `FeedBook` now keeps quote/mark at 120s, but treats funding per venue: Bybit uses `next_funding_ns` or the captured verified 8h manifest cadence, and Hyperliquid (whose adapter supplies no next timestamp) uses the official documented hourly funding window; both retain a 120s post-window fail-closed grace. Tests explicitly cover valid intervals and true expiry; targeted 15 passed, full runtime suite 54 passed. VPS deployed final source commit `5a4421f` with a restart of only `coinmaster-paper.service`; `coinmaster-runtime.service` remained active. Four feeds stayed `READY` in 14 loopback health samples over ~130s, beyond the former false 120s funding threshold. Final authenticated snapshot: `READY`, `mode=paper`, `live_order_capability=false`, only `SandboxExecutionClient`, `positions=[]`, `orders=[]`. This fixes monitoring freshness only and does not make the system live-ready.

P5.2/P6 hardening 2026-09-20: `PaperRuntime` now has an isolated test for an open Sandbox-group snapshot with a simulated crash after submit but before durable order ACK: a matching restored native cache reconciles, while a missing order fails closed; open orders never make increases safe. Added `runtime/scripts/paper_doctor.py` (read-only, always `live_gate=BLOCKED_PAPER_ONLY`) and `runtime/ops/PAPER_RUNBOOK.md` for loopback monitoring, online backup, disposable restore drill, incident response, and explicit no-live gate. Targeted verification: 16 passed. VPS at `da731f8` ran an online SQLite backup via the standard SQLite backup API into an isolated drill directory; backup and disposable restore copy had matching SHA-256 and doctor reports `OK`/WAL/reconciled/zero positions-orders/`BLOCKED_PAPER_ONLY`. `coinmaster-paper.service` and `coinmaster-runtime.service` stayed active; neither was stopped. Current documentation now names Nautilus paper as active and Freqtrade as historical. No trading policy or live capability changed.
