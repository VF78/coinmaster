# Generate a fresh local token; do not commit its value.
COINMASTER_API_TOKEN=
# The P2 control API is intentionally local-only by default.
COINMASTER_API_HOST=127.0.0.1
COINMASTER_API_PORT=8000
COINMASTER_ALLOWED_ORIGIN=http://localhost:5173
