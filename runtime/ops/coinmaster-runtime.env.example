# Root-owned 0600 at /etc/coinmaster-runtime.env. This is intentionally
# separate from trader and paper environments; do not add venue credentials.
COINMASTER_RUNTIME_API_TOKEN=replace-with-local-operator-token
COINMASTER_PAPER_CONTROL_TOKEN=replace-with-worker-relay-token
