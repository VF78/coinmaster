# CoinMaster GUI migration audit — 2026-09-23

Scope: reuse the checked-in React components for the sealed Stage-G BTC/SOL Nautilus research and the credential-free Hyperliquid public-data / native-Sandbox dry run. This is an interface and API audit, not a change to strategy or execution. The running HL instance is `hl-stageg-testnet`, despite its historical service name; it uses public MAINNET data and local Sandbox only.

## Existing surfaces and disposition

| Existing surface | Useful behavior to retain/adapt | Remove or exclude from the Nautilus UI | Required data/API gap |
| --- | --- | --- | --- |
| Legacy `DashboardPage` | Account summary, open positions/orders, feed age, recent activity; reuse `Card`, `Stat`, `Badge`, `DataTable` and the responsive layout. | FVG evaluator, manual bias toggles, old Hyperliquid account-credential status, manual confirmation controls and live-exchange equity labels. None describes the Stage-G Sandbox. | Dedicated read-only HL instance projection: identity/hash, process/feed/warmup gates, native Sandbox cash/equity if actually available, orders/positions and timestamps with provenance. Never map an absent balance or PnL to zero. |
| Legacy `HistoryPage` | Paginated, filterable fill table and formatting. | Old `getLiveHistory` and inferred `sourceExchange=hyperliquid` fallback; these would mislabel virtual events as exchange trades. | Durable native fill details (time, instrument, side, quantity, price, maker/taker, fee, realized PnL when verified), cursor and explicit `SANDBOX` provenance. The current journal exports only event IDs/kinds. |
| Legacy `BacktestPage` | Date-range input, progress/history, candidate comparison, parameter controls and readable reports. | Freqtrade hyperopt/Trading Rules/one-asset/FVG/SL-TP form and its JS/legacy backtest path. No second PnL engine. | Native Nautilus job schema for interval, verified data snapshot/hash, immutable candidate, bounded staged search, cancellation, TOP-20 sorted by terminal ACTIVE+RESERVE, fees/funding/fills/DD and artifact hashes. Current `research` job runs a single native baseline; `backtest` returns an immutable reference, not a new run. Do not expose a working Optimize button until this exists. |
| Legacy `SettingsPage` / `TradingRulesPage` / `AlphaRadarPage` / `WaveEnginePage` | Shared React primitives and optional read-only diagnostics where source provenance is explicit. | API-key entry, Freqtrade strategy/radar controls, Trading Rules hot reload, Wave Engine/FVG settings and Telegram setup from the Stage-G MVP navigation. Preserve files; do not delete unrelated legacy features. | No MVP API coupling to Nautilus. Live credentials and real-account controls belong to a separately approved later stage. |
| Current Nautilus `StrategyPage` | Typed candidate fields, immutable save, version catalog and preflight layout. | Stale text saying `corrected-v0` is the running default; free-form edits must not imply hot promotion. Existing HL preflight always reports obsolete `UNKNOWN_TIERS`. | Show sealed Stage-G identity and exact candidate/code/policy hashes; separate research draft from running instance; venue-aware public-profile preview with explicit unknown account facts; explicit audited promotion only in a later stage. |
| Current Nautilus `ResearchPage` | Immutable catalog, owned subprocess lifecycle, status/cancel. | Synthetic fixture and reference buttons from the main optimization workflow; no claim that a catalog reference starts a native run. | Real native optimizer job orchestration and verified ranked results; preserve one worker/lease and isolated artifacts. |
| Current Nautilus `RuntimePage` | Basic feed table and pause/resume/flatten pattern. | Current `/api/v1/runtime` reads the older `coinmaster-paper` DB/worker. Pointing this page at it would show the wrong instance. Hide commands until the HL process has an authenticated, instance-bound command channel. | Separate read-only HL status projection; explicit instance selector; `UNKNOWN` for unavailable cash/margin/PnL; durable event cursor. The HL worker currently logs status but exposes no API endpoint to the sidecar. |

Two SPAs exist: `App.tsx` is the current Nautilus Strategy/Research/Runtime UI; `CustomApp.tsx` is a legacy Freqtrade/Radar UI. The large legacy Dashboard/History/Backtest pages remain checked in but are not mounted in the current Nautilus navigation. Migrate selected components and interaction patterns into `App.tsx`; do not route it to the old Express API.

## Ordered work and acceptance

1. **Read-only Runtime/Dashboard bridge.** Add a narrowly scoped, authenticated projection for the isolated HL Sandbox instance without giving the API ownership of Nautilus orders or access to its private writable DB. Show identity, candidate/code/policy hashes, dry-run mode, `live_order_capability=false`, fresh/warmup/feed gates, process status, positions/orders/events and verified account fields. Display explicit `UNKNOWN`/`UNPOSTED` where no observed value exists. Reuse Dashboard/History components. Test disconnected/stale/empty/open states and prove it never reads the older paper instance as HL.
2. **Strategy page.** Show exact sealed Stage-G candidate as the running immutable version, research drafts separately, venue/currency and 10,000 USDC virtual-seed assumption. Correct the stale HL preflight copy and wire public margin previews only to real profile evidence. Saving a draft never changes the running strategy.
3. **Research/optimizer workspace.** Retain owned-process native BacktestEngine jobs and immutable artifacts. Add bounded staged optimizer job configuration and truthful progress/cancel; compare control/winner and TOP-20 by terminal TOTAL, with ACTIVE/RESERVE, ROI, maker/taker fees, funding, fills, liquidation count and DD. Block new runs when genuine source coverage is absent. Keep diagnostic results visibly non-faithful/non-live-ranking.
4. **Commands and deployment.** Add pause/resume/flatten only after a dedicated authenticated, idempotent HL Sandbox command channel and fail-closed tests exist. Deploy the SPA/API separately from the trader; verify authenticated access, stale/error states, no trader restart and no real-exchange execution capability. Domain/TLS work is a later operational step, not a prerequisite for local GUI adaptation.

No Freqtrade package, JS PnL engine, exchange credential form, live-order action or new GUI stack is required. D5 natural virtual entry/exit/account evidence remains in progress and must not be manufactured by the GUI. The warmup artifact must be refreshed and revalidated at every UTC day boundary independently of this GUI work.
