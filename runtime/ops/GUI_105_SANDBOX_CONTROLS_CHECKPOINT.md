# GUI #105 — Sandbox controls / acceptance checkpoint

Status: local fail-closed slice implemented; #105 and parent #100 are not accepted or deployed.

- Audited `hl-stageg-testnet`: its worker serves only loopback `GET /status`; POST/PUT/DELETE are 405. The existing `/api/v1/runtime/commands/{command}` relays to the separate `coinmaster-paper` worker. There is no authenticated, idempotent native pause/resume/flatten path bound to this instance, nor a proven promotion/restart/recovery path. No legacy relay was connected to the HL GUI.
- Added an authenticated, typed, read-only `/api/v1/instances/hl-stageg-testnet/controls` contract. Pause/resume/virtual flatten/promotion remain disabled with exact blockers; stale/disconnected projection is itself a blocker. The Runtime page shows these blockers, requires a future explicit flatten confirmation/recovery design, and never submits a command. Removed only the unreachable `LegacyResearchPage`; the separate custom build still passes.
- Verification: full runtime suite 214 passed (72 existing Pandas warnings); TypeScript check, web/custom builds, and 28 UI invariants passed. Temporary isolated loopback API: controls without token 401, with dummy token 200 and all actions disabled, POST 405, built SPA 200. After the user's Chrome extension blocked interactive automation, a separate headless Chrome checked 1440px and 390px with local fake open+filled, stale, and disconnected projections (six asserted states). It found a 390px overflow on long native status codes; scoped text wrapping fixed it, and all six checks then had no horizontal overflow. These are fixture/browser checks, not deployed or economic evidence.
- Read-only VPS preflight on the user-supplied host: `coinmaster-runtime` PID `3653324` and `coinmaster-paper` PID `3653323` both use the canonical checkout at `caa4cdb`; it is 61 commits behind this local branch. The active Stage-G trader PID `3671128` uses independent `/srv/coinmaster/runtime` with the Sandbox-only execution factory; no private-key variable names were found in its process environment (only the `COINMASTER_LIVE_ENABLED` flag name from the checked set). However the deployed trader worker has no GET status listener at `127.0.0.1:18183` (only runtime `18182` listens), so the new projection/controls API would remain unavailable. Its worker source SHA differs from this local version. Updating the shared canonical checkout would also replace code used by active paper; that is not a safe isolated GUI/API deploy. PIDs remained unchanged; no remote files or services were changed.
- No deployment, trader restart, live credential/account/order/transfer, virtual flatten, or complete virtual entry/exit occurred. Do not mark #105/#100 Done. Next gate: stage an isolated runtime API/SPA release and the read-only trader status-listener update under a separately verified rollout/rollback plan; implement and test a durable instance-owned command protocol (including restart recovery and read-after-write) before enabling controls. Then repeat deployed browser QA and reconcile a full virtual entry/exit to native records.
