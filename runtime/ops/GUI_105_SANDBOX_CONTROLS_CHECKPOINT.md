# GUI #105 — Sandbox controls / acceptance checkpoint

Status: local fail-closed slice implemented; #105 and parent #100 are not accepted or deployed.

- Audited `hl-stageg-testnet`: its worker serves only loopback `GET /status`; POST/PUT/DELETE are 405. The existing `/api/v1/runtime/commands/{command}` relays to the separate `coinmaster-paper` worker. There is no authenticated, idempotent native pause/resume/flatten path bound to this instance, nor a proven promotion/restart/recovery path. No legacy relay was connected to the HL GUI.
- Added an authenticated, typed, read-only `/api/v1/instances/hl-stageg-testnet/controls` contract. Pause/resume/virtual flatten/promotion remain disabled with exact blockers; stale/disconnected projection is itself a blocker. The Runtime page shows these blockers, requires a future explicit flatten confirmation/recovery design, and never submits a command. Removed only the unreachable `LegacyResearchPage`; the separate custom build still passes.
- Verification: full runtime suite 214 passed (72 existing Pandas warnings); TypeScript check, web/custom builds, and 28 UI invariants passed. Temporary isolated loopback API: controls without token 401, with dummy token 200 and all actions disabled, POST 405, built SPA 200. Chrome desktop DOM showed the disabled controls and unknown money/funding labels while unauthenticated. Browser automation stopped when another Chrome extension UI took control before authenticated/mobile checks; no deployed-API browser evidence was obtained.
- No deployment, trader restart, live credential/account/order/transfer, virtual flatten, or complete virtual entry/exit occurred. Do not mark #105/#100 Done. Next gate: implement and test a durable instance-owned command protocol in the native worker (including restart recovery and read-after-write), then repeat desktop/mobile and deployed browser QA; deploy only SPA/API separately with PID, Sandbox-only allowlist, rollback and unchanged trader/research verification.
