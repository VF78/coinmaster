## 2026-02-14 — Daily notes
- Hetzner VPS `coinmaster-prod-01` (public IPv4 `46.225.133.161`) всё ещё не даёт SSH-доступ из среды агента: `Permission denied (publickey,password)`.
- Владимир заходил через Hetzner Web Console под `root`; в `/root/.ssh` обнаружены оба файла: `authorized-keys` (с дефисом) и `authorized_keys` (с подчёркиванием).
- Проверка `ssh-keygen -lf /root/.ssh/authorized-keys` показала fingerprint **SHA256:5Ro14/aev3QIpHisDGlK2IdYkrX9VXCfAoffU/cuoMs** (ключ совпадает с локальным `~/.ssh/id_ed25519.pub`).
- Гипотеза/фикс: `sshd` по умолчанию читает `authorized_keys` → нужно скопировать содержимое `authorized-keys` в `authorized_keys`, выставить `chmod 600`, при необходимости `systemctl restart ssh`, затем подтвердить fingerprint уже для `/root/.ssh/authorized_keys`.
- Chrome Browser Relay для Hetzner оказался нестабильным (вкладка то не видна, то цепляется не туда; ловили Hetzner security check/login). Пока опираемся на Web Console + короткие команды.
- Стратегическое решение подтверждено: перенести OpenClaw бота на VPS для 24/7 (prod на VPS + dev/backup на ноутбуке).
- После команды пользователя "готово" SSH всё равно не заработал; следующий шаг — проверить fingerprint именно для `/root/.ssh/authorized_keys` (а не `authorized-keys`) и при несовпадении снова сделать `cp /root/.ssh/authorized-keys /root/.ssh/authorized_keys && chmod 600 /root/.ssh/authorized_keys && systemctl restart ssh`.
- По запросу Владимира отправлен полный weekly RU executive summary по OpenClaw для криптотрейдинга файлом в Telegram: `openclaw-crypto-weekly-2026-02-14-ru.txt` (в workspace).
- По запросу Владимира отправлен полный weekly RU executive summary по OpenClaw для НЕ‑крипто доходов файлом в Telegram: `openclaw-noncrypto-weekly-2026-02-14-ru.txt` (в workspace).
- Уточнена причина SSH-путаницы: вход под `root` по SSH на VPS отключён намеренно (`PermitRootLogin no`), рабочий доступ теперь через `coinmaster@46.225.133.161` по ключу.
- Подтверждено, что `coinmaster` уже имеет `sudo` без пароля (`/etc/sudoers.d/coinmaster`, права 440), дополнительный шаг из Web Console не требуется.
- Проведена самодиагностика: `openclaw status --deep`, `openclaw security audit --deep`, `openclaw update status`, health cron-jobs, `npm run check/build` — критических сбоев не выявлено.
- Выявлен операционный пробел на VPS: HTTPS ещё не поднят (443 не слушает), сейчас домен работает по HTTP через nginx->127.0.0.1:8787.
- Стартована следующая задача разработки: deterministic historical replay (добавлен `src/core/replay.ts`, расширен `runSimulationStep` под replay timestamp/mode, добавлен API `POST /api/replay/run`, smoke-test успешен).
- Разобран инцидент Telegram-спама `⚠️ Agent failed before reply`: причина — cooldown/rate-limit у `openai-codex` в isolated cron задачах + fallback к моделям `openai/*` без API key.
- Антиспам-фикс применён: для `coinmaster:btc-open-alert-telegram` и `coinmaster:hourly-progress-telegram` переведён `delivery.mode=none`, отправка в Telegram теперь через `message` tool внутри payload; добавлены `model=openai-codex/gpt-5.3-codex`, `thinking=low`, ограничение timeout.
- По прямому указанию Владимира усилен process: текущая рабочая задача должна всегда быть в GitHub Project `In Progress`; для длинных задач — hourly Telegram апдейты с блоком тестирования; после завершения — отдельное сообщение + demo/приёмка + перенос следующей задачи в `In Progress`.
- Владимир отменил приоритет replay на текущем этапе: фокус смещён на production-ready контур и подключение Hyperliquid аккаунта для controlled live проверки на небольшой сумме.
- В GitHub синхронизирован фокус: issue #1 закрыт как выполненный (append-only trade event log), issue #6 переведён в `In Progress`, issue #2 (replay) оставлен в backlog/паузе.
- По новому решению Владимира пересобран план проекта в режиме "prod-first": до запуска делаем только P0 (infra+execution safety+account connectivity+manual confirmation), вторичное (расширенная статистика, replay-доработки и др.) переносим на post-launch.
- Владимир подтвердил стартовые live-ограничения: **30 USDC max notional**, **до 10x**, **manual confirmation ON**.
- Запрошен от CoinMaster самый простой безопасный формат передачи Hyperliquid trading credentials для немедленного подключения live-аккаунта.
- Владимир добавил Hyperliquid credentials на VPS в `/opt/coinmaster/.env` (права `600`, owner `coinmaster`); connectivity к account state/open positions подтверждена.
- На прод-сервере расширен Hyperliquid adapter (private account/trading path) и добавлены live API endpoints для controlled запуска с `manual confirmation`.
- На дашборде `coinmaster` выведены реальные данные аккаунта Hyperliquid (equity/available/open positions), добавлено отображение плеча в строке live-сделки.
- По вопросу Владимира пояснено: `source=sim` означает симуляционную/paper-сделку (не live execution).
- Владимир сообщил, что в `coinmaster24.com` видел «старые цифры»; выполнен UI-фикс: в верхнем блоке теперь приоритетно показываются **реальные live-метрики аккаунта Hyperliquid**, а симуляционная статистика явно помечена как `paper/sim`.
- Решение по текущей открытой live-сделке: **оставить в текущем виде** (без принудительного изменения позиции).
- По дополнительному запросу Владимира переделан верхний блок дашборда: старый `Performance overview` заменён на `Account overview (real, Hyperliquid)` с реальными account-метриками; заголовок интерфейса обновлён с `Paper trading` на `CoinMaster`.
- По запросу Владимира ограничен публичный доступ к `coinmaster24.com`: включена HTTP Basic Auth на уровне nginx (доступ только по приватным учётным данным владельца).
- По UI-запросу удалён тестовый раздел `Strategy positions (internal state)`; в `Live open positions (exchange)` добавлены поля `Deal value`, `Stop loss`, `Take profit`, `Opened at` (при недоступности данных показывается `—`).
- По новому требованию Владимира объединены account/performance-блоки в единый live-фокус: на дашборде оставлены только актуальные данные аккаунта Hyperliquid и текущих live-позиций, без тестовых/симуляционных блоков.
- Разобран инцидент доступа со второго ноутбука (`Firefox не может установить соединение`): корень проблемы — отсутствовал HTTPS (443). На VPS выпущен и установлен Let's Encrypt сертификат для `coinmaster24.com`/`www.coinmaster24.com`, настроен редирект HTTP->HTTPS, Basic Auth сохранён.
- По запросу Владимира выполнен HL-like terminal redesign: объединены/дедуплицированы account/performance блоки, оставлены только live-данные, усилена адаптивность (mobile-first, terminal grid, unified tokens/components). Базовые UI/UX правила и стиль зафиксированы в `PROJECT_TRUTH.md` как обязательный стандарт для всех следующих страниц.
- По новому циклу правок UI: заголовок сменён на `Trading copilot`, добавлен левый sidebar (`Dashboard` / `History` / `Settings`), удалён лишний top-nav шум, удалён `Instrument BTC-USD`, возвращены `Weekly P&L` и `Monthly P&L` в account overview, история вынесена в отдельную live-страницу (exchange fills), добавлена страница settings подключения к бирже.
- Исправлена логика account-метрик: `Available to trade` теперь рассчитывается от `accountValue - totalMarginUsed` (а не `withdrawable`), `Used margin` выводится напрямую из `totalMarginUsed`; это устраняет заведомо некорректное отображение margin в UI.
- Проведён production-рефакторинг backend/frontend: вынесена сборка live snapshot в отдельный модуль (`src/server/liveSnapshot.ts`), удалены дубли и неиспользуемые UI-элементы, отключены simulation/replay HTTP endpoints по умолчанию (`ENABLE_SIMULATION_API=false`, `ENABLE_REPLAY_API=false`), paper engine ingestion отключён по умолчанию (`ENABLE_PAPER_ENGINE=false`).
- Dashboard API сокращён до live-полей (`latestBias`, `latestTick`, `live`) без paper stats/activePositions; фронт адаптирован под single live view.
- По новой задаче Владимира развернут новый Hetzner VPS с US IP (`5.78.138.147`), SSH-доступ по ключу `id_ed25519` подтверждён.
- На новом сервере установлен `openclaw 2026.2.13` + подготовлены два изолированных профиля/daemon:
  - `main`: `~/.openclaw`, порт `18789`, systemd user unit `openclaw-gateway.service`.
  - `rescue`: `~/.openclaw-rescue`, порт `18790`, systemd user unit `openclaw-gateway-rescue.service`.
- Оба daemon-сервиса активны и включены в автозапуск (`systemctl --user is-enabled/is-active` => `enabled/active` для обоих), bind loopback + token auth; токены сохранены локально на сервере в `/root/.openclaw-secrets/*.token` (600).
- По запросу Владимира добавлен внешний SSH-ключ `openclaw-jay@vps` в `/home/coinmaster/.ssh/authorized_keys` на новом VPS (`5.78.138.147`), пользователь `coinmaster` создан, добавлен в группу `sudo`; fingerprint ключа: `SHA256:7xcKMJccsI3XueqUiKGeYDEmtVUNeSufHQBnzCAaAc4`.
- Выполнен fast QA-аудит по торговому API/рискам в `coinmaster`: `npm run check`, `npm run build` — OK; smoke endpoints `/api/live/order/limit` и `/api/live/leverage` подтвердили работу guardrails (max notional 30, manual confirmation, leverage cap 10) и корректную деградацию без ключей (`HYPERLIQUID_ACCOUNT_ADDRESS is missing`). Зафиксированы логические risk-gaps перед масштабированием live: нет auth на live endpoints, нет hard-stop по дневной просадке 20%, нет портфельного лимита плеча 10x/aggregate exposure, нет антидублей/idempotency для submit order.
- Владимир явно усилил приоритет quality/risk-check перед дальнейшим live: «всё тщательно проверить по логике, тестированию, ошибкам и рискам», т.к. на кону реальные деньги и потенциально большие суммы со временем.
- Уточнение Владимира: dual OpenClaw (`main`+`rescue`) не является частью контура CoinMaster; второй бот под отдельную задачу, при необходимости можно привлекать как вспомогательный ресурс, но не считать это базовой архитектурой CoinMaster.
- Реализован UX для управления risk-уровнями по открытой позиции: на Dashboard добавлена кнопка `Chart + SL/TP` в строке live-позиции; открывается панель с TradingView Lightweight Charts (свечи 15m), визуальными уровнями Entry/SL/TP, drag редактированием линий и применением уровней по подтверждению.
- Добавлены backend endpoints для этого потока: `GET /api/live/candles` и `POST /api/live/position/levels`; в адаптер Hyperliquid добавлена отправка trigger reduce-only ордеров (`tpsl: sl/tp`) для установки SL/TP после подтверждения.

### 23:00 — daily-project-truth-save
- Подтверждён режим **prod-first**: до первого controlled live закрываем только launch-critical задачи P0.
- Зафиксирован инфраструктурный статус: новый US VPS (`5.78.138.147`) с dual OpenClaw профилями `main`/`rescue` активен и включён в автозапуск.
- Зафиксирован критичный риск-блокер: в текущем контуре live API пока нет полноценной owner-auth защиты endpoints.
- Зафиксированы незакрытые safety-gaps перед масштабированием live: hard-stop дня 20% и агрегатный лимит портфельного плеча 10x пока не enforced в execution-контуре.
- Следующий обязательный шаг: закрыть idempotency/dedupe + pre-trade risk gates и только после этого согласовать окно первого controlled live запуска.
