# 2026-02-15

- Проведён форензик-инцидент по «самопроизвольному переключению модели» и «12+ часов статусов без видимого результата».
- Подтверждено, что в логах/форвардах смешивались два контура: `main` (Codex OAuth) и отдельный `rescue` (дефолт anthropic без auth), что визуально выглядело как непредсказуемый switch модели.
- Подтверждено, что в `main` были rate-limit/cooldown и failover-попытки на неподключённые fallback-модели (`openai/*` без API key), что усиливало шум ошибок.
- Подтверждено, что отчётный cron мог продолжать писать при отсутствии новых прод-артефактов деплоя; зафиксировано требование «только подтверждённый прогресс».
- Зафиксирована договорённость: до отдельного подтверждения новых подключений CoinMaster работает только на `openai-codex/gpt-5.3-codex`.
- В `PROJECT_TRUTH.md` и `MEMORY.md` внесены процессные правила token-efficiency и truth-gating уведомлений.
- Добавлен в план после текущей задачи owner-approved список UI/Trading Rules доработок (без изменения текущей цветовой схемы/стиля).
- Добавлено требование: completion-уведомление только после обновления статуса задачи в GitHub Project и назначения следующей `In Progress`.

## Инцидент 24/7 доступности / контуры (вечер)

- Пользователь уточнил: сервер `5.78.138.147` — **сервер друга**, не относится к CoinMaster. Любые упоминания `rescue`-контура оттуда считать ошибкой. Цель: 24/7 доступность через развёртывание полноценного OpenClaw на **нашем** VPS `coinmaster24.com` (`46.225.133.161`).
- На `5.78.138.147` выполнено best-effort отключение Telegram у чужого OpenClaw:
  - перезаписан `/home/coinmaster/.secrets/telegram_bot_token.txt` строкой `REVOKED-<timestamp>` (сделаны `.bak.*`),
  - в `/home/coinmaster/.openclaw/openclaw.json` выставлено `channels.telegram.enabled=false` и `plugins.entries.telegram.enabled=false`, удалены tokenFile/botToken.
  - Без sudo/без доступа к root service невозможно гарантировать моментальную остановку отправок до рестарта root-процесса `openclaw-gateway`.
- На прод-VPS `46.225.133.161` начато развёртывание OpenClaw для CoinMaster 24/7:
  - установлен `openclaw@2026.2.14`,
  - создан workspace `/home/coinmaster/openclaw-workspace`, загружены проектные файлы памяти/правил, `coinmaster` → symlink на `/opt/coinmaster`,
  - скопирован `auth-profiles.json` (providers: `openai-codex`, `anthropic`) в `~/.openclaw/agents/main/agent/auth-profiles.json` (600),
  - создан `~/.openclaw/openclaw.json` с модельной политикой: primary `openai-codex/gpt-5.3-codex`, fallback `anthropic/claude-opus-4-6` (alias `opus`),
  - установлен/запущен user-systemd `openclaw-gateway.service` (loopback+token).
  - Telegram на новом VPS оставлен **disabled** до передачи bot token; далее нужно перенести cron-уведомления на VPS и убрать дубли с Mac.
