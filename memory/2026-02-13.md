## 2026-02-13 — Daily notes
- В 23:00 задача `context:daily-project-truth-save` завершилась с ошибкой summary `fetch failed` (частично успела создать/обновить `MEMORY.md`).
- Пользователь спросил о статусе (почему тишина ночью).
- Восстановление: вручную обновлён `PROJECT_TRUTH.md` (timestamp, текущий статус и next steps).
- Торговый трек остаётся в приоритете: калибровка v1.1 после отрицательного baseline v1.
- Выполнена повторная ежедневная фиксация в коротком режиме (только локальные файлы workspace, без веб-поиска).
- Долгоживущие risk/MM-параметры v1 подтверждены без изменений (hard-stop 20%, cap SL 0.70%, partial 40/35/25, risk per trade 1.25%).
- По состоянию на фиксацию новых подтверждённых результатов бэктеста v1.1 не добавлено (TODO: внести после следующего валидного прогона).
- Пользователь попросил повторить вопросы, если требуется его ответ.
- Запрошены 6 подтверждений для фиксации v1.1: правило полного выхода остатка, тайм-стоп, формат bias-gating команд, режим подтверждения ордеров, критерии успешности BTC-этапа для перехода на ETH/SOL, риск-лимиты в мульти-режиме.
- Владимир дал решения по v1.1:
  1) полный выход остатка — только reverse-сигнал на 4H,
  2) тайм-стоп не используем,
  3) bias-команды: `BTC long` / `BTC short` / `BTC off`,
  4) production старт — ручное подтверждение (в тесте только информирование),
  5) критерий успешности BTC-этапа — стабильный положительный ROI 4–8 недель,
  6) суммарное плечо портфеля — до 10x.
- Важная правка методологии: пользователь подтвердил, что массовый бэктест по всем сигналам менее релевантен; основной фокус — виртуальная торговля по его bias-сигналам.
- Запущена сборка MVP `paper-trading-dashboard-mvp` (web dashboard + SaaS-ready архитектурный контур + история/статистика).
- Пользователь добавил дизайн-требование: использовать визуальный ориентир `https://app.hyperliquid.xyz` для trading dashboard.
- Реализован редизайн `coinmaster`: modern responsive UI + shared DTO foundation для простого native-порта (iOS/Android).
- По запросу пользователя выполнен автономный деплой-просмотр: backend настроен на выдачу собранного web-интерфейса с того же порта.
- Проведены локальные проверки (seed/check/build + API health/dashboard) и поднята публичная временная ссылка для просмотра результата.
- По запросу пользователя переименован проект интерфейса: `paper-trading-control-panel` -> `coinmaster`.
- Обновлены ссылки/имя пакета и перезапущен сервер из нового пути `coinmaster`; публичный preview-link сохранён рабочим.
- По новому запросу пользователя докручен dashboard `coinmaster`: добавлены Deposit size, проценты для Realized/Open PnL, селектор периода week/month, а в Active positions — количество монет и deal value.
- Обновлены backend/frontend контракты (shared DTO + period-aware stats), server продолжает отдавать web-build и API с одного origin.
- По запросу UX доработан дашборд: удалён блок Simulation tick из UI, добавлены проценты для Equity/Realized/Open PnL за выбранный период (week/month), в позициях добавлены Coins и Deal value.
- Реализован live price ingestion с Hyperliquid allMids (BTC) на сервере с автообновлением ~каждые 5 минут.
- Для просмотра поднята новая рабочая публичная ссылка (предыдущая истекла): `https://08ae3e9cb7a566.lhr.life`.
- По фидбеку удалён KPI Deposit size из UI, добавлен equity growth/decline % за выбранный период, а данные цены переведены на live ingestion (Hyperliquid allMids) с шагом ~5 минут.
- После истечения предыдущего туннеля поднята новая рабочая ссылка: `https://a991f045280c1e.lhr.life`.
- По запросу пользователя выполнен полный ретест контура `coinmaster`: `npm run check`, `npm run build`, `npm run start`, проверены `GET /` и `GET /api/dashboard` (200 OK).
- Поднят новый временный tunnel-preview: `https://7f1969d5939b45.lhr.life` (рабочий на момент проверки).
- Выяснено и подтверждено: free tunnel localhost.run не дает постоянный домен; для стабильного subdomain нужен аккаунт + привязанный SSH-ключ в `admin.localhost.run`.
- Сгенерирован SSH-ключ для туннеля: `~/.ssh/id_ed25519` (comment: `coinmaster@localhost.run`).
- В проект `coinmaster` добавлены и закоммичены утилиты для стабильного туннеля:
  - `scripts/tunnel-localhost-run.sh`
  - `docs/PUBLIC_LINK.md`
  - npm scripts: `tunnel`, `tunnel:fixed`
  - commit: `91c6535` (`Add localhost.run tunnel scripts and fixed-link runbook`).
- Для окончательной фиксации постоянной ссылки требуется действие Владимира: добавить `~/.ssh/id_ed25519.pub` в `https://admin.localhost.run`, затем запускать `npm run tunnel:fixed`.
- По запросу Владимира приоритизирован запуск тестового trading-движка (второстепенные задачи отложены).
- В `coinmaster` внедрён paper-engine под зафиксированные параметры v1: риск 1.25% от депозита на сделку, cap SL 0.70%, partial-фиксация 40%/35%/остаток на уровнях 1.0R/2.2R/3.8R, перевод стопа в BE после TP1.
- Обновлён adapter-trigger чувствительности для bias-driven paper режима (лучше подходит для получения первых результатов в горизонте нескольких суток).
- Тестовый прогон подтверждён: подан bias `BTC short`, открыта позиция, затем зафиксирован partial-событие `tp1_40pct_be` в audit log.
- По новому запросу пользователя: открытую paper-позицию принудительно остановили (`manual_stop_by_user`), активирован режим мониторинга BTC с bias `short`.
- Движок переключён на фиксированный размер первой тестовой сделки `0.15 BTC` (вместо расчётного размера) для ближайшего практического прогона.
- Поднят persistent сервис `coinmaster` через LaunchAgent `com.coinmaster.server` (KeepAlive), чтобы мониторинг не падал при закрытии терминала.
- Создан cron `coinmaster:btc-open-alert-telegram` (каждые 3 минуты, isolated), который отправляет уведомление в Telegram только при появлении новой открытой BTC-позиции.
- По запросу на оперативность обновлён feed цен: в `coinmaster` добавлен real-time ingest через Hyperliquid WebSocket (`allMids`) с fallback на REST-опрос каждую 1 минуту.
- Проверено на отдельном порту (8788): тики BTC обновляются в секундах, мониторинг работает в near real-time.
- Из-за конфликтов портов/рестартов launchd-инстанса временно использован стабильный running-процесс на `:8788` для оперативной работы.
- Восстановлен публичный доступ к дашборду через новый tunnel: `https://a5533bc9290917.lhr.life`.
- На момент фиксации: bias `BTC short`, активных позиций нет; система в режиме ожидания нового входного триггера с Telegram-уведомлением.
- Пользователь уточнил требуемую логику входа: ожидание ещё одного заброса цены к ~70000 и только затем открытие short на первом bearish engulfing на 5m/15m.
- Выявлена ошибка: софт открыл short (0.15 BTC) по временной упрощённой логике `momentum_down_hook` без обязательного условия заброса к 70k и без engulfing-подтверждения.
- Коррекция выполнена сразу: ошибочная позиция закрыта вручную (`manual_stop_wrong_trigger`), bias переведён в `BTC off` для блокировки повторного автовхода до внедрения правильной двухэтапной логики (zone arm + engulf trigger).
- По уточнению Владимира отменено обязательное ожидание заброса к 70k: вход должен мониториться сразу по поглощению.
- Зафиксировано точное определение short-триггера: локальный хай = high за последние 30 свечей (на текущем ТФ), затем снятие этого хая и следующая медвежья свеча закрывается ниже открытия предыдущей.
- Обновлены `PROJECT_TRUTH.md` и логика `strategyAdapter` в `coinmaster` под это правило (мониторинг 5m/15m; long — зеркально).
- По запросу Владимира очищена история ошибочных сделок в `coinmaster/data/db.json`; восстановлен baseline дашборда: deposit/equity = 1000, closed trades = 0, realized PnL = 0.
