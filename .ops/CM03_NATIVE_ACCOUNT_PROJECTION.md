# CM-03 native Sandbox account projection — source only

The Stage-G worker previously projected only Nautilus USDC balance_total. Native Sandbox position records already expose realized PnL including commissions, individual USDC commissions, and unrealized PnL at a mark. The new worker read model uses those records and the same native account for settled cash, free, locked, realized net of fees, fees, marked unrealized and equity. It adds unrealized to settled cash exactly once. The account's locked balance is not labelled initial or maintenance margin; those remain unknown. An open position with a missing, future or >120-second venue mark has null unrealized/equity and STALE_OR_MISSING. A flat account has known zero unrealized from its native positions.

The source does not rewrite the installed worker or its historical fills. The installed open BTC/TP worker still has historic zero-fee economics and cannot be restarted safely without CM-05 native open-state recovery. No retroactive correction is claimed. Funding scheduled by public updates is not a payment; the existing worker funding_state remains explicit, and posted funding is not added a second time by this projection. Realized position PnL is net of native commissions and does not include a separate funding claim.

Focused and connected checks: 14 passed, 4 seal-dependent cases deselected because the checkout's exact strategy hash does not match its approval. The new focused projection test covers entry, partial TP, adverse mark, stale mark and flat close with USDC values. Generated runtime OpenAPI and TypeScript contract updated. No deploy, restart, order or seal change.
