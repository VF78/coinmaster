# CM-05 live mode seam, source only (2026-09-26)

The existing `hl-stageg-testnet` identity still selects the native Nautilus 1.231 Sandbox adapter and its local USDC seed. A separate `hl-stageg-live` identity has its own state DB and warmup path, and its config selects `HyperliquidExecClientConfig` with `HyperliquidLiveExecClientFactory`. Config construction does not resolve credentials. The worker rejects live mode before opening state or building a node; direct node construction rejects it before `TradingNode.build`. No exchange call or live order is possible from this source state.

Pinned 1.231 Hyperliquid execution client sets `base_currency=None`. A native multi-currency `MarginAccount` fixture verifies exact USDC total/free/locked and distinct account-wide initial/maintenance margin. Missing USDC is rejected. The read-only live projection labels cash and equity `UNKNOWN`; it does not add position unrealized PnL to native balance. The tagged pinned adapter parser maps `crossMarginSummary.totalRawUsd` to native `balance_total`, top-level `withdrawable` to native free, and `totalMarginUsed` to account-wide margin. It does **not** map `accountValue` to native total. The unresolved economic blocker is an authoritative, complete account-value/equity mapping for the actual account type, with pending collateral and unrealized PnL reconciled before strategy sizing. Source: https://github.com/nautechsystems/nautilus_trader/blob/v1.231.0/crates/adapters/hyperliquid/src/common/parse.rs

The live `WaveOverlayStrategy` cannot be instantiated safely yet: its attached margin policy, funding sink, and verified seed are Sandbox specific; complete pinned live recovery reports are also unavailable. The exact strategy source and approval seal were untouched. No virtual order, position, cash, or episode is transferred. No push, deploy, service action, credential read, or exchange call occurred.

Validation: 24 passed, 2 pre-existing exact strategy-seal cases deselected, across focused mode/account tests, Sandbox money projection, pinned native flat/reopen connected lifecycle, and testnet config tests. The full Stage-G lifecycle pass remains blocked by the pre-existing `BLOCKED_SEALED_STRATEGY_MISMATCH` at current HEAD.
