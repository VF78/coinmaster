# CM-05 #110 — bounded Nautilus 2.0.0rc5 qualification

Date: 2026-09-26. This pass used only the pre-existing disposable /tmp/coinmaster-nt-rc5 wheel/source/venv. Official cp312 manylinux wheel SHA-256: eab45fafd2312deda1236554c49a9798bfc76bc8465af864878e2f70189ebebe; isolated Python reports 2.0.0rc5. Canonical active runtime/.venv, uv.lock, service, worker, approval seal, credentials, and real orders were untouched.

| Contract | Observed rc5 evidence | Qualification |
| --- | --- | --- |
| Report batch | Installed wheel exports model.ExecutionMassStatus with reports_complete and lookback_start. Rust HL generate_mass_status combines order, fill, position sweeps and sets the report window. | Promising native contract; consumer still must require complete coverage from its durable checkpoint. |
| Genuine empty / nonempty | Prior isolated public-account probe saw genuine empty order/position replies and fill incompleteness. Official extracted Rust fake-server tests cover nonempty orders/fills/positions below cap. | Wheel-level fake nonempty mass-status was not exercised in this pass. No takeover claim. |
| RuntimeError / malformed | Prior disposable proxy probes saw transport errors propagate; rc5 source returns incomplete sweeps for malformed/unresolved rows, and direct fill request errors on incomplete parsing. | Source and prior probe evidence only; mixed-row wheel-level acceptance remains unproved. |
| History cap | Rust mass-status marks reports_complete=false if recent historical orders or fills reach the 2,000-row venue cap; official fake-server tests assert this. | Source-level proof; wheel-level cap fixture remains unproved. |
| Lost ACK / partial restart | Existing 1.231 fake LiveExecEngine/Redis tests cannot run in rc5 unchanged. | No rc5 native-cache restart proof. Disposable Redis unavailable; none installed. |

Current imports versus rc5 wheel: TradingNode and live.node are gone in favor of LiveNode; TradingNodeConfig/LiveExecEngineConfig shift to LiveNodeConfig/LiveExecutionEngineConfig; model.data, model.currencies, model.objects, and model.identifiers are absent in favor of flat model exports; trading.strategy becomes trading.Strategy; old BybitLiveDataClientFactory and SandboxExecutionClient paths are absent in favor of BybitDataClientFactory and SandboxExecutionClientFactory. The new Hyperliquid Python API exposes a factory/config with base_url_http rather than a directly constructible Python execution client. At least 12 runtime source modules and 21 files including tests import moved native paths. The existing Sandbox fee subclass also depends on the old Python SimulatedExchange constructor and needs a separate supported rc5 money path; it cannot be silently carried across.

Decision: NO-GO-for-now for rc5 migration or recovery release. The report completeness design is better than pinned 1.231, but a wheel-level fake HTTP LiveNode mass-status test (empty, nonempty, malformed, capped, error) and a disposable-persistence lost-ACK/partial restart native-cache test must pass first. This is a multi-module node/model/adapter/fee migration, not a one-file import change. Do not add compatibility shims or alter the active Sandbox worker on source-level report evidence.
