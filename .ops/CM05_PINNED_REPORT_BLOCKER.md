# CM-05 #110 — pinned Nautilus 1.231 native report blocker (source-only)

Date: 2026-09-26. Checkout: codex/nautilus-btc-sol-mvp at 39d62d5 before this checkpoint. No credentials, private requests, worker action, adapter fork, dependency change, or recovery takeover were used.

Installed Nautilus 1.231 HyperliquidExecutionClient bulk methods in runtime/.venv/lib/python3.12/site-packages/nautilus_trader/adapters/hyperliquid/execution.py:371-463 catch asyncio cancellation, transport errors, and Python report-conversion errors, log them, and return empty lists. A credential-free fake transport invoked all three installed methods directly. Order, fill, and position report calls each returned the same [] for an actual empty reply and for a RuntimeError; only the side-effect log distinguished them. Inherited LiveExecutionClient.generate_mass_status (execution_client.py:440-519) gathers those lists into a non-None ExecutionMassStatus. Native LiveExecEngine.reconcile_execution_state (execution_engine.py:1670+) therefore cannot infer trustworthy empty from that mass status.

A tiny subclass that re-raises _log_report_error could distinguish Python-visible exceptions. It would not establish completeness: pinned request_order_status_reports, request_fill_reports, and request_position_status_reports expose parsed lists without an explicit complete/window/cap/malformed-row receipt. A nonempty partial list may omit fills, and an empty list cannot by itself prove flat. The existing LiveRecoveryReconciler requires owned order/fill/position/account parity and stays fail-closed on mismatch; it does not provide a missing transport completeness guarantee. Do not mark recovery_confirmed or enable management/entries from these bare lists.

Existing focused recovery tests: 19 passed, 3 skipped. The three skipped tests are the fake LiveExecEngine submit/lost-ACK and partial/restart process tests requiring a disposable loopback Redis backend; redis-server and redis-cli are absent on this host, and no Redis service or package was installed. They remain source test code, not an executed vertical proof. No broader connected integration was run after this hard transport-contract blocker.

Smallest safe next decision: choose one version-pinned native report path that returns explicit batch success and sufficient continuous fill-window completeness from the durable checkpoint, with malformed/truncated/error as failure; then test native LiveExecEngine reconciliation on disposable Redis with fake transport for accepted-before-ACK and partial-fill restart, followed by owned TP/SOL/exit without duplicate submit. Keep unexplained/foreign state RECOVERY_REQUIRED. Do not replace error with trustworthy empty, reseal approval, restart the open Sandbox worker, or authorize real orders on this evidence.


## Focused report completeness gate (2026-09-26)

A new test invokes the installed pinned 1.231 HyperliquidExecutionClient bulk order, fill and position methods with fake transport. For each method, genuine empty [], transport RuntimeError and malformed None all produce the same returned []; errors are logged but not carried in the report value. The command's start/end are not passed to the underlying request for any of the three methods. Two different requested fill windows consequently make identical unbounded calls. The returned list contains no page cursor, cap, source window, row count or completeness receipt. A nonempty prefix cannot distinguish an entire fill history from a capped/truncated prefix. The inherited mass-status aggregator accepts all three lists and cannot restore lost provenance.

Decision remains NO-GO for automatic recovery takeover from these pinned reports. Existing owned-order/fill/position/account reconciliation and fill cursor correctly reject many mismatches and duplicates, but they cannot certify that the venue report was complete or that [] means genuinely flat. No adapter subclass that merely re-raises Python errors can solve a silent partial/capped response. A native report path must expose explicit successful batch and continuous fill-window pagination before any complete gate can be truthfully implemented. No production adapter/reconciler hook or worker action was made.
