2026-09-26 CM-04 #93 source-only replay checkpoint: One isolated native TestClock engine with the unchanged WaveOverlayStrategy and 800 feature-only seed rows executes 22 live UTC sessions. Verified chain: BTC short entry d801, three owned maker TPs d806, SOL add d808, half exit d809, full exit d810, exact residual BTC forced close to flat d821, and a distinct new episode/entry d822. Native fill/position callbacks, quantity conservation, positive commissions, flat native cash/free/locked, and no duplicate fill are asserted in runtime/tests/test_wave_native_integration.py; artifact is .ops/CM04_NATIVE_REPLAY.md. Focused 1 passed; connected 63 passed, 4 seal-dependent cases deselected, 22 existing Pandas warnings. First IOC entry filled completely, so matcher-generated partial fill in this same chain remains a coverage gap; separate partial-cancel/zero-fill native callback tests pass. The replay's research fixture is USDT; it does not replace the separate CM-03 HL USDC proof. Existing exact strategy seal remains unchanged and blocks deployment, as does open-position restart recovery. No worker/order/release/service mutation.

2026-09-26 CM-02 #99 source-only lifecycle checkpoint: Proposed strategy SHA-256 25f566b258a8fbded644a16465d94f957a0c0c1124e5ef0795cc7362b9c83542 (unsealed); existing exact approval deliberately remains unchanged, so attachment is BLOCKED_SEALED_STRATEGY_MISMATCH and the two exact-seal assertions remain expected failures. No Candidate, execution policy, fee/margin/risk parameter, route, or accepted SOL-exit rule changed. A confirmed zero-fill BTC_ENTRY terminal now releases only an empty episode with no other pending risk; partial fills preserve the episode; unknown ACK stays pending and is not retried. Local pre-submit denials are recorded separately. Entry attempts are bounded by daily decision index and side; a later permitted daily decision can retry with a distinct intent identity. Strategy accepts each paired BTC/SOL UTC close once and in contiguous order, waits for late SOL/gaps, uses verified same-source warmup rows only as feature backfill without retroactive orders, and reports accepted/deferred session plus reason. Existing current-process paused-gate one-shot retry remains; after restart a deferred process-local decision does not replay and requires a fresh eligible signal under the fail-closed recovery gate. Focused/native tests: 56 passed, 2 exact-seal assertions intentionally excluded; final late-SOL/UTC targeted 4 passed; diff check clean. No worker release/restart, virtual or real order, deploy, staging, or push; current Stage-G virtual BTC plus TP remains owned by the running process. #99 remains In Progress pending owner review, exact-hash approval, and separate position-preserving recovery gate.

2026-09-26 CM-03 native Sandbox monetary source checkpoint (#112, P0): Nautilus 1.231 public HL BTC/SOL instruments are canonically USD quoted, USDC settled, and adapter maker/taker rates are zero; the prior USDC-only Sandbox could not calculate USD/USDC and produced zero/unposted native commissions. Proposed source now retains the official Hyperliquid data factory and canonical instrument metadata, seeds a model-only same-venue USD/USDC CurrencyPair plus 1:1 bid/ask quote into the native Cache under the already declared nominal peg assumption, and uses one version-pinned SandboxExecutionClient/SimulatedExchange with a native FeeModel that posts approved public-base 0.00015 maker / 0.00045 taker fees directly in USDC at full Decimal notional precision. Model FX absence blocks native order submit; synthetic pair orders are disallowed. Native isolated entry then genuine post-only maker BTC TP with a public instrument refresh: 0.27 USDC taker + 0.090015 USDC maker, open total/free/locked 9999.73/9999.54/0.19 USDC, flat final total/free 9999.739985 USDC and locked 0; losing maker exit and tiny/partial fee precision also pass. Funding stays OBSERVED_MODELLED_UNPOSTED; no second ledger, strategy rule, public route, or trading-policy hash changed. Focused 23 passed, relevant native integration 37 passed, and a separate read-only real public provider metadata plus isolated Sandbox lifecycle pass reproduced canonical USD/USDC metadata and exact native USDC fees/cash. This is source-only pending safe native position-preserving recovery: the installed Stage-G worker has an open virtual BTC position and TP, and was not restarted/deployed/ordered. #112 remains In Progress; no current natural-worker money claim is repaired retroactively.

2026-09-25 Stage-G deferred daily retry source checkpoint (#99): `WaveOverlayStrategy` now retains a paired flat daily session skipped while the entry gate is closed and re-evaluates it once from the next native quote after reopening; a native position clears it and clear-before-decide dedupes repeated quotes. No candidate, execution-policy, economic, route, or real-order change. Proposed strategy SHA-256 is `6ee2d33c296a9c00d273eb967ed0f5d544fc80793c479389ea275cffadfe53b3`; candidate/policy remain `637762...b6d` / `eee44...687`. Quote-callback regression plus native/domain integration tests: 32 passed; diff/compile pass (20 existing Pandas warnings). The current sealed approval pins `3d79e504...2f7`, so attachment correctly blocks `BLOCKED_SEALED_STRATEGY_MISMATCH`; no approval file, release copy, restart, or deploy occurred. This marker is process-local: a restart before reopening fails closed and can miss the session. Next: owner separately authorizes this exact hash under the sealed-approval workflow, then rerun full attachment/lifecycle checks and perform a guarded Sandbox-only release.

2026-09-25 GUI trader-workspace visual release (#100/#102/#103/#105/#108): c789fca is pushed and active in isolated /srv/coinmaster-native-gui/releases/c789fcaf0d6ffb45c3cf043edbed21a775b2cb44, assembled from exact active 0cfb2ce runtime baseline plus c789 web bundle only. Canonical guarded stage and activation passed authenticated API/SPA smoke, exact READY sealed projection, disabled controls, no paper route, and no research lease; active index.html SHA-256 matches the c789 build. Only coinmaster-runtime restarted (461914/NRestarts0); paper remains 3653323/NRestarts0 and Stage-G remains 452805/NRestarts0. Token-free worker status remains PUBLIC_FEEDS_READY with native cash 10000 USDC, zero positions/orders, live_order_capability=false, and sealed strategy SHA-256 3d79e50495de8b648045a43ecf4a6b320d4565bead3140e2455848a879c012f7; warmup timer active. TypeScript, 41 UI invariants, and both bundles passed. Isolated mocked fixture visual review covered desktop populated dashboard, 390x844 populated mobile, empty state, and mobile Strategy; no persisted screenshot file is available from the browser tool. Authenticated public browser acceptance remains unverified; no real orders/credentials/paper route/native control command changed.

2026-09-25 GUI production checkpoint (#100/#102/#103/#105/#108): Source checkpoints c3d7842, 4b18c2b, c2d31f8 and 0cfb2ce are pushed. The isolated GUI/API release 0cfb2ce is active at coinmaster24.com from the exact 32a runtime baseline plus the verified Stage-G UI/API overlay; only coinmaster-runtime restarted (PID 451884). A flat-state backup preceded the only Stage-G worker restart; the new PID 452805 has NRestarts=0, READY/PUBLIC_FEEDS_READY, sealed strategy SHA-256 3d79e50495de8b648045a43ecf4a6b320d4565bead3140e2455848a879c012f7, native Sandbox cash 10000 USDC, zero positions/orders, and live_order_capability=false. Paper PID 3653323 is unchanged. The worker rollback copy is /var/lib/coinmaster-hl-stageg-testnet/backups/gui-account-projection-20260925T0830Z/ (old worker SHA-256 89b72b519cfb92b0ac7e6040553f831b50417a3fdf51fb8a33faffcbfec805b6); installed minimal account-projection worker SHA-256 is 5d91445c6671b446d301647c08cafd3e603da0e7f01c39ae70a6a292b9ba5997. Focused projection tests 2 passed; TypeScript, generated API contract, UI invariant (41), and web/custom builds passed. GUI stage/activation smoke verified authentication, SPA, exact sealed READY projection and disabled controls with unchanged peer PIDs. Authenticated desktop/mobile browser acceptance remains unverified because the known client-to-VPS asset body timeout persists. No real orders, credentials, paper routing, Freqtrade route, or native control command were enabled; pause/resume/flatten remain visibly disabled pending the separate native lifecycle gate.

2026-09-25 Stage-G Sandbox maintenance deployment (#99): Scoped installed release uses exact 5bef600 maintenance source plus ead1329 lexical current-manifest path and 4dca174 status projection fix; installed sealed WaveOverlay SHA-256 remains 3d79e50495de8b648045a43ecf4a6b320d4565bead3140e2455848a879c012f7, Nautilus 1.231.0. Actual unit config points to /var/lib/coinmaster-hl-stageg-testnet/data/current/manifest.json; current immutable genuine Bybit artifact SHA-256 4b998fde471d7b7a80f310558a3bc434a96278ea7bf2118111f632787a679170 covers 1,483 BTC/SOL sessions through Sep23 completed UTC. Warmup timer first refresh succeeded Sep24 22:04 UTC, then remains active on 15-minute cadence. Stage-G PID 258525 had NRestarts=0 and five latest status cycles PUBLIC_FEEDS_READY, native node/strategy running, BTC/SOL feeds READY, orders_enabled and safe_for_increase true. Loopback /status HTTP 200 reports Sandbox-only, live_order_capability=false, virtual 10000 USDC, zero positions/orders. Latest flat journal read has no paper events/intents/funding or unwanted fills. Exact sealed-release isolated verification: 51 relevant tests passed, compile/diff clean. Rollback tar: /var/lib/coinmaster-hl-stageg-testnet/backups/stageg-maintenance-pre-5bef600-20260924T2153Z/installed-release-and-units.tar (SHA-256 f0a5d1df7e4342beb7c2c50c02c10e5534cbf85950b38ad3574d240239bcc248). Next UTC completed-session refresh remains to be observed; no virtual round trip yet. Native Sandbox open-exposure crash rehydration remains unsupported and fails closed. No real order capability or live recovery deployment.

2026-09-25 #110 isolated Nautilus 2.0.0rc5 qualification: official PyPI cp312-manylinux_2_34_x86_64 wheel SHA-256 eab45fafd2312deda1236554c49a9798bfc76bc8465af864878e2f70189ebebe and sdist SHA-256 d566bade9e8dc6ad35de0e2566658fbd1ae75a89ee2c45c8e48de5fcc6929399 verified; /tmp-only venv reports 2.0.0rc5. On read-only public account, native bulk order/position reports returned genuine empty, while fill reports raised Fill snapshot incomplete rather than a partial list. Loopback proxy refusal/429/500 raised ValueError for all three bulk report methods; canceled Future raised CancelledError. rc5 source uses ReportSweep.complete for malformed/unresolved venue rows and mass-status reports_complete=false when historical orders or fills hit the 2,000-row cap. Direct request_fill_reports does not itself carry cap completeness: takeover must consume validated mass-status window. Current #110 code is not rc5-compatible: model.data/model.currencies modules and BybitLiveDataClientFactory import are absent; native LiveNode/config/strategy state migration and fake transport crash tests remain. Mixed-row/cap wheel-level fixture acceptance and native restart proof are NOT complete. No pinned dependency/lockfile/runtime venv/service change; recovery gate closed. Prefer a separate deliberate rc5 migration qualification over an unbuilt private 1.231 Rust wheel on the 7.9GB-free production VPS. Upstream source: https://github.com/nautechsystems/nautilus_trader/tree/v2.0.0rc5/crates/adapters/hyperliquid/src .

2026-09-25 #110 read-only HL report/feed checkpoint: owner-supplied public master address queried through official POST /info only; clearinghouseState returned 0 positions, frontendOpenOrders 0, and userFillsByTime (last 7 days, unaggregated) 0. Association is not independently proven and empty replies do not validate nonempty parser/fee/cause behavior. Official Info docs cap userFillsByTime at 2,000 per page/10,000 recent; installed Nautilus 1.231 Python bulk report methods return [] on exception, and native Rust parsing can skip bad rows, so response completeness and manual-vs-liquidation close cause are NOT proven. Unexplained flat remains RECOVERY_REQUIRED, no synthetic close/lockout. Live recovery subclass now requires reconciliation-confirmed plus fresh BTC/SOL quotes and marks within explicit positive max_mark_age_ns before entries or any new reduce-only submit; restored state discards quote freshness. 60 relevant tests passed. One VPS only; no credentials, real orders, deploy, or recovery-gate opening. Next scoped blocker: version-pinned native adapter/report completeness semantics (including timeout, malformed row, history cap) and authoritative close cause before open-state takeover. Official API: https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint .

2026-09-25 #110 TP identity correction: zero-fill resting BTC TP recovery now checks intent side even without fill reports and requires the durably recorded native side/LIMIT/GTC/post-only/price plus open native status. Missing or altered shape fails closed before any episode/cursor commit; wrong side, price, type, TIF, status, and missing shape regressions preserve checkpoint bytes. 59 relevant tests passed with disposable loopback Redis; diff/compile checks pass. Architecture is one VPS only: local durable Redis/SQLite, native venue reconciliation, bounded systemd restart; no standby, second node, or offhost HA. The recovery gate remains closed pending full forced-close, native protective-management, fresh-feed, and sanitized adapter evidence. No service deploy, credentials, or real orders.

2026-09-25 #110 open-exposure ownership checkpoint: native report reconciliation now requires every checkpointed pending order to appear and match durable instrument/quantity/reduce-only fields plus native account, strategy, venue ID, side, fill set, position, and finite positive native USDC balance before any domain commit. A resting BTC TP reduce-only fixture proves owned-open-order validation; missing, foreign, altered reduce-only, and wrong-account reports leave episode/checkpoint unchanged. A position that vanishes without a coherent close fill/reason stays RECOVERY_REQUIRED; no synthetic liquidation/close. 53 relevant tests passed; compile/diff checks passed. This is still NOT autonomous takeover: sealed WaveOverlay has no standing downside protective stop (BTC TP is a reduction, not a stop), installed HL 1.231 adapter needs persisted Cache order for CLOID remap and returns empty reports on transport exceptions, and no sanitized real account/order fixture is available. Forced/venue-close reason, protective exit ownership, and fresh-public-feed management gate remain unproven; recovery_confirmed remains false. No credentials, real orders, deploy, or operational approval change.

2026-09-25 #110 acceptance-slice checkpoint (VPS UTC Sep24 21:04): fake native LiveExecutionClient durably accepts an order and hard-exits before ACK; restored TradingNode/Redis reclaims the exact generated ID with native partial fill/order/position reports and no duplicate submit. Independent uninterrupted native callback path and recovered path match native position, fill count, exact 0.27 USDC commission row, and USDC account cache total for identical fake-venue events. Cash is still an authoritative fake venue account event, not a second PnL calculation or proof of exchange cash correctness. Shared pure domain fill transition now replays SOL_HALF_EXIT cooldown and full-fill parent/pending/order mapping cleanup exactly once; entry gate remains closed. 48 relevant tests passed; compile/diff checks passed. Protective-order ownership/exit and forced/terminal close callback restoration remain acceptance blockers. No credentials, real orders, deploy, or approval change. Stage-G installed release remains old: at VPS UTC Sep24 21:04 PID 180775/NRestarts0, PUBLIC_FEEDS_READY, node/strategy running, BTC/SOL READY, orders_enabled/safe_for_increase true, Sandbox-only and live-order capability false. Genuine 1,483-session Bybit artifact ends at Sep24 00:00 UTC (latest completed Sep23); refresh timer is absent, so next UTC rollover will require maintenance rollout approval or fail-close.

2026-09-24 #110 P1 recovery correction: full scoped native account/order/strategy/side/trade-set/quantity and position parity are validated against a detached versioned episode before one atomic checkpoint-plus-trade-ID commit. Invalid reports leave durable bytes, cursor, and in-memory episode unchanged. Normal WaveOverlay fill callbacks now commit the same applied trade ID and updated episode atomically before follow-on decisions; a repeated historical report after restart cannot double the domain quantity. Real Redis/TradingNode and negative tests plus native strategy/config checks: 46 passed; compile/diff checks passed. The fake transport still supplies venue state instead of accepting a real submit, and its account cash is a supplied report, so uninterrupted-versus-recovered execution/fee/cash parity remains unproven. No deploy or live order authorization; Stage-G worker unchanged.

2026-09-24 #110 live-first source checkpoint: a credential-free native Nautilus 1.231 TradingNode recovery slice now persists versioned WaveOverlay episode state atomically with the pre-submit intent and replays missing domain fill transitions from verified native report/cache identity with a trade-ID cursor. A real disposable Redis cache and fake report-only execution transport proved crash-before-ACK, partial BTC fill, native account/position/order reconciliation, exactly-once episode transition across two restarts, and zero duplicate submits. 44 relevant tests passed; compile/diff checks passed. This is a fail-closed proof, not operational activation: recovery_confirmed stays false; absent/conflicting reports block takeover, and complete live management/entry gates remain future work. Current Stage-G service is unchanged; no credentials, real orders, approval update, or deploy.

# ACTIVE_TASK

2026-09-25 Stage-G native-mark compatibility checkpoint (#99): Installed Nautilus 1.231 introspection proves `MarkPriceUpdate(instrument_id, value, ts_event, ts_init)` exposes `value`, not `price`. The live mark bridge now reads `value` and the native regression constructs that exact four-argument object while proving mark-first liquidation is invoked. This repairs the previously failing funding-observation callback without changing any trading rule, parameter, stop, execution route, seal, deploy, service, or real order. Proposed strategy SHA-256 is `f890667eb14a3e399f36325554ffa237dc548af54d5a62395517ae67fd94d4a0`; the temporary release-seal adjustment was reverted and no approval file changed. Focused native callback/funding tests: 15 passed (20 existing Pandas warnings); compile and diff checks pass. Full runtime suite: 253 collected, 221 passed, 29 failed, 3 skipped. Relative to the established 30-failure baseline, the only resolved node is `tests/test_hyperliquid_testnet.py::test_next_funding_timestamp_is_observation_only_not_an_early_cash_post`; the remaining 29 match baseline failures.

2026-09-25 Stage-G lifecycle accounting correction (#99): Replacement BTC TP leaves now remain capped, completed, and proportioned against a persisted original per-level target (`confirmed BTC initial quantity × configured TP fraction`), so repeated partial fill/cancel cycles cannot falsely complete a TP or leave unsold BTC. Prepared native funding now precedes the current-process owned-TP exception and blocks any increase. Recovery decoding accepts older episode checkpoints without the new target map and derives it only from their immutable confirmed entry quantity and candidate fraction at the next fill. No strategy/economic parameter, standing stop, execution route, seal, deploy, service, or real order change. Focused PaperRuntime/domain/native-callback/live-recovery suite: 67 passed (20 existing Pandas warnings); changed modules compile and diff checks pass. Full runtime suite: 253 collected, 220 passed, 30 failed, 3 skipped; the exact 30 failure node IDs match the baseline previously established at `2089bcd` (including six expected unsealed strategy-hash assertions).

2026-09-25 Stage-G lifecycle source checkpoint (#99): The current-process native Sandbox lifecycle now admits increases while open orders exist only when every visible order is a reconciled, acknowledged, durable owned `BTC_REDUCE` with matching instrument and `reduce_only=true`; unknown, non-reduce, non-BTC, unacknowledged, stale, or restarted state remains fail-closed. The actual Nautilus 1.231 `MarkPriceUpdate` bridge now uses `price` and runs the existing mark-first liquidation check. Partial BTC entry IOC cancellation queues confirmed targets, partial BTC TP cancellation requeues only unsold target leaves, and a second daily SOL timeout terminals its duplicate rather than orphaning or replacing the first mandatory exit. No strategy/economic parameter, standing stop, execution route, seal, deploy, service, or real order change. Focused PaperRuntime/domain/native-callback suite: 48 passed (20 existing Pandas warnings); changed modules compile and diff checks pass. Full runtime suite: 253 collected, 220 passed, 30 failed, 3 skipped; six failures are the expected unsealed strategy-hash attachment assertions, while 24 are pre-existing unrelated API/process/ignored-fixture failures. Current strategy source therefore remains `BLOCKED_SEALED_STRATEGY_MISMATCH` until the owner separately authorizes an exact new hash under the sealing workflow.

Updated: 2026-09-24 Europe/Moscow
Status: ACTIVE — Stage-G virtual-only worker currently runs the older isolated release; recovery reliability changes are committed source preparation, not acceptance of open-state takeover. Owner pivot prioritizes one live-ready native engine with fake transport before any real order authorization.
2026-09-24 reliability checkpoint: Canonical source prepares verified incremental Bybit daily warmup refresh (15-minute no-op until a missing UTC tail; immutable hash-validated artifact and atomic current pointer), same-unit-env preflight, dynamic readiness revalidation, native journal revision crash barrier, conservative flat native USDC cash handoff, native-thread health and orderly shutdown. Focused Stage-G/paper/config/refresh checks: 57 passed, 2 unrelated native-paper tests blocked by absent ignored historical paper fixture; compile, systemd unit verify, diff check passed. Real isolated Sep-23 Bybit artifact remains READY with 1,483 BTC/SOL sessions and manifest SHA-256 4b998fde471d7b7a80f310558a3bc434a96278ea7bf2118111f632787a679170. Installed and canonical Nautilus are 1.231.0, pinned by pyproject/uv.lock (Linux wheel SHA-256 8c438e95c275a13df0c0ddb7012c462708b5e99ff3612e36a1b7bd49ab39c216). Current service PID 180775, zero restarts, state DB flat, zero intents/events/funding. The open-state Sandbox matching engine cannot be restored by cache persistence or Strategy hooks; no production-like 24/7 takeover claim. Owner pivot: #110 tracks a version-pinned live-ready recovery proof with fake HL transport, same strategy/risk, and no live orders. Deployment of source changes remains separate and limited to safe maintenance, not experimental open-state handoff.

2026-09-24 19:13 UTC owner-authorized D5 recovery: commit `84ee851` pins the verified Sep-24 warmup and updates only the approved strategy-code SHA-256 to `3d79e50495de8b648045a43ecf4a6b320d4565bead3140e2455848a879c012f7`; candidate/execution-policy identities remain unchanged. The actual root-owned nonsecret `/etc/coinmaster/hl-stageg-testnet.instance.json` now matches committed config, and the isolated `/srv/coinmaster/runtime` release has matching hashes for four changed files; rollback copies are under the trader state directory. New artifact manifest SHA-256 `4b998fde471d7b7a80f310558a3bc434a96278ea7bf2118111f632787a679170` is readable by `coinmaster-hl` and verifies READY with 1,483 contiguous Bybit BTC/SOL daily sessions through Sep 23. The first start failed its strict path check because the host `/etc` instance still referenced the old artifact; only this worker was stopped, the host instance was corrected, the actual service-user gate passed, and it restarted cleanly. Current service PID `180775`, zero restarts after that correction; 16 status samples show `PUBLIC_FEEDS_READY`, BTC/SOL public feeds READY, `node_running=true`, `trading_strategy_registered=true`, `orders_enabled=true`, `safe_for_increase=true`, `prime_error=null`, `SEALED_APPROVAL_MATCH`, only `SandboxExecutionClient`, and `live_order_capability=false`. Native Portfolio observed total/free 10,000 USDC, locked 0. Durable snapshot is reconciled with zero positions/orders; events/intents/funding ledgers remain zero and current PID has zero tracebacks/RuntimeErrors. Paper/runtime services retained PIDs `3653323`/`3889311`; no real order, artificial signal, /opt edit, or other service restart. Focused config/money tests: 15 passed; prior native suite: 12 passed. #99 remains In Progress pending a natural virtual entry/exit and ongoing UTC-boundary warmup refresh.


2026-09-24 11:43 UTC Stage-G midnight repair checkpoint: canonical branch HEAD was clean `3bafa4b`. Fixed all three native account money lookups in `runtime/coinmaster/strategy/wave_overlay.py` to use the actual ledger base currency (USDC for HL Sandbox) and fail closed on missing balance/account; no strategy parameters, economic policy, or execution route changed. Prior strategy SHA-256 `1f9597c38f895bd64af48634c8fe5112b50df004a8a4b597dfb4fe5d6144c2da`; proposed new code SHA-256 `3d79e50495de8b648045a43ecf4a6b320d4565bead3140e2455848a879c012f7`. Candidate SHA-256 remains `637762130c76396cd7c6e24644e31b28079a1683e4460d57f103dde42c603b6d`; execution-policy SHA-256 remains `eee44dde50781b6364923e6aad8df714860796b0ec7cd6121adef7c25406f687`. The operational sealed approval file was deliberately not changed: auto-review rejected that security-sensitive gate update and prohibited workaround. Thus `SEALED_APPROVAL_MATCH` will fail for this proposed code until the owner separately authorizes the new exact hash. No release copy or service restart. Real public Bybit Sep-23 raw tail is at isolated state data path `bybit-tail-20260924` (raw-manifest SHA-256 `c7de2092ee63eab23cc61d7afb14912260f82f37bc59445c7591d1757dd7ef26`); new immutable `stageg-bybit-warmup-20260924-verified1/manifest.json` SHA-256 `4b998fde471d7b7a80f310558a3bc434a96278ea7bf2118111f632787a679170` validates READY with 1,483 BTC/SOL contiguous daily sessions through Sep 23, exact kline/mark coverage, zero gaps/duplicates/off-grid. Read-only durable SQLite: zero events, intents, commands, funding rows; sole snapshot positions/orders empty, reconciled=false after crash. Focused native money/strategy tests 12 passed (20 pre-existing Pandas warnings). Existing isolated service remains active PID 4004152 but node_running=false, orders_enabled=false, no fills. Required next step after exact approval: pin the new warmup path in strict config, validate gate and isolated release hashes, prove flat/no-intent state again, then controlled restart only of `coinmaster-hl-stageg-testnet.service` and verify public feed readiness/Sandbox-only state. GitHub Project #2 #97 remains In Progress.



2026-09-24 GUI #105 post-login white-screen diagnosis: the public HTTPS login succeeds (303) and the authenticated shell returns 200, but the browser keeps an empty #root because its JS module and stylesheet responses do not finish. Chrome Network showed JS 200 then net::ERR_TIMED_OUT during body download, and CSS 206 Partial Content then timeout; the module never ran and /api/v1/auth/session was never requested. A separate macOS curl using the existing Keychain login fetched only 16,131 of 179,106 JS bytes in 45 seconds before timeout (curl 28). Earlier authenticated loopback GET completed the full JS (179,106 bytes), CSS (55,394 bytes), and HL projection JSON with HTTP 200; the active release files match the HTML asset names. Nginx has a standard exact-Host reverse proxy, HTTP/2 and global gzip, with no asset-specific route or error-log evidence. SSH from the same client intermittently timed out during banner exchange. This isolates the observed blank page to incomplete client-to-VPS response-body transfer; no GUI JS/API source change or deploy is justified yet. The manifest request redirects to login and causes a separate PWA parse warning, but is not the blank-screen trigger. GUI API PID 3889311 and paper PID 3653323 were active; trader state belongs to the separate D5 investigation. No service restart, order, or transfer occurred. Next: verify path/VPN/PMTU or an independent client route, then repeat the authenticated asset and dashboard test before a targeted transport change. #105 remains In Progress.

2026-09-23 17:28:04 UTC deployed Stage-G funding checkpoint: the primary operator copied committed 4d63b5b source (SHA-256 43164535ed3baf0927dc8210cb9c26ac5b6e9b5f2f70ddb092677e189c7b108f) to the fixed /srv/coinmaster/runtime release after a flat/no-intent durable-state check and backup, then restarted only coinmaster-hl-stageg-testnet.service. The current worker PID is 3873200; warmup is READY, state PUBLIC_FEEDS_READY, and orders_enabled=true for local Sandbox only. Across 17 live status samples through 17:28:04 UTC, both BTC/SOL funding rates stayed at 0.0000125 while each funding age reset four times (max observed age about 14.2 seconds); current-PID errors were zero. Native Portfolio startup reported AccountBalance(total=10_000.00000000 USDC, locked=0). Focused routing/funding tests passed (2); the broader VPS suite had 191 passed and 25 failures tied to research-child process identity and absent ignored warmup fixtures, so it is not a green full-suite result. Longer live continuity and a natural virtual entry/exit remain pending; no funding settlement was posted. The 17:24 and earlier funding paragraphs below are historical pre-deploy checkpoints.

2026-09-23 17:24 UTC deployment correction pending: after deploying the prior aggregate-feed source, the live Stage-G worker logged that Nautilus could not route SubscribeData for HyperliquidAllDexsAssetCtxs to instance ID HYPERLIQUID-MAINNET-DATA (custom data has no venue). The installed 1.231 adapter exports native client ID HYPERLIQUID. Canonical source now subscribes that one custom feed with HYPERLIQUID_CLIENT_ID; focused routing/funding tests pass. This correction is not yet copied to the Stage-G release or restarted. Existing initial funding may keep the process temporarily READY; repeated aggregate receipts must be proven after controlled deployment. No real order or settlement posting.

2026-09-23 funding-feed correction, source-only checkpoint: Nautilus 1.231 suppresses unchanged Hyperliquid funding values on the shared activeAssetCtx stream, so the old funding timestamp ages out while fresh quotes/marks continue. Canonical runtime now subscribes once to native HyperliquidAllDexsAssetCtxs and records only exact BTC/SOL finite funding values with each real aggregate-frame receipt; older callbacks cannot overwrite newer observations. Missing/invalid instrument entries still expire at the existing hourly plus 120-second fail-closed window; no settlement is posted. Focused funding/Bybit tests: 2 passed. Full VPS runtime suite: 191 passed, 25 failed (research child process identity and absent ignored warmup fixtures; Stage-G lifecycle gate fails with absent fixture), 72 existing warnings. Source is not yet deployed to /srv/coinmaster/runtime; Stage-G service was not restarted or modified by this checkpoint. No repository-native guarded Stage-G deploy script exists, so release activation remains a separate controlled step after flat Sandbox and source/hash checks. No older paper/runtime service changed.

2026-09-23 14:11 UTC dry-run observation: `coinmaster-hl-stageg-testnet.service` is active (PID `3671128`, zero restarts since 07:52 UTC), native `TradingNode` and sealed Stage-G strategy are registered, 24-month warmup is `READY`, and local execution is only `SandboxExecutionClient` with `live_order_capability=false`. It was `PUBLIC_FEEDS_READY` from 07:52:47 to 08:54:50 UTC, then `DATA_STALE/PAUSED` continuously in 4,481 five-second status samples; `orders_enabled=false`. SOL HL funding age reached 378.5 minutes (documented readiness window 62 minutes), while its quote age was 0.1 seconds; BTC HL feed was `READY`. The latest reconciled Sandbox snapshot was fresh at 14:11:17 UTC and had zero positions/orders; `paper_events`, `paper_intents`, native funding and funding ledger tables all had zero rows. The separate older `coinmaster-paper` service is also active but reports `DATA_STALE/PAUSED` from SOL HL funding age >7 hours; its doctor is `OK` for journal integrity with zero positions/orders, not a trading-readiness pass. No adapter error/reconnect was observed in the recent non-status journal lines; this points to missing per-instrument funding updates or their handling, not a proven exchange outage. Do not claim virtual trade-cycle or ROI validation. Next: diagnose the SOL funding subscription/event cadence and correct it without weakening the fail-closed gate; #108 durable restart barrier remains prerequisite to any trader restart. No service, order, or trading state was changed by this read-only audit.

2026-09-23 13:56 UTC GUI #109 public-host acceptance: `https://coinmaster24.com` now has an exact GoDaddy A record to `46.225.163.123`, a valid single-host Let's Encrypt certificate (expires 2026-12-22), dedicated HTTP 308/ACME and HTTPS Nginx vhosts, and the isolated GUI API release `63bce35` running as PID `3790995`. The existing operator password was copied into the new domain's macOS Keychain item, its root-only VPS verifier was loaded by restarting only the GUI API, and the primary task verified public HTTPS login 200, logout 200, then session 401 without exposing the password. Anonymous API/assets return 401, unknown API stays JSON 404, malformed cookie returns 401, the public proxy strips Authorization, and the guarded API smoke confirms disabled HL controls and no paper command route. The retired `cm.f-ai.studio` Host returns HTTP 421 and rejects TLS rather than falling through to Paperclip. Exact-Host after-checks for `app.f-ai.studio` and `f-ai.studio` retained their valid certificate fingerprints, redirects, and page hashes from before the Nginx changes. Paper/trader PIDs `3653323`/`3671128` remained unchanged; no real order or transfer occurred. GitHub Project #2 issue #109 is Done. The user explicitly deferred GUI content/UX rework to separate future work; do not treat this hosting/auth acceptance as approval of the GUI's content. Earlier #109 blockers below are historical and superseded.

2026-09-23 13:31 UTC GUI #109 approval-provenance update: the user replied `разрешаю` in the coordinator task for the exact guarded GUI release transfer to the existing VPS. The same `deploy_native_gui.sh stage` command was retried once, but automatic approval review rejected it because the approval reached this delegated task only through untrusted delegation content. A focused read of the source task did not surface a direct user message to this task. No workaround or alternate transfer path was used; the prior release, certificate, and Nginx state remain unchanged. The source `7c9f7ff` and checkpoint `6d62fc8` are pushed. The two existing sites' after-state TLS fingerprints, redirects, and page hashes matched the pre-change baseline. Next step requires the exact approval to be available as trusted user input in this task or an approved equivalent; then run the same guarded stage/activation and continue the apex TLS acceptance. Project #2 issue #109 remains In Progress.

2026-09-23 13:28 UTC GUI #109 apex-host checkpoint: the user changed the final public host to `coinmaster24.com`. GoDaddy authoritative `ns17`/`ns18` and public `1.1.1.1`/`8.8.8.8` all returned A `46.225.163.123` (owner screenshot TTL 600); no AAAA or restrictive CAA was seen. Read-only Nginx inspection found no exact apex vhost or certificate: apex HTTP/HTTPS currently falls through to the existing Paperclip default, matching an unknown Host, so there is no apex-specific app to replace. Baseline exact-Host checks for `app.f-ai.studio` and `f-ai.studio` showed valid TLS and their intended HTTP redirects/content; neither site was modified. Focused source commit `7c9f7ff` changes the GUI Origin, release-stage smoke, tests, Keychain provisioning fallback, Nginx templates, and runbook to the apex, with an explicit deny vhost for retired `cm.f-ai.studio`. It was pushed; full runtime 216 passed (72 warnings), 28 GUI invariants, TypeScript and both builds passed. The existing operator password and root-only verifier were preserved; credential reprovisioning was rejected by automatic approval review and is unnecessary for the host-only change. The guarded GUI release staging transfer to `root@46.225.163.123` was rejected twice by automatic approval review as private artifact egress even after read-only proof of the existing isolated target; do not retry through another path without explicit approval. No source archive, Nginx vhost, certificate, service, paper/trader process, or public route changed on the VPS. API release `975cff1` remains active at PID `3762561`, paper/trader PIDs `3653323`/`3671128` unchanged, Project #2 issue #109 In Progress. Next exact step after approval: stage/activate only the isolated GUI release via the guarded wrapper, install/test the apex HTTP ACME vhost, issue its exact-host certificate, enable/test HTTPS, compare `app.f-ai.studio` and `f-ai.studio` TLS/HTTP identities against their baseline, then replace the old `cm` HTTP redirect with the deny vhost and complete public browser/auth acceptance. The earlier `cm.f-ai.studio` DNS checkpoint below is historical and superseded.

2026-09-23 13:08 UTC GUI #109 DNS/TLS checkpoint: the DNS owner added A `cm.f-ai.studio` → `46.225.163.123` with TTL 300. At the bounded recheck, Timeweb authoritative `ns1`, `ns2`, and `ns4` served the A record at zone serial 20, but `ns3.timeweb.org` still served authoritative NXDOMAIN at serial 18; public resolver answers varied. Certificate issuance was not attempted while an authoritative server denied the hostname. The exact HTTP ACME/redirect vhost is enabled; the certificate is absent and the dedicated HTTPS vhost remains disabled, so the public GUI is not accepted. Isolated API release `975cff1` remains active at PID `3762561`; paper/trader PIDs `3653323`/`3671128` are unchanged. GitHub Project #2 issue #109 remains In Progress. Next exact gate: recheck all authoritative A/SOA answers and public DNS; once consistent, issue the exact-host certificate through `/var/www/letsencrypt`, enable HTTPS with `nginx -t` and exact-Host verification, then complete public desktop/390px login/logout, expiry/CSRF/401, disabled-control, and process-isolation acceptance. The prior no-A statement below is historical.

2026-09-23 GUI current checkpoint: #106 native bounded optimizer and #107 isolated GUI deploy are Done. #109 single-operator browser login is implemented and deployed as API-only release `975cff1` on `127.0.0.1:18182` (GUI PID `3762561`). Critical audit found malformed-cookie 500 and mobile login overflow; the one correction pass fixed both and verified deployed 401 plus a 352px login card without overflow at 390px. Desktop/390px login/logout checks, 216 runtime tests, 28 UI invariants, TypeScript, and both builds passed. Password is in local macOS Keychain; only its scrypt verifier is in the VPS root-owned environment. Existing Bearer automation remains loopback-only. Dedicated `cm.f-ai.studio` HTTP ACME/redirect Nginx host is staged, HTTPS host is disabled pending exact-host certificate. `cm.f-ai.studio` has no A/AAAA record; DNS owner must add A `cm` → `46.225.163.123` (TTL 300), then issue certificate and complete external acceptance before Project #109 can be Done. Public GUI URL is not yet available. Paper PID `3653323` and trader PID `3671128` were not restarted; no real order or transfer occurred. #108 safe trader listener/restart gate remains Todo. #105 and #104 remain In Progress. The immediately following GUI deployment checkpoint below is historical and superseded by this paragraph.

2026-09-23 GUI deployment checkpoint: #105 remains In Progress. Local read-only Sandbox controls and mobile layout are committed/pushed in `48e22dc`/`da64a9c`; full 214 runtime tests, TypeScript, both builds, 28 UI invariants and desktop/390px fixture-browser states passed. Critical Astra/medium audit found the deployed trader lacks GET `/status` and safe shutdown snapshot/drain, while old runtime and paper share a checkout 61 commits behind. User authorized controlled dry-run stop/restart only for safe deployment. Project #2 now has #107 (isolated API/SPA deploy, In Progress) and #108 (native listener and durable restart gate, Todo) under #105. #107 code/script commits `d404006`/`048cb7a` are pushed; full 215 runtime tests and staged API smoke passed. VPS activation hit an immediate startup readiness race and automatically restored the prior runtime unit; old runtime active PID `3728526` on port `18182`, paper PID `3653323` and Stage-G trader PID `3671128` unchanged. Production GUI is NOT deployed/accepted, no trader restart or orders occurred. Next bounded step: fix activation with a bounded readiness wait and retry-safe rollback in #107, then production browser acceptance; after #107, implement #108 durable flat barrier/listener before any trader restart. Pause/resume/flatten/promotion remain disabled pending a separate native command protocol.

2026-09-23 GUI #106 local checkpoint: branch HEAD `32ff75f` adds the minimal owned native optimizer job behind verified GUI readiness. The only search is five sealed Stage-G BTC multiplier variants on the existing Nautilus diagnostic engine; shared lease, immutable request, source/result SHA checks and TOTAL-ranked TOP-20 are implemented. No 24-month economic optimizer run, VPS GUI deployment, trader action or real order occurred. Executor's focused 70 Python tests, TypeScript, both builds and 26 UI checks passed; coordinator's full runtime suite passed 213 tests (72 existing Pandas warnings) using `PYTHONPATH=. uv run --frozen pytest -q tests` with access to own test subprocess. #106 is ready for local closure after push; #104 remains In Progress pending the broader staged workflow and #105 production acceptance remains Todo. Model routing is `.ops/MODEL_ROUTING.md`.

2026-09-23 GUI #103 local Strategy pass: added the authenticated, read-only `/api/v1/instances/hl-stageg-testnet/strategy` source-identity contract. It verifies the checked-in strict HL Sandbox instance, Stage-G candidate and strategy code against the sealed approval document, then exposes the exact candidate and candidate/strategy/execution-policy hashes. The Strategy page now treats that sealed `hl-stageg-testnet` identity as the only running instance and labels all editable persisted configurations as separate research drafts. It removes stale `corrected-v0` and Hyperliquid `UNKNOWN_TIERS` preflight framing from the active Strategy page. The public profile is described only as current public-mainnet tiers plus local Sandbox policy; account-specific margin and fee schedule stay `UNKNOWN`, funding stays observed/modelled/unposted, and the 10,000 USDC Sandbox seed versus 10,000 USDT research comparison is explicitly a nominal 1:1 assumption. Draft save has client validation, loading/error feedback and a list read-after-write; it never invokes a trader route or changes the running instance. Promotion is disabled with `SEPARATE_NATIVE_LIFECYCLE_GATE_REQUIRED`; its native lifecycle gate is intentionally not implemented here. Focused sidecar tests and generated TypeScript/UI invariants passed locally. No VPS, service, trader, draft-to-runner wiring, order, transfer, deploy, or GitHub Project update occurred.

2026-09-23 GUI #103 review correction: the Strategy endpoint now compares the local sealed candidate, strategy and policy hashes against a fresh GET-only worker projection. It returns `RUNNING_MATCH` only when the projection is fresh, process is not paused/disconnected, and all three hashes match. Stale/unavailable projections return `NOT_CONFIRMED`; a fresh differing identity returns `MISMATCH`. The UI calls the source a running strategy only for `RUNNING_MATCH`; all other states say “Local sealed configuration; running not confirmed.” Stopped the task's own local Vite/API dev-server (PIDs 14702 and its children) and restored only its generated `coinmaster/data/db.json` change. Focused sidecar tests: 6 passed; TypeScript, UI invariants (18) and both builds passed. No VPS action.

2026-09-23 GUI #104 local Research pass: the Nautilus Research page now adapts the old Backtest flow into a verified native baseline workspace: fixed manifest-backed period (2024-09-01 through 2026-09-01 exclusive), immutable saved configuration, owned subprocess launch, 3-second active-job progress refresh, cancellation, compact TOTAL-only result and native run history. `/api/v1/research/capabilities` reports only baseline availability/blockers and the fixed period; it does not return source manifests or logs. The local native baseline capability is `READY` with no coverage blockers, but no baseline/backtest/optimizer job was started during this pass. `native_optimizer.py` remains deliberately blocked in the GUI as `OPTIMIZER_JOB_PROTOCOL_NOT_IMPLEMENTED`: its current CLI lacks immutable job-request validation, typed progress/result envelopes, canonical one-worker lease binding, per-job private artifact placement and manager-verifiable artifact hashing. Existing native baseline jobs retain all those controls and revalidate config/coverage before spawn. No full logs or manifests are displayed. Full runtime verification: 210 passed (existing Pandas warnings only); TypeScript, generated API, UI invariants and builds passed. No VPS, trader, deployment, order, transfer or GitHub Project mutation occurred.

2026-09-23 GUI #102 correction: replaced the unusable `/run/coinmaster` file handoff with a GET-only status endpoint bound to `127.0.0.1:18183`; the runtime sidecar fetches it through loopback and has no HL command route. Read-only VPS inspection confirmed the deployed trader runs as `coinmaster-hl`, `ProtectSystem=strict`, writable only at `/var/lib/coinmaster-hl-stageg-testnet`, and `/run/coinmaster` does not exist. The deployed runtime unit is active but differs from the checked-in unit (host unit has blank `User` and runs from `/root/.openclaw/workspace/...`); no process or unit was changed. The API validates fixed instance identity, Sandbox-only capability and observation timestamp (15-second max age, 5-second future skew); dead endpoint returns `UNAVAILABLE`, stale returns `STALE`, and swapped instance/capability returns `INVALID`. Worker GET generates a current read model without polling/writing the journal; POST/PUT/DELETE return 405. Event projection returns the latest 100 records and global high-water cursor. Nautilus app makes the old shell/sidebar pattern explicit with branded header, grouped nav, mobile bottom navigation and Runtime landing page. Desktop and 390px mobile browser inspections passed after replacing the wide feed table with responsive cards; Runtime polls every five seconds and clears old data after fetch failure. Native cash/equity/margin remain `UNKNOWN`; funding remains `UNPOSTED`. No service restart/deploy or trading action occurred. Focused tests: 27 passed; full runtime suite: 207 passed (72 existing Pandas warnings); TypeScript/OpenAPI generation, UI invariant and both builds passed.

Checkpoint commit/push to `VF78/coinmaster` was requested, and read-only inspection confirmed `origin=https://github.com/VF78/coinmaster.git` plus matching branch tracking. Auto-review still rejected the combined commit/push as sensitive code egress and instructed not to retry through another path. Changes remain local and uncommitted pending explicit approval for that destination.

2026-09-23 GUI migration audit: `runtime/ops/GUI_MIGRATION_AUDIT.md` maps checked-in Freqtrade Dashboard/History/Backtest/Settings and current Nautilus Strategy/Research/Runtime to keep/adapt/remove decisions and exact API gaps. Existing Project #2 GUI parent #100 is In Progress with true child issues #102 (read-only HL Runtime/Dashboard/History, In Progress), #103 (sealed Strategy identity/drafts, Todo), and #104 (native Research/optimizer workspace, Todo). Implement sequentially, starting with #102 in a separate Terra task. The current Nautilus `/api/v1/runtime` reads the older paper worker and must never be shown as the running HL Stage-G instance. No GUI deployment or trader process change occurred for this audit.

2026-09-23 07:52 UTC DRY_RUN_STARTED: user-confirmed `root@46.225.163.123`; isolated `/srv/coinmaster/runtime` release files match local committed `8537f69` by SHA-256, and the dedicated warmup manifest at `/var/lib/coinmaster-hl-stageg-testnet/data/stageg-bybit-warmup-20260923-verified3/manifest.json` matches `f397cb1cda1ba7d8279a85789317bf4456fe02c6b6fccff749d9fdc80d409c9d`. Remote preflight as `coinmaster-hl` returned `attachable=true`, `warmup_state=READY`, `SEALED_APPROVAL_MATCH` and candidate `637762130c76396cd7c6e24644e31b28079a1683e4460d57f103dde42c603b6d`; only public Bybit/HL data clients and `SANDBOX` execution route configured. Installed and enabled only `coinmaster-hl-stageg-testnet.service` with root-only non-secret env and isolated `0700` state. At launch and repeated status, `TradingNode` and `WaveOverlayStrategy` are running, BTC/SOL HL quote/mark/funding feeds are READY, 1,482 Bybit daily warmup rows are READY, `live_order_capability=false`, `safe_for_increase=true`, `prime_error=null`, `FLAT_RESTART`, and zero native open orders/positions initially. Service active with zero restarts; existing `coinmaster-paper.service` and `coinmaster-runtime.service` remained active. The launch is virtual lifecycle observation, not a 24-month HL ROI claim. No actual live signal/fill has yet been observed; funding cash is unposted; source artifact must be refreshed after the next UTC daily boundary. Next: observe first natural signal, virtual entry/exit and native account/fee/margin reconciliation; keep real-capital promotion blocked.

Prior checkpoint below is historical; its transfer/launch blockers were resolved by explicit host confirmation and successful scoped deployment.

2026-09-23 D3 final virtual-launch pass: rebuilt an immutable ignored artifact `runtime/var/data/stageg-bybit-warmup-20260923-verified3/manifest.json` from predecessor `stageg-bybit-warmup-20260922-verified2` plus the owner-provided preserved public tail `runtime/var/data/bybit-tail-20260923/coinmaster-warmup-tail-20260923`. It is READY at the Sep-23 UTC boundary: BTC/SOL each have 1,482 contiguous kline+mark daily sessions through Sep 22, zero gaps/duplicates, and the appended raw manifest SHA-256 is exactly `0974b9e0d2c39a0cdf6afa6a522540710ebbdb5b3ec0569659532b87da7fb7e0`; artifact manifest SHA-256 is `f397cb1cda1ba7d8279a85789317bf4456fe02c6b6fccff749d9fdc80d409c9d`. The strict instance is pinned to this new artifact. Candidate/strategy/execution-policy approval compares `637762130c76396cd7c6e24644e31b28079a1683e4460d57f103dde42c603b6d` / `1f9597c38f895bd64af48634c8fe5112b50df004a8a4b597dfb4fe5d6144c2da` / `eee44dde50781b6364923e6aad8df714860796b0ec7cd6121adef7c25406f687` and is `SEALED_APPROVAL_MATCH`. The attached local HL-ID strategy construction passed with one public Bybit signal route, public HL MAINNET data route, and only Nautilus 1.231 SandboxExecutionClient/USDC. The native lifecycle test drives a sealed-configured HL BTC entry and reduce-only exit (two native fills), reports native USDC commissions/account, finishes flat, and retains durable HL exposure as `MANAGE_ONLY_DURABLE_OPEN_STATE` after restart. Funding remains `OBSERVED_MODELLED_UNPOSTED_NEXT_PAYMENT_NOT_CONFIRMED_SETTLEMENT`: `next_funding_ns` is never posted early and no settlement cashflow/equity claim is made. Focused verification: 38 passed; full split runtime verification: 205 passed (72 existing Pandas warnings). Remote read-only preflight found `coinmaster-paper.service` and `coinmaster-runtime.service` active, dedicated Stage-G state directory `coinmaster-hl:coinmaster-hl 0700`, no dedicated env/warmup, and Stage-G unit inactive. The safety system rejected the attempted source/config/artifact transfer to the unverified VPS, so no remote file, unit, enable/start, or existing service changed. Local Git commit/push is additionally unavailable because this host's Git requires acceptance of the Xcode license; do not accept it without owner action. Exact next step: explicit approval to transfer the named verified Stage-G source/config/artifact to `/srv/coinmaster/runtime` and `/var/lib/coinmaster-hl-stageg-testnet`, then provision the credential-free root-only env, verify remote gate, and start only `coinmaster-hl-stageg-testnet.service`.

2026-09-23 final launch check: a new self-contained v2 artifact `runtime/var/data/stageg-bybit-warmup-20260923-verified2/manifest.json` preserves the entire verified predecessor and appends only the credential-free public Sep 22 BTC/SOL Bybit daily kline/mark sessions. It validates raw content hashes, daily coverage/rebuild, and is READY with 1,482 sessions through the Sep 23 UTC boundary; it must be provisioned read-only at `/var/lib/coinmaster-hl-stageg-testnet/data/stageg-bybit-warmup-20260923-verified2/manifest.json` and revalidated after every later UTC boundary. `runtime/configs/stage-g-hl-sandbox-approval.json` pins and compares exact candidate, strategy-code, and execution-policy SHA-256 identities before attachment. These checks pass locally, but the funding settlement oracle does not; generic Sandbox and Backtest fixtures remain insufficient to prove an attached HL-ID Stage-G lifecycle. Therefore no isolated VPS service was activated or altered.

2026-09-23 #97 D3 attachment pass: the verified source-preserving Bybit warmup artifact is configured at the isolated provision path `/var/lib/coinmaster-hl-stageg-testnet/data/stageg-bybit-warmup-20260922-verified2/manifest.json`; its local ignored source artifact remains `runtime/var/data/stageg-bybit-warmup-20260922-verified2/manifest.json`. The attachment gate permits `WaveOverlayStrategy` only when that warmup is fresh/READY and the current public MAINNET BTC/SOL margin profile is available. Bybit signal bars retain their native IDs; HL `BTC-USD-PERP.HYPERLIQUID` / `SOL-USD-PERP.HYPERLIQUID` are execution and mark IDs only. Research `TierMarginPolicy` is no longer imported by the strategy: a public-profile `HyperliquidSandboxMarginPolicy` is injected with explicit local 40x BTC / 20x SOL leverage and fails closed for missing/stale marks or tier excess. Sandbox is seeded with 10,000 USDC; comparison to the sealed 10,000 USDT research seed is explicitly nominal under a 1:1 assumption, never historical/live parity. Fixed public-base fee policy is maker 0.015% / taker 0.045%; account-specific fee tier and 24-month HL execution-quality evidence are not claimed.

Native Sandbox funding is supported through the public `SimulatedExchange.adjust_account` API, with an idempotent pre-post journal that becomes manage-only after a crash window. The public adapter's `FundingRateUpdate.next_funding_ns` is explicitly the *next scheduled payment*, not a confirmed settlement. It carries no settlement identity/oracle, so the production strategy observes it but posts no funding; any future posting requires a separate confirmed-settlement event plus causal same-venue mark and native position. This is a precise adapter-data limitation, not a claim that Sandbox cannot adjust cash. Polling now preserves durable open/uncertain recovery state rather than overwriting it with a fresh process's empty Sandbox cache. Focused native fixtures cover BTC/SOL entry/exit, reduce-only/post-only/taker lifecycle, native fill/fee accounting, margin/equity liquidation accounting, funding idempotency, pause/flatten, and restart-to-manage-only. A no-start local construction confirmed the READY gate and a `SimulatedExchange`; a direct native funding probe changed only the local Sandbox account from 10,000 to 9,998 USDC. No external connection or order was made.

Remaining no-go evidence gaps: the warmup artifact must be freshly revalidated at each UTC boundary (it is not a static READY approval); `NativeSandboxFundingPoster` does not yet verify a post-adjustment account-balance delta; candidate/code hashes are recorded but not compared to an external sealed-approval record; and no isolated test yet drives the attached Stage-G strategy through native Sandbox fills using HL BTC/SOL IDs. Existing generic native Sandbox and BacktestEngine fixtures must not be represented as that attached-HL proof.
2026-09-23 D3 provenance/parity pass: prepared a new isolated artifact at `runtime/var/data/stageg-bybit-warmup-20260922-verified2/manifest.json` without changing frozen Bybit inputs. It pins base/tail source manifest SHA-256 `a3ece5d12a229a6bbb7d64d079f7b2c78e914eef46867d8df1e95935c9bcac74` / `d2e1ff66be91a81a9680a1483ea924836e765fca0a7903ee6f2e532377f836b7`, rebuilds both daily Parquets from canonical raw pages, verifies all source-page hashes, and contains 1,481 contiguous daily kline+mark sessions per symbol from 2022-09-02 through the 2026-09-21 completed session. Loader state was `READY` at 2026-09-22 22:39 UTC and dynamically changes to `WARMUP_LATEST_COMPLETED_SESSION_MISSING` at the next UTC date boundary until refreshed. Signal provenance stays Bybit; execution IDs stay `BTC-USD-PERP.HYPERLIQUID` / `SOL-USD-PERP.HYPERLIQUID`; configured data clients are public Bybit signal + public HL MAINNET, while the sole exec route is Sandbox. Stage-G capital is set to 10,000 USDC against 10,000 USDT research capital only under an explicit 1:1 peg assumption; no equivalence is claimed. Candidate SHA-256 `637762130c76396cd7c6e24644e31b28079a1683e4460d57f103dde42c603b6d`; strategy SHA-256 `02473b22d61aea1ede0dd93685ad2faf7133cb501564bf6194075d5979a63302`; declared HL Sandbox fee/execution-policy SHA-256 `67bdfa1aea32e40202a6a50d7390afd311a59b92451129fd42d35c48d2c10472`. Gate remains `attachable=false`: strategy still imports research-only `TierMarginPolicy`; account margin/liquidation and fee tier/execution quality are unproven; funding cash is not posted by Sandbox; USDT/USDC parity is only assumed. Historical Bybit SOL funding off-grid rows are recorded but excluded from any funding claim. Focused native tests: 39 passed (58 existing Pandas timestamp warnings). No node/service was started and no strategy execution occurred.
Artifact manifest SHA-256: `2e6d111d044e77b33fce4aafe6db95d95442888be68d43df51a6aca499087d14`.
2026-09-23 D3 mainnet-route integration: the data adapter is now explicit public Hyperliquid MAINNET while the only executor remains native Sandbox/USDC; no account/authenticated or exchange route exists. This is still not startable: `_load_warmup` rejects the checked-in Bybit seed as stale (last completed session 2026-09-01), and `WaveOverlayStrategy` is not parity-safe for HL execution because it couples Bybit signal/execution IDs and imports research `TierMarginPolicy`. Do not attach it, synthesize an HL warmup, or start a service. Required next gate: exact sealed-winner signal-source mapping through launch date plus a production venue-aware HL margin/fee/funding adapter and executable parity proof.
2026-09-23 D3 cross-venue gate: `cross_venue_stage_g_gate` pins the immutable candidate hash and strategy-code hash while declaring BYBIT BTC/SOL IDs as signals and HL BTC/SOL IDs as execution only. It loads the verified current HL mainnet profile and fails closed on the stale warmup and research-margin import; focused gate coverage: 37 passed. Public Bybit/HL tail acquisition is feasible read-only from VPS, but any refresh must be a new isolated provenance artifact, never a rewrite of frozen research inputs.
2026-09-23 read-only VPS inventory: `MyShopAI-prod-1` has existing `coinmaster-paper.service` and `coinmaster-runtime.service` active/enabled at remote checkout `caa4cdb`; D4 users, `/srv/coinmaster/runtime`, trader StateDirectory, and new env files are absent. Existing runtime state is root-owned. About 9.6 GB disk and 1.4 GiB memory were available. No VPS changes occurred. Keep existing services running during a staged isolated installation. Project #2: #97 and #98 In Progress, #99 Todo.
Issue #98 D4 inert VPS staging (2026-09-23, after P1 isolation/config correction): only the new `coinmaster-hl:coinmaster-hl` identity, its `/var/lib/coinmaster-hl-stageg-testnet` StateDirectory (`0700`), and a root-owned `/srv/coinmaster/runtime` release were created. The release is local verified `5562fb5`, excludes `.venv`/`var/data` source inputs, has content SHA-256 `1055aa6715fbbd91bb630d92c2fcc702da0d920ab41a669aa021070f6569acce` excluding generated bytecode, and has a root-owned pinned non-dev `uv.lock` environment (~867 MiB); remaining disk ~9.5 GiB. No trader secret env, unit file, enable/start/restart, node/wallet/account query/order/transfer, `/opt`, existing service/user/dir, or canonical checkout was changed. Existing paper/runtime stayed active (PIDs `2315259`/`2334804`). The local D4 process contract remains: separate `coinmaster-hl` trader vs `coinmaster-research` runtime/research owner/state, read-only shared release, writable research root `/var/lib/coinmaster-runtime/data`, scrubbed Hyperliquid/Bybit/DB/token child env, private child artifacts, fixed one worker, fail-closed testnet paths, doctor host metadata mode, and real Popen sentinel test. Local focused verification remains `55 passed` (2 existing Pandas warnings), doctor `OK`. This is not D4 operational acceptance: #97/D3 remains at account value 0 and needs separately authorized authenticated lifecycle plus concurrent VPS evidence.
Issue #97 D3 corrected local infrastructure checkpoint (2026-09-23): `hl-stageg-testnet` now uses one Nautilus 1.231 node with public Hyperliquid TESTNET data and the exact native `SandboxLiveExecClientFactory` allowlist for Hyperliquid/USDC. There is no API-wallet/address env contract, Hyperliquid execution factory, account query, exchange order, transfer, or live activation. `FeedObserver` is the only registered strategy; D1 lifecycle/ledger hooks remain dependency-injected fixtures with submit/cancel/reduce-only/post-only/taker/partial-fill and restart-to-`MANAGE_ONLY` coverage. Status reports `SANDBOX_NO_REMOTE_RECONCILIATION` and separates observed public data from modelled Sandbox fees/funding/margin. Focused D3/D4/profile/isolation suite: `42 passed`; isolation doctor `OK`; broad split runtime verification: `194 passed`, one unrelated research-child cancellation test blocked by host `os.killpg` permission. A bounded local public-data probe built/ran the node with only `SandboxExecutionClient`, no orders/fills/positions, and stopped; the host could not resolve the public TESTNET endpoint, leaving BTC/SOL feeds `DATA_STALE/PAUSED`. This is not an executable Stage-G dry run: `WaveOverlayStrategy` remains unattached until both gate checks pass—(1) the latest 24 months of real/provenanced/gap-checked signal warmup, with Bybit allowed only when it is the sealed strategy's exact signal venue and recent through launch; (2) executable sealed-winner parity. `NativePaperNode` now rejects a hashed 730+ day seed when its last completed daily session is older than two days. HL proxy/empty/zero-volume rows are not tradable execution, mark, funding, or warmup evidence. The data route remains TESTNET, not the requested public MAINNET route, and must not be launched as the requested dry run. No deploy, VPS action, wallet/account query, exchange order, transfer, GitHub mutation, or push occurred. Exact next step: convert the route to public MAINNET, restore local public DNS, then verify the source-specific 24-month warmup + Stage-G parity gates before requesting strategy attachment. The 24-month HL BBO/L2 gap remains non-substitutable for execution claims.
2026-09-22 preservation checkpoint: branch `codex/nautilus-btc-sol-mvp` is clean and synchronized with `origin` at `5d60986341c6e4a153cef8d0185fd674d2ec55fd`. D0 `ab0521d` and D1 `eb56033` are accepted and Done in GitHub Project #2. D2 `5d60986` is pushed as a partial checkpoint; issue #96 remains In Progress. Its public metadata snapshot and margin tiers are MAINNET facts, while D3 targets Hyperliquid TESTNET; official documentation says testnet tiers differ. Verify testnet BTC/SOL metadata and preflight before D3. Independent D2 audit did not complete because the audit agent reached its usage limit. The D2 implementation's full runtime suite reported 174 passed (70 existing Pandas warnings), and `git diff HEAD^ --check` is clean. No CI run is reported for this branch. No Hyperliquid node, wallet use, order, VPS deployment, or live action occurred.
2026-09-23 D2 correction: public read-only `https://api.hyperliquid-testnet.xyz/info` `metaAndAssetCtxs` snapshot was captured at `2026-09-22T21:01:26Z` without credentials/account data. It validates TESTNET BTC `BTC-USD-PERP.HYPERLIQUID` tiers `0/10k/50k → 40/25/10x` from actual raw table `54`; SOL `SOL-USD-PERP.HYPERLIQUID` single-tier `10x` is an explicitly separate documented derivation because its ID `10 < 50` has no raw table row. The profile rejects an invented low-ID raw table; Nautilus 1.231 accepts the same canonical IDs under `HyperliquidEnvironment.TESTNET`. It requires explicit `mainnet`/`testnet`, validates snapshot environment plus all tier facts, and scopes normalized funding identity by environment. Focused profile test passed; earlier full runtime suite: `177 passed` (70 existing Pandas warnings). Local checkpoint amended only; no node, wallet, account, execution, order, VPS/deploy, optimizer/backtest, or push occurred. Remaining blockers: separately authorized D3/testnet runtime integration and unavailable verified 24m HL BBO/L2 history (never replace with Bybit).
Issue #96 [HL D2] production-facing Hyperliquid BTC/SOL perpetual profile is committed locally, without activation: it records a non-secret public `metaAndAssetCtxs` provenance snapshot (2026-09-21T23:33:17Z), USDC collateral/account currency, BTC/SOL native IDs, public size increments (BTC `0.00001`, SOL `0.01`), $10 minimum order value, Hyperliquid price rule, configurable maker/taker rates, public margin-table boundaries (BTC 40x to 150M then 20x; SOL 20x to 70M then 10x), and a rate-independent funding settlement identity. A production-only instrument/margin preflight has no import or dependency on research/native_fixture or Bybit tier code; it rejects unknown account collateral and never substitutes Bybit data. The verified missing 24-month HL BBO/L2 history remains an explicit non-substitutable blocker. Focused profile/data/node suite: 22 passed; full runtime regression: 174 passed (70 existing Pandas warnings). No D3 node, network/order, VPS/deploy, backtest/optimizer, GitHub mutation, push, or secrets action occurred. Next: activation remains separately scoped.
Issue #95 [HL D1] execution-semantics parity is implemented locally and recorded in this local checkpoint. SOL planned exits are explicit native taker IOC control A and pass at zero spread; BTC TP targets alone retain post-only GTC. Regime/trail and hard-timeout exits preempt queued/resting SOL work, cancel native leaves, and reduce reconciled remainder; forced liquidation clears timeout ownership before quote processing, submits BTC close without waiting for SOL, and uses actual remaining SOL leaves. Every actual BTC entry and SOL add rechecks the pause/stale/safe-for-increase gate immediately before submit, while reductions remain permitted. Confirmed partial BTC TP fills grant SOL rights only by cumulative filled fraction and use the actual confirmed fill cycle for strict H3; buy sizing uses ask and sell sizing uses bid before precision/minimum validation. Funding journal isolation was not modified. New unit/native coverage covers zero-spread taker exit, queued pause rejection, side-aware sizing, partial TP rights, timeout preemption, forced liquidation leaves, and later resting-TP fill cycle; broad relevant runtime suite: `125 passed` (68 existing Pandas warnings). No deploy, VPS/service, paper command/order, live action, GitHub action, optimizer/backtest, or push occurred. Next: D2/Hyperliquid activation remain out of scope and require separate authorization.
Issue #94 [HL D0] immutable Stage-G and instance configuration is implemented locally. `runtime/configs/stage-g-v1.json` contains every frozen `Candidate` field for the sealed winner (EMA34, BTC TP `(0.2125,0.2125,0.575)`, BTC `7.875`, SOL `(1.75,2.625,3.5)`); its strict loader rejects missing/extra/non-finite/economically invalid values and hashes canonical Candidate content only. New strict instance configuration requires `instance_id`, BYBIT/paper identity, strategy config, state DB, trader/strategy IDs, and order tag; checked-in `paper-stage-g-example.instance.json` is a non-secret explicit template. The production paper worker now fails closed without valid `COINMASTER_PAPER_INSTANCE_CONFIG`, loads both files once before `TradingNode` construction, logs instance/venue/mode/strategy/order identity plus exact candidate SHA-256 at startup, and status reports the same immutable identity. `strategy_id` and `order_id_tag` are passed into the actual native `WaveOverlayStrategyConfig`; direct node construction retains a test-only default. Tested locally: focused Stage-G/native-node/paper-worker suite `26 passed`, including schema/equality/hash/rejection/file-mutation, real strategy identity, and status identity. No deploy, VPS/service, paper command/order, live action, GitHub action, optimizer, or push occurred. The CLT Git binary is available for the required local checkpoint. Next: separately authorize/runtime-validate an explicit instance file and service environment; do not use legacy named candidate selection.
Stage G max-ROI correction (2026-09-20, local-only) promotes the already completed, economically valid `g4-btc-7.875` as final reproducible research winner: EMA34 / TP `(0.2125,0.2125,0.575)` / BTC `7.875` / SOL `(1.75,2.625,3.5)`. The source row and exactly one clean from-genesis confirmation match exactly: ACTIVE/RESERVE/TOTAL `1978792.33053629`/`0`/`1978792.33053629`, ROI `196.879233053629`, DD `0.4133691361147726452460031732`, zero liquidations, 421 fills, fees `446207.87769090` (maker `234301399.5194`/`46860.27990388`; taker `726086541.4305`/`399347.59778702`), funding UNKNOWN signed amount / 2280 events; delta vs Stage F `50084.62324134`. Fee splits/notionals, fills, liquidation, funding count, and normalized fills/orders match; normalization drops only per-engine `init_id` (fills SHA-256 `4bfa030bed736b9a9b5351804f7f955803aaea9b339fd75ae6c8b2a42cbb7eda`, orders `1cfd55719f785d3cbf69bd760ad2bd1e96f7ee1831fbc31e6f7a397fadaac003`). Prior G1 (`1945213.42678339`) is runner-up; its former exclusion of G4 was procedural only, not an invalid simulation. No new parameter/grid/extension or other native simulation was run. Confirmed JSON SHA-256 `120aea8b1237e5a9eb8c72aad42d111eb17ac79a10ae9e90cf04b9625d9cb078`, TOP20 CSV SHA-256 `233f8b86c026145b3e9bca40975499de3c033f733f37253f628e6b93ff994fe5`, checkpoint SHA-256 `b5c72194fd6d7c7b4a0576700f9b5817a12dc9b34d2e815bbc38a166f19191bb`, confirmation SHA-256 `f3501f9046ea0a5938bec00b659ead32d05200ef09cc32f0b91878b27d7fb26c`; all results remain `NOT_FAITHFUL_DIAGNOSTIC` and non-live-ranking. No reserve/restart, Hyperliquid/VPS/paper/live action, GitHub action, or push occurred.
Stage F BTC boundary closure (2026-09-20, local-only) is complete with corrected isolated funding journals and no strategy/other-axis changes. Accepted clean E baseline EMA34 / TP `(0.2,0.225,0.575)` / BTC `4.375` / SOL `(1.875,2.8125,3.75)` at `795648.32049983` was reused. Sequential fresh BTC rows were `4.5` `816160.99060990`, `4.75` `856857.80014249`, `5.0` `897597.59766207`, then 0.25 upper steps through `7.75` `1928707.70729495`; `8.0` turned down to `1596324.64144040`. Exact fine neighbors `7.625`/`7.875` were `1883153.62676328`/`1842000.89030423`, establishing interior winner BTC `7.75`; no liquidation occurred in any ranked row. Two distinct fresh from-genesis labels reproduced the winner exactly, including ACTIVE/RESERVE/TOTAL `1928707.70729495`/`0`/`1928707.70729495`, 422 fills, fees `441546.20782708` (maker 174 / `231541075.4135` / `46308.21508270`; taker 248 / `718614532.2621` / `395237.99274438`), 2291 funding postings (UNKNOWN signed amount), zero liquidations, and normalized fills/orders SHA after excluding only `init_id`. Deltas: +`1133059.38679512` vs Stage E, +`1187801.15615570` vs Stage D, +`1790197.34629997` vs accepted 7.5. Boundary is resolved (`upper_tested=8.0`, non-liquidated turn); no BTC point beyond 8.0, no other search axis, reserve, deploy, paper/live action, GitHub action, or push occurred. Compact JSON SHA-256 `be063d203a78c81ea01249da09cc8fc99da08dd547be8240616a94f987342400`, TOP20 CSV SHA-256 `7a40230411301507f3a68d08f30f1f78155f99bfd8217112f728684834533d7e`, checkpoint SHA-256 `4ec0555bab478a0d35267acd15f12e5a31a150bc7e48d0a12d7540b5fc5f6b9a`. All ranked evidence SHA/fees/funding validated before publishing; results remain `NOT_FAITHFUL_DIAGNOSTIC` and non-ranking for live.
Stage E funding-ledger correction (2026-09-20, local-only) is implemented and verified. Root cause: a from-genesis `run_native_diagnostic` opened a prior canonical `<label>-funding.sqlite`, so durable funding IDs correctly skipped reposting into a new native account. The run path now creates a unique same-directory hidden attempt journal; only after successful completion does it checkpoint/close and atomically publish to the canonical artifact name. Failed Python-level attempts are quarantined; a hard interruption leaves a unique noncanonical attempt DB that no later run can open. Genuine same-engine/process-restart ID idempotence is unchanged. Two exact 24m from-genesis clean labels for EMA34 / TP `(0.2,0.225,0.575)` / BTC `4.375` / SOL `(1.875,2.8125,3.75)` matched ACTIVE/RESERVE/TOTAL `795648.32049983`/`0`/`795648.32049983`, 422 fills, zero liquidations, fees `182721.51343851` (maker 174 / `92414684.1685` / `18482.93683370`; taker 248 / `298615593.8265` / `164238.57660481`), and 2291 current-run funding postings (BTC 2069, SOL 222). Normalized fills/orders exactly match after dropping only Nautilus per-engine `init_id` UUID. Corrected compact JSON SHA-256 `35e285d45aa86c3c54fde8f143974cabcfa2b385cda8e28de5de39031932bcfa`, TOP20 CSV SHA-256 `c5afa06602baaa79251e925a090430cb31f0a11fa226e77f5ecb9b1e33c6abbc`; clean reports SHA-256 `8694561605aa0aa55dec788c554b0db5af764bcf665f293bd9d88be0a63d3728`/`da15ffe4a047cf2330af7861db3c3db605098978bf2a1192c8ce1ceceb014aff`. It improves prior clean E4 leader `788940.61008073` by `6707.71041910` and Stage D by `54741.76936058`, so no grid rerun was authorized. Pre-fix `808600.57496264` / 421-fill evidence is preserved only as excluded incident. The BTC 4.375 boundary remains unresolved; no point beyond it was run. Implemented: attempt journal isolation/checkpoint/publish/quarantine and corrected compact exporter. Tested: preexisting canonical/interrupted attempt isolation plus genuine restart test; 63 focused tests passed (48 existing Pandas warnings). Assumed/unchanged: historical funding settlement cash amount remains `UNKNOWN_NATIVE_AUDIT_HAS_POST_TOTAL_NOT_CASH_DELTA`; all results remain non-live-ranking diagnostics. No deploy, VPS/service, paper command/order, live action, GitHub action, or push occurred.
Stage E fine grid (2026-09-20, local-only) is complete but blocked from promotion by a fresh-engine reproduction mismatch. E1/E2/E3 selected TP `(0.2,0.225,0.575)`, BTC boundary `4.25`, and SOL `(1.875,2.8125,3.75)`; E4's best joint reuse was `792186.50859590`. Per steering, exactly one further BTC `4.375` run on that tuple produced observed TOTAL `808600.57496264` (delta vs Stage D `67694.02382339`, delta vs accepted 7.5 `670090.21396766`; fees `186198.40387288`, 421 fills, DD `0.4549238674952161755056529955`, no liquidation). The one distinct fresh-engine reproduction of the exact same tuple returned `795648.32049983` and 422 fills, so economics are not exact and the final artifact is `REPRODUCTION_MISMATCH_NOT_FAITHFUL_DIAGNOSTIC`; `unresolved_btc_boundary=true`. No further grid/boundary expansion ran. All 27 reported rows validate their fills/orders SHA, reconciled fees, and explicit funding evidence; full checkpoint also retains one pre-guard baseline execution plus eight resume E4 aliases as excluded incident evidence. Compact JSON SHA-256 `b195a0214c12764fe0468731f97ca29cf7dd6f9153cb3552e7be4c911817cfc5`, TOP20 CSV SHA-256 `44b09cc7d91abcfea1ce6e35a687a932fca2557c643febc7e005b42db0ac2e6a`, checkpoint SHA-256 `4b5becdaf3a9ddbfbfc431eb5df6540939301fb6ae46972650fded86b12c2370`; focused wave+optimizer tests: 30 passed. Runner fixes were checkpoint/dedup/reporting only, not strategy semantics. No deploy, VPS/service, paper command/order, live action, or push occurred.
Hypothesis lifecycle follow-up complete (2026-09-20, local-only; continuation of the same correction): strict H3 now stores the actual decision cycle when a resting BTC TP fill is confirmed/applied, not the target creation cycle. Regression proves a target created in cycle 0 and filled in cycle 1 can add SOL only in cycle 1; an unused right cannot add in later cycles. Only H3 reran; H0/H1/H2/H4 were SHA/fee/funding-verified reuse. Final sealed IDs are exactly `H0-control-reuse`, `H1-timeout-preempts-sol-add`, `H2-episode-fixed-beta-rolling-z`, `H3-strict-tp-fill-cycle`, `H4-btc-only`; normal resume rejects any other cohort and cannot append legacy H2/H3 rows. H3 TOTAL `548601.24002896`, delta `-192305.31111029`; all five ranked rows validate. Final checkpoint SHA-256 `7d2de71d1b4a4c54fe0cf1ee036cf224f654c93d4fa474f0416b917853ad9d60`, correction evidence SHA-256 `92f874ee17084c5d035c7f0faf52a352b465f5aa7c83bd31e69411f7aa88e58b`; focused tests: 29 passed. No Stage E, search expansion, deploy, VPS/service, paper/live action, or new correction occurred.
Hypothesis correction complete (2026-09-20, local-only; one allowed substantial correction only): H0/H1/H4 source evidence was SHA/fee/funding revalidated and reused; only H2/H3 reran. H2 now freezes only beta captured at BTC episode start, recomputes current relative with it, and uses rolling/current `mu` and `sigma` throughout; stored/noncurrent sigma cannot affect H2 decisions (focused regression). H2 TOTAL remains `740906.55113925` (`0E-8`). H3 is accurately `strict-tp-cycle`: a confirmed TP right may add SOL only in that exact decision cycle; unused strict rights cannot add later. H3 TOTAL `548601.24002896`, delta `-192305.31111029`, so accepted late-right control semantics remain default. Corrected five-row compact checkpoint SHA-256 `439d68a0e449f49916446d3b16543efbe24a6563f795d2b1617957ce1b7c82d9`, correction evidence SHA-256 `7aad4e663c6702440006bb72e4ea87ad1548dd6a3128721ab6b8c8ead018b136`; all ranked rows validate fills/orders SHA, fees, and explicit funding. Focused wave+optimizer tests: 27 passed. No Stage E, deployment, VPS/service, paper/live action, or further correction occurred.
Focused independent hypothesis pass complete (2026-09-20, local-only; no Stage E): H0 reused the Stage D winner EMA34 / TP `(0.2,0.25,0.55)` / BTC4 / SOL `(2,3,4)` evidence at TOTAL `740906.55113925`. H1 timeout-preempts-coincident-SOL-add, H2 entry-fixed beta with rolling `mu/sigma` unchanged, and H3 explicit late-SOL-entry-after-confirmed-TP-right each separately reran and exactly tied H0 (`0E-8`), so H1/H2 remain default-off while H3 documents existing control semantics. H4 BTC-only was `89591.16982085`, so SOL incremental is `651315.38131840`; it remains default-off. H2 is fully defined (capture rolling beta at BTC episode start, recompute current relative with it, retain rolling history distribution, reset on episode end) and covered by a focused domain test. Compact ignored JSON/CSV checkpoint SHA-256 `4c41d47f0bcba9bbeb475f523a05ef3bdfb21be6e09cc4a2da597250e7e87725`; all five ranked rows have validated fills/orders SHA, reconciled fees, and explicit UNKNOWN funding amount/count. Focused wave+optimizer tests: 25 passed. No deploy, VPS/service, paper command/order, live action, hypothesis combination, or Stage E occurred.
Stage D joint refinement complete (2026-09-20, local-only; no Stage E/hypotheses): the Stage C full-tuple baseline EMA34 / BTC TP `(0.2,0.3,0.5)` / BTC `4.0` / SOL `(2.25,3.375,4.5)` / TOTAL `714572.72107702` was hash-verified and reused as `d0`. Ordered one-axis stages tested exactly six TP allocations, EMA `33/34/35`, BTC `3.75/4.0/4.25`, and SOL `(2,3,4)/(2.25,3.375,4.5)/(2.5,3.75,5)`, with full-tuple reuse. Sealed selections: TP `(0.2,0.25,0.55)` `730194.46903890`, EMA34, BTC4, SOL boundary `(2,3,4)` `740906.55113925`; the required `d5` joint reuse retained it. The sole authorized lower SOL extension `(1.75,2.625,3.5)` turned down to `730860.33336992`, so no further expansion ran. Final winner: EMA34 / TP `(0.2,0.25,0.55)` / BTC `4.0` / SOL `(2,3,4)`, ACTIVE/RESERVE/TOTAL `740906.55113925`/`0`/`740906.55113925`, ROI `73.090655113925`, DD `0.4153026781172542752865056310`, zero liquidations, 422 fills; maker `79372148.7906`/`15874.42975812`, taker `252721748.5400`/`138996.96169727`, total fees `154871.39145539` reconciled; funding `UNKNOWN_NATIVE_AUDIT_HAS_POST_TOTAL_NOT_CASH_DELTA` (2291). Ignored checkpoint SHA-256 `67cbc3b42bc6afce6df6268483189b7942b3446cfebf66ede65ad189021e5076`; compact JSON/CSV TOP20 validated fills/orders SHA, reconciled fees, and explicit funding for all 18 ranked rows. Focused native optimizer tests: 10 passed. No deploy, VPS/service, paper command/order, live action, Stage E, or hypotheses.
Stage C BTC management fine-neighbor pass complete (2026-09-20, local-only; no Stage D): sealed c1-c4 winner EMA34 with BTC TP fractions `(0.2,0.3,0.4)` / TOTAL `656406.65217368` was resumed under one atomic lock without reopening prior stages. Exact EMA neighbors `[29,32,34,36,39]` retained EMA34 (29 `428977.44530581`, 32 `451660.25841293`, 36 `249978.94426847`, 39 `LIQUIDATED_EARLY_CUTOFF` `2999.29477690`), so no EMA boundary extension ran. Exact one-axis TP neighbors at EMA34 then selected boundary `(0.2,0.3,0.45)` at `694665.48886911`, authorizing exactly one valid third-component `+0.05` extension. Final Stage C winner is EMA34 / TP `(0.2,0.3,0.5)`: ACTIVE/RESERVE/TOTAL `714572.72107702`/`0`/`714572.72107702`, ROI `70.457272107702`, native cash DD `0.3969324321828586822022051476`, no liquidations, 420 fills, maker `77523579.8320` notional / `15504.71596640` fees, taker `241082130.7296` / `132595.17190151` fees, native total `148099.88786791` reconciled exactly; funding signed amount `UNKNOWN_NATIVE_AUDIT_HAS_POST_TOTAL_NOT_CASH_DELTA` (2240 events). The compact ignored JSON/CSV TOP20 references local checkpoint SHA-256 `8dd0adcb45784cb5367207fecce21368f91de319cb457527d98f4586db761ee6`; all 35 ranked rows had fills/orders SHA validated, reconciled fees, and explicit funding amount/count before publication. Focused native optimizer tests: 9 passed. No deploy, VPS/service, paper command/order, or live action occurred. Stage C is complete; no Stage D/hypotheses authorized.
Stage B SOL signal/exit search complete (2026-09-20, local-only; no Stage C): accepted Stage A BTC `4.0`, SOL sizes `(2.25,3.375,4.5)` was hash-verified before a single-owner, atomic-resume native 24-month search. B1 tested 11 entry configurations; B2 tested 11 exit/holding coordinates; B3 tested nine history windows; B4 was the deterministic joint reuse. The compact ignored JSON/CSV has 32 rows and local checkpoint SHA-256 `c8b9c19d8290a974138e7747b80134c374683eb89e9c2bc46388eff47e4d2bf6`. No candidate improved terminal TOTAL: sealed B1 `uniform +0`, B2 `half 0.25` (terminal tie), B3 `beta 270`, and B4 joint retain exact accepted candidate/TOTAL `256757.84645271` (delta `0E-8`). Boundary extension and joint neighbors were not authorized because no improvement occurred. Every compact row has hash-valid fills/orders evidence, reconciled maker+taker fees, explicit UNKNOWN funding signed amount plus its recorded count, and SOL add-level attribution `UNKNOWN_NATIVE_FILLS_DO_NOT_EXPORT_SOL_ADD_LEVEL`. Focused native tests: 26 passed (six existing pandas warnings); compact validation passed. No deploy, VPS/service, paper command/order, or live action occurred.
Stage B accepted-control guard (2026-09-20, code-only): reuse now additionally rejects an accepted Stage A source unless its full candidate configuration, nonempty policy/data hashes, and every referenced fills/orders hash validate before any Stage B work. Focused tests remain 26 passed; no native run occurred for this guard.
Stage A native sizing complete (2026-09-20, local-only; no Stage B): canonical 42-row/24-month sequential native checkpoint is compactly reported at ignored `runtime/var/data/runs/native-stage-a-sizing-v1.json` (177,577 bytes) with TOP20 CSV and full local immutable checkpoint SHA-256 `7acc8e2559a1c062607310d27af13669fd91650ecb31f8e8dabc9371285c5a08`. Ranking is terminal ACTIVE+RESERVE TOTAL only; liquidations remain eligible. Winner: BTC `4.0`, SOL `(2.25,3.375,4.5)`, ACTIVE/RESERVE/TOTAL `256757.84645271`/`0`/`256757.84645271`, ROI `24.675784645271`, native cash DD `0.4597795622673682705357929212`, no liquidations, 453 fills; maker `27537483.3382` notional / `5507.49666764` fees, taker `125807105.5157` / `69193.90803388`, native total fees `74701.40470152`, funding signed amount `UNKNOWN_NATIVE_AUDIT_HAS_POST_TOTAL_NOT_CASH_DELTA` (2333 events), delta vs accepted 7.5 `118247.48545773`. The 2/3/4 profile improved, authorizing the one additional 2.25/3.375/4.5 step. Harness now atomically resumes by full Candidate tuple, records explicit reuses, freezes staged selection, emits compact JSON/CSV referencing hash-verified local evidence, and preserves seven interrupted pre-fix resume rows as excluded non-ranking incident evidence. Focused native tests: 25 passed (six pandas timestamp deprecation warnings); compact artifact validation passed. No deploy, VPS/service, paper command/order, or live action occurred.
Stage A evidence correction (2026-09-20, local-only; no Stage B): review found overwritten fills/orders for authorized A3 BTC `3.0` and `4.0` at SOL `(1.5,2.25,3.0)`. The mismatched original paths/reports and seven out-of-plan interrupted-resume rows remain ignored local incident evidence; normal resume is now sealed to the original 42 ordered rows and cannot schedule or rank them. Fresh unique-label sequential reruns are stored in ignored `native-stage-a-sizing-v1-evidence-correction-v1.json`, SHA-256 `4ec8bbd2726a40d32189c9f98045988f4a7fc510446d54a6f13e5cde6bff8d1f`, and replace only the two canonical evidence references. BTC3 old/new TOTAL `238079.45514457`/`238079.45514457`, fees `49419.54919133`/`49419.54919133`; BTC4 old/new TOTAL `249554.29787206`/`249554.29787206`, fees `71900.00966542`/`71900.00966542`. New fills/orders hashes validate; all 42 canonical rows now validate artifact hashes and maker+taker fee sums exactly. A3 winner remains BTC `4.0` at SOL `(1.5,2.25,3.0)`, so downstream plan remains valid and the final winner remains BTC `4.0`, SOL `(2.25,3.375,4.5)`. Focused tests: 25 passed (six existing pandas timestamp deprecation warnings). No deploy, VPS/service, paper command/order, or live action occurred.
Stage A sealed-resume regression correction (2026-09-20, local-only): once a corrected 42-row cohort exists, normal `run_stage_a_sizing` now delegates directly to the correction-aware compact exporter; it cannot overwrite fresh A3 evidence paths/hashes, correction metadata, or excluded incident rows with stale partial references. New focused regression proves zero candidate execution and preservation of correction hash, 42-row result count, fresh evidence reference, and incident exclusion. No optimizer/native run, Stage B, deploy, paper command/order, or live action occurred.
P5.3 native fixed-base fee/execution control (2026-09-20, local-only): code commit `32c19d9` replaces IOC reductions with genuine post-only/reduce-only resting limits where targets are known, retains market/IOC as native taker for entries, adds, forced/regime/trail/liquidation/terminal exits, and records native `OrderFilled.liquidity_side` per fill. The final fresh 24m 7.5 control artifact `runtime/var/data/runs/native-fee-policy-v3-control-7_5-final-v5-20260920T140000Z.json` is `NOT_FAITHFUL_DIAGNOSTIC`/non-ranking: TOTAL `138510.36099498`, ROI `12.851036099498`, 453 fills, maker 185 / `39647802.1545` notional / `7929.56043090` fees, taker 268 / `179659538.4178` notional / `98812.74613005` fees; native total `106742.30656095` reconciles exactly. No liquidation events; terminal native cache is flat with zero open positions/orders. Earlier failed pre-fix control artifact sets remain retained as `ABORTED_REPORT_ORDERING` incidents; old `125198.29` is policy-incomparable. Full runtime suite: 124 passed. No deploy, paper command/order, or live action occurred.
Canonical root: `/root/.openclaw/workspace/coinmaster/coinmaster`; branch `codex/nautilus-btc-sol-mvp`.
Deployed state: paper and loopback runtime services are active. The paper node is native Sandbox-only (`live_order_capability=false`) with public Bybit/Hyperliquid data, verified warmup, reconciliation, and no open positions/orders.
P2.2 research lifecycle final P1 race correction (2026-09-20, local-only, pushed `b021779`): a manager without the local `Popen` can now cancel another manager's canonical child only after the persisted owner token and birth-bound process identity verify its exact group. It sends bounded TERM→KILL, confirms the group has gone, and only then writes `CANCELED`/releases the SQLite lease; the original observer cannot overwrite that terminal state. Identity or termination uncertainty is retained as `CANCEL_REQUESTED` with a precise conflict evidence and the lease remains held, so a successor cannot start. Terminal `update_run` calls for an active leased research job require an explicit verified-death release path. The earlier launch permit, compact SHA-validated artifact envelope, bounded stdout, observer reaping, immutable saved input, and atomic idempotency/lease invariants remain in force. Real two-store/two-manager tests cover cross-manager cancellation followed by successor start only after first-child death, plus identity mismatch retaining the lease; focused API tests: 37 passed; full runtime suite: 111 passed. No canonical 24-month run, VPS/service deployment, paper command/order, or live action occurred. Next: separately authorize real native research only after its data/evidence scope is selected; a blocked run remains truthful evidence, not a result.
P2.2 runtime-only deployment (2026-09-20): deployed source commit `caa4cdb378595cd4db669d8429f37df5ad409e42` to the canonical VPS checkout and rebuilt/copied only the runtime SPA; only `coinmaster-runtime.service` restarted (PID `2304656` → `2334804`). `coinmaster-paper.service` remained active with unchanged PID `2315259`, `mode=paper`, `RECONCILED`, `live_order_capability=false`, and flat positions/orders. Online SQLite-backup API copy: `/var/lib/coinmaster-runtime/drills/20260920T050156Z/control.sqlite`, SHA-256 `dd765abbf3184a839e7bd8603643f288ca4dabb87c59f29e5fdcc86b0fbed1a9`. Runtime loopback smoke: unauthenticated configuration request `401`; authenticated default configuration save, conservative preflight (`allowed=false`), 7-entry catalog, and static UI asset `200`; a native research request was exactly `BLOCKED` at `MISSING_1M_MANIFEST` before spawn (`pid=null`, persisted report, zero leases), and terminal cancel remained `BLOCKED`. No backtest/data copy, paper command/order, proxy/firewall/TLS, private-exchange, or live action occurred.
P1 native fixture final margin-audit correction (2026-09-20, local-only code `8cf6ce3`): the genuine Nautilus 1.231 `LimitOrderPartialFillModel` fixture now removes the false equation of raw native `locked` with position IM. A 10-contract parent reaches native `PARTIALLY_FILLED` at five, receives native cancel confirmation, then receives a native reduce-only close. The durable record distinguishes pending reservation, position IM, position MM, raw native locked/free collateral, requested/filled/leaves/canceled quantities, and parent/close fees; its migration renames the misleading former `native_margin_init` snapshot. At cancel confirmation the pending parent reservation releases `25.025 → 0`, while raw native maintenance collateral is separately `2.5025`. With an explicit fresh mark, existing captured-tier `BybitTierMarginModule`, and selected 40x leverage, the open five-contract position records IM `12.5125` (`5 × 100.1 × 2.5%`) and captured-tier MM `1.65165`; both margin fields and native locked collateral clear only after the native reduce-only close. No execution or PnL is simulated outside Nautilus. The separately retained fail-closed limitation is automatic liquidation because `BacktestEngine.add_venue` exposes no liquidation path (`UNSUPPORTED_NAUTILUS_1_231_ADD_VENUE_LIQUIDATION_PATH`); reserve restart/pause remains unsupported/fail-closed. Focused native tests: 21 passed; full runtime suite: 115 passed. No strategy rule, backtest, VPS, paper command/order, or live action occurred.
P3.2 sequential seen-data reporting pass (2026-09-20, local-only code `a22bb5c`): native `run_native_diagnostic` used fresh 730d warmup/account/engine/journal per annual train candidate (v0 9.0, 7.5, 8.25, 9.75, 10.5; `[2024-09-01,2025-09-01)`) and fresh locked-winner 7.5 plus v0 test (`[2025-09-01,2026-09-01)`). The terminal ACTIVE+RESERVE objective selected 7.5 at `202860.47789066`; test 7.5 `19346.04346858` exceeds v0 `13989.05214807`. Incident retained for audit: after a shell timeout the primary continued, but an early manual resume launched a conflicting noncanonical local partial attempt; immutable primary JSON/CSV were never overwritten and partial was reconciled from that primary final. Initial immutable JSON `9966d424cede1e0b5208007cbc353b8b67f8c0d18d45620462c94ab352edef7a` is `SUPERSEDED_REPORTING_VALIDATION` because its month-count guard turned two authoritative liquidation lockouts into failures. Only 9.75 and 10.5 were intentionally rerun once as reporting recovery: `13296.92456772` and `14092.01598100`, both `LIQUIDATED_EARLY_CUTOFF`; winner remains 7.5. Recovery JSON SHA-256 `1115999202b1937ebfb13ea61f19f2dff285f0dd05a579579979b0eb12a6f02d` plus matching CSV/partial are ignored immutable artifacts and catalog-visible read-only. Every result is `SEQUENTIAL_SEEN_DATA_VALIDATION_NOT_OOS` + `NOT_FAITHFUL_DIAGNOSTIC`; no rule/policy/data/profile change or promotion occurred. Focused: 53 passed; TypeScript check, UI invariant (11), both builds, and full runtime 120 passed. No VPS/deploy/paper/live action occurred.
P2 configuration-honesty pass (2026-09-20, local-only, pushed `4643038`): the API now rejects every non-v0 value for controls that have no native paper implementation: initial active allocation/total, zero-wave and sigma switches, optional SOL/BTC/portfolio stops, future SOL margin earmark, `clip` margin policy, reserve transfer/trigger, and restart/liquidation policy. Each rejection is a stable `UNSUPPORTED_PAPER_CONFIG:<field>` error; exact inactive original v0 values remain valid. The Strategy UI locks those controls, labels venue as preflight-only, and explicitly states that saved candidate drafts do not change the running `corrected-v0` paper service. Candidate/domain fields remain editable only as stored drafts for future separately scoped research/config wiring. Focused API/runtime tests: 18 passed; `npm run check`, `npm run invariants:nautilus-research-ui` (10), and both production builds passed. No 24-month backtest, VPS, service, paper command/order, or live action occurred. Next: a separately authorized immutable-config-to-native-paper activation path may wire supported Candidate parameters; account/reserve/stop policies require their own native/recovery design.
P2/P3 Strategy + Research GUI integration (2026-09-20, local-only, pushed `38c51e7`): starting from `031cd15903d5e070c3448c7eef97fbb17fa800a6`, the Nautilus local API now projects a small checked-in immutable research catalog for reporting-v2 original v0, selected diagnostic 7.5, 8.25, liquidated 9.75/10.5 exclusions, fixed stress, and blocked Hyperliquid REST evidence. Each entry records artifact SHA/status, interval/warmup, totals/cash/ROI/DD, fees/fills/funding, limitations, and supersession; ignored local reports return `VERIFIED_LOCAL`, `ARTIFACT_NOT_LOCAL`, or hash mismatch and expose safe verified detail only on a matching SHA. The Strategy page keeps original `corrected-v0` as its paper default and shows versions read-only; Research renders the catalog and converts Backtest into `COMPLETED_ARTIFACT_REFERENCE` for immutable selected-7.5 evidence (`NO_NEW_BACKTEST_COMPUTE`), never new computation. API/OpenAPI/client types/UI and the checked-in UI invariant are updated. Verified locally: runtime `pytest` 75 passed; `npm run check`, `npm run invariants:nautilus-research-ui`, and `npm run build` passed. No VPS, GitHub Project, runtime configuration, paper/live trading, or deploy action was taken. Exact next step: retain the read-only catalog and paper default; any newly requested computation requires a separately scoped research worker.
Runtime-sidecar integration/debug (2026-09-20, deployed `e7dfc95`): the loopback `coinmaster-runtime.service` now reuses the control/research route surface rather than exposing only `/runtime`; `COINMASTER_RUNTIME_CONTROL_DB=/var/lib/coinmaster-runtime/control.sqlite` keeps operator configurations/runs separate from the paper journal. The combined OpenAPI contract drives the SPA types, every API route (including OpenAPI) requires the runtime token, and the sidecar retains sole ownership of strict paper runtime/command endpoints while projecting `live_order_capability`. Local authenticated socket smoke passed default/save/list configuration, preflight, catalog, immutable artifact-reference run, runtime projection, `401` unauthenticated requests, and safe `503` unavailable command relay. Full runtime `pytest` 75 passed; `npm run check`, research UI invariant, and production web/custom build passed. VPS checkout is `e7dfc95`, runtime is active on `127.0.0.1:18182`, unauthenticated config/OpenAPI requests return `401`, and authenticated smoke confirmed selected 7.5 catalog with `ARTIFACT_NOT_LOCAL`, detail `verified_detail=null`, `COMPLETED_ARTIFACT_REFERENCE`/`NO_NEW_BACKTEST_COMPUTE`, and empty positions/orders. No runtime command was issued. `coinmaster-paper.service` stayed active with unchanged PID `2238852`; it reports `DATA_STALE/PAUSED`, `RECONCILED`, `mode=paper`, and `live_order_capability=false`, so it remains fail-closed. Exact next step: retain this paper-only fail-closed state; a validated warmup manifest and separately scoped authorization are required before any paper lifecycle or live change.
P4 feed-recovery integration/debug (2026-09-20, controlled restart only): read-only health showed live quotes/marks for all public feeds and fresh Bybit/SOL funding, but only `BTC-USD-PERP.HYPERLIQUID` funding had stopped updating for about 3.5 hours, making the otherwise reconciled worker fail closed as `DATA_STALE/PAUSED`. The Hyperliquid data client remained connected and logs had no adapter error/reconnect; this was a stale public subscription observation, not a venue-data substitution. With zero positions/orders and no runtime command, only `coinmaster-paper.service` was restarted (PID `2238852` → `2306169`); `coinmaster-runtime.service` PID stayed `2304656`. Immediately and through the final >135-second sample, both worker and authenticated runtime reported `READY`, all four feeds `READY`, Hyperliquid BTC funding within the documented hourly plus grace window, `RECONCILED`, `mode=paper`, `live_order_capability=false`, and only `SandboxExecutionClient`; positions/orders remain empty. No code/deploy change was needed for this recovery. Next: monitor the public Hyperliquid BTC funding cadence; a repeated stale subscription is the exact trigger for a separately scoped adapter lifecycle correction. Live remains unauthorized.
Critical autonomous/recovery correction (2026-09-20, deployed `961b23a` then `90cbea5`): paper polling is now a monotonic, single-owner periodic loop, not an HTTP-triggered side effect. Before every native order submit, a durable intent record is written; Sandbox cache reconstruction is intentionally unsupported, so any durable open position/order or submit-before-ACK intent on restart yields `MANAGE_ONLY` and blocks increases rather than comparing it to a fresh empty cache. A journal lock makes the new poll thread and HTTP status/command reads SQLite-safe. Local coverage includes autonomous snapshots with no HTTP, plus a real SIGKILL native-Sandbox subprocess: it opens BTC, natively reduces it from 1.0 to 0.6, journals/Sandbox-submits SOL before ACK, then restarts from the same SQLite DB in `MANAGE_ONLY_PENDING_INTENT`; flat-restart reconciliation and duplicate durable submit are also covered. Full runtime suite: 78 passed. VPS deploy restarted only `coinmaster-paper.service` (PID `2306169` → `2311653` → `2315259`); runtime PID stayed `2304656`. With no worker HTTP reads, the deployed journal advanced `1789876876038435190` → `1789877026037009612` over 150 seconds; the prior 135-second observation is also retained. One subsequent authenticated runtime read reported `READY`, all four Bybit/Hyperliquid BTC/SOL feeds `READY`, `RECONCILED`, `mode=paper`, `live_order_capability=false`, and empty positions/orders. No runtime command/order, private execution, proxy, or firewall action occurred. Exact next step: monitor autonomous snapshots and public-feed freshness; any real durable open/uncertain state must remain `MANAGE_ONLY` until a separately authorized recovery design exists.
Done: isolated open-group/crash-before-ACK reconciliation proof, read-only doctor, and backup/restore/monitoring/live-gate runbook. VPS online backup and restore-copy drill verified matching hashes; both doctor reports are `OK`/WAL/reconciled/zero positions-orders/`BLOCKED_PAPER_ONLY`, with both services active throughout.
Next: paper-only monitoring and evidence collection. Remaining blockers: this is not live-ready; historical execution/fee/liquidation evidence and separately authorized live promotion are absent.

## Historical legacy Freqtrade reference

Nautilus replacement status: the following is historical context only and must not override the current paper status above.
Status: ACTIVE — #73 Wave Engine dry-run experiment running; #65 observer monitors it. #74 Wave Engine GUI/replay slice implemented and smoke/QA accepted locally. No live trading approved.
GitHub Project item: #65 native Freqtrade observer In Progress; #70 Strategy C In Progress; #71 validation plan created; #72 Mozart research paused; #73 Wave Engine In Progress; #74 GUI/replay created/accepted locally.
Canonical root: `/root/.openclaw/workspace/coinmaster` (app `/root/.openclaw/workspace/coinmaster/coinmaster`; runtime `/opt/coinmaster/freqtrade`).
Branch / HEAD / origin/main / deploy commit / divergence: `freqtrade-stage1-migration` / `fd634736f7c2fff592c2cd52a5fbd6e957a69edd` / `f6f2279cd10eb97223d20cbf958a6c4e2f93737a` / `fd634736f7c2fff592c2cd52a5fbd6e957a69edd` / `origin/main...HEAD = 0 46`.
Goal: keep native dry-run safe while validating/optimizing Wave Engine toward aggressive ROI; replacement remains dry-run/research-only.
Done: #73 Wave Engine V1 staged/deployed to dry-run with separate DB `tradesv3.wave_engine_dryrun.sqlite`, preserving old DB/open simulated trades; rollback backup under `/var/lib/coinmaster/freqtrade/backups/wave-engine-switch-20260430T0715Z/`.
Done: deployed bot verified after switch: `dry_run=true`, `state=running`, strategy `CoinMasterWaveEngineV1`, bot `coinmaster-wave-engine-dryrun`, open trades `0`, locks `0`.
Done: corrected no-lookahead profiles validated native no-cache `20260101-20260427`: 158 trades, `+205.331 USDC` / `+20.53%`, PF `1.90`, max underwater `1.89%`; lookahead `has_bias=No`; recursive no formula variance.
Done: later aggressive split profile deployed `20260430`: full `+139.203 USDC` / `+13.92%`, Apr OOS `+65.902 USDC` / `+6.59%`, PF `3.15`; lookahead/recursive clean after debug-column fix.
Done: #74 replay/GUI implemented: backend `/api/wave-engine/profiles|replay`, Python exporter, WaveEnginePage chart, density caps, validations; frontend renders backend replay JSON only.
Done checks: `npm run check`, `npm run invariants:wave-engine-rules` (31/31), `python3 scripts/check-wave-engine-prototype.py`, native Freqtrade list/backtest/lookahead/recursive, Docker exporter smoke, `npm run build:web` passed for relevant slices.
Next exact step: verify `/api/wave-engine/replay` end-to-end against a running Express app on a host with Docker daemon access; then monitor first Wave Engine dry-run signal/order/trade/fill/close or any health regression and notify Vladimir.
Checks / commit / deploy / push: runtime dry-run API/log checks ok; #74 local checks ok; cleanup removed generated runtime/pycache junk; Wave Engine source + protocol docs are committed locally in this branch; GitHub Project/API sync may be blocked by auth 401; do not expose tokens.
Blockers / risks: no live trading; Docker daemon unavailable in current sandbox for endpoint E2E; Strategy C not promotion-grade; Mozart exact clone abandoned; April/recent-regime/stress risk remains; do not discard dirty tree.
Key files: `.ops/ACTIVE_TASK.md`; `coinmaster/freqtrade/wave_engine/`; `coinmaster/freqtrade/user_data/strategies/CoinMasterWaveEngineV1.py`; `coinmaster/src/server/waveEngineReplay.ts`; `coinmaster/src/web/pages/WaveEnginePage.tsx`; `/var/log/coinmaster/freqtrade-dryrun-observer-state.json`; `/var/lib/coinmaster/freqtrade/research/wave-engine/`.

P4 native paper implementation in progress (2026-09-20): clean branch `codex/nautilus-btc-sol-mvp` has a new one-node Nautilus paper worker pending commit/deploy. It uses public Bybit BTCUSDT/SOLUSDT and Hyperliquid BTC/SOL data clients plus only the native `SandboxLiveExecClientFactory` for BYBIT; no private exchange execution transport is imported/registered. Health is fail-closed (`DATA_STALE/PAUSED`) until a validated >=730 daily manifest and current quote/mark/funding data exist; funding is observed only and never auto-posted. Local P4 tests plus full runtime suite passed (42 total); isolated native sandbox lifecycle: 5 fills, flat positions, account report. Current local public-provider smoke produced `ValueError` during provider initialization and remained safely paused. Exact next step: commit/push then controlled restart of only `coinmaster-paper.service` on VPS, verify loopback health/client allowlist/journal persistence and isolated self-test. No live orders/transfers/production edits authorized.

P4 native paper deployed (2026-09-20): VPS canonical checkout/service code is `989f2486ad63a9a1bb6c3087cbb2ae048adf2ed9`, `coinmaster-paper.service` is `active/running`, and `127.0.0.1:18181/health` shows public Bybit+Hyperliquid BTC/SOL feeds READY, only native Sandbox execution factory/client, `live_order_capability=false`, and no credentials scrubbed/used. Isolated VPS sandbox self-test: 5 native fills, 0 open positions, account report present; `/var/lib/coinmaster-paper/paper.sqlite` persists lock/commands/events/snapshot in WAL. The service remains correctly `DATA_STALE/PAUSED` / `safe_for_increase=false` because it has no validated >=730 daily history manifest; funding remains observation-only. A stuck legacy worker required force-stopping only the paper service process during its controlled restart; no unrelated service was touched. Exact next step: provision the validated public warmup manifest into paper runtime and only then exercise/reconcile a paper strategy lifecycle. Issues #88/#89 remain open; do not treat this as live-ready.

2026-09-25 Stage-G native entry-control production checkpoint: commits `882cb75` and `1a94874` add only authenticated, instance-bound pause/resume-new-entries for `hl-stageg-testnet`; flatten and promotion remain unavailable. The worker patch was derived from the exact installed sealed 1.231 baseline, not the later #110 source surface: `paper.py` adds durable command/idempotency/latest-state access using existing `paper_commands` and `paper_command_audit`, and the strategy admission callback fails closed before non-reduce submissions. Existing reduce-only exits remain available. Exact-baseline isolated tests covered pause, duplicate replay, conflict, restart persistence, entry gating, and loopback HTTP 401/403/422/409/200 behavior. The sidecar requires normal GUI auth/CSRF, relays only to the loopback worker using a separate internal transport token, and requires native read-after-write; it keeps controls disabled when that transport is unavailable.

The backed-up production overlay is active. A root-owned 0600 shared internal-token EnvironmentFile and unit-scoped drop-ins are installed without recording the token. Only `coinmaster-hl-stageg-testnet.service` and then `coinmaster-runtime.service` were restarted. The GUI release `/srv/coinmaster-native-gui/releases/1a94874cb99dc116f6bc065c070c2fe925e4d67c` is active; its deployed index hash matches the canonical build. Guarded activation passed authenticated Pause -> native read-back -> Resume -> native read-back with no order submission. A further flat worker restart preserved `RUNNING` control state. Current status is READY, native_cash `10000`, zero positions/orders, `live_order_capability=false`, and sealed candidate/strategy/policy hashes `637762...b6d` / `3d79e...2f7` / `eee44...687`; GUI/paper peer PIDs stayed unchanged during worker restarts and all services have zero restarts afterward. The Stage-G warmup timer is active. Public browser acceptance remains limited by the earlier client-to-VPS asset-body timeout; no credential probing was performed. No real order, live execution, strategy-economic change, reseal, or `/opt` edit occurred.

2026-09-25 Stage-G seal/release checkpoint (#99): guarded release of source 5481478 was blocked before any release copy or service action. The explicitly requested strategy source SHA-256 989581fb4b7557137b67a04359363dd4c3217d96bbfa2d3ea60d0f0ad4f5753d was verified, and the temporary approval pin was reverted after the native lifecycle suite failed: test_next_funding_timestamp_is_observation_only_not_an_early_cash_post raises AttributeError because this source calls MarkPriceUpdate.price while the installed Nautilus 1.231 object has no price attribute. The previous sealed approval/release remains pinned to 3d79e50495de8b648045a43ecf4a6b320d4565bead3140e2455848a879c012f7. No candidate, parameter, policy, warmup, order, intent, cash, service, or runtime release state changed. Next: correct and independently validate the MarkPriceUpdate bridge, obtain a new exact strategy hash authorization, then repeat the guarded seal/release workflow.

2026-09-25 Stage-G Sandbox seal checkpoint (#99): owner-authorized strategy SHA-256 f890667eb14a3e399f36325554ffa237dc548af54d5a62395517ae67fd94d4a0 is pinned in the operational approval. Candidate and execution-policy identities remain 637762130c76396cd7c6e24644e31b28079a1683e4460d57f103dde42c603b6d and eee44dde50781b6364923e6aad8df714860796b0ec7cd6121adef7c25406f687. Relevant native attachment and lifecycle tests passed: 86 passed, 3 skipped; compile and diff checks passed. This is Sandbox-only and does not authorize real execution. Next: create rollback evidence, stage the guarded isolated release, restart only the Stage-G service from a flat durable state, and verify sustained public-feed readiness with zero orders and positions.

2026-09-25 Stage-G Sandbox release checkpoint (#99): deployed sealed release commit 626fd337639e8b8b4d3a45c00614728d465220d0 to isolated /srv/coinmaster/runtime after a flat-state rollback snapshot. Installed strategy and approval SHA-256 are f890667eb14a3e399f36325554ffa237dc548af54d5a62395517ae67fd94d4a0; candidate and execution-policy identities remain 637762130c76396cd7c6e24644e31b28079a1683e4460d57f103dde42c603b6d and eee44dde50781b6364923e6aad8df714860796b0ec7cd6121adef7c25406f687. Rollback archive and SQLite snapshot are under /var/lib/coinmaster-hl-stageg-testnet/backups/stageg-seal-pre-626fd33-20260925T173459Z with hashes c74a0c240de968a14099923f96c266c669e8c3747db3e3d7757d740371c53d15 and 4f09319c4250ff35eb00912484792a8c84cf5b285b47cc7cb95ece112a548fd9. Only coinmaster-hl-stageg-testnet.service restarted, now PID 638008 with zero restarts; peer paper/runtime PIDs remain 3653323 and 475232. Five post-start samples report PUBLIC_FEEDS_READY, READY BTC/SOL feeds, SEALED_APPROVAL_MATCH, Sandbox native cash 10000, zero orders/positions, no recovery requirement, and live_order_capability=false. No real order capability or external execution route exists. Next: continue natural virtual lifecycle observation and refresh warmup after UTC boundaries.
