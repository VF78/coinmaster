2026-09-26 CM-05/#110 source-only live boundary: Canonical VPS source adds strict read-only HL Info to pinned Nautilus 1.231 OrderStatusReport/FillReport/PositionStatusReport normalization. It proves durable OID/CLOID alias and native report field mapping for a fake partial BTC TP but always returns CONDITIONAL_ONLY or FAILED, never recovery_confirmed; 32 focused collector/observer/native-report tests pass. The installed Stage-G trader is unchanged; no deploy, restart, live execution client, or real order. HL Info has no common atomic snapshot or proven complete fill cursor, so automatic LiveExecutionEngine takeover remains RECOVERY_REQUIRED. The owner permits discarding the current virtual position once during an eventual host migration, while future restarts must preserve exposure; host choice is pending.

2026-09-26 CM-07 read-only GUI deployment identity correction: the observed installed GUI release differs from the prior repo-backed checkpoint and matches a previously recorded rollback target. The installed legacy artifact has no release identity manifest, so its payload is not manifest-attested. The current source staging gate requires and verifies that manifest for new releases. This observation supersedes the earlier claim about which GUI release was active. No deployment or service action was taken.

2026-09-26 Owner clarification (#110 / CM-05): Nautilus 2.0.0rc5 was an erroneous MVP research branch; it is NOT selected or used. The executable runtime and `runtime/uv.lock` remain pinned to Nautilus 1.231.0. No automatic migration or deployment: any version change requires a separate explicit owner decision after wheel-level native recovery proof. Preserve the currently open 1.231 Sandbox BTC+TP; do not restart/deploy it. Track the #110 recovery blocker separately.

2026-09-25 GUI trader-workspace visual release (#100/#102/#103/#105/#108): the deployment checkpoint recorded a GUI release as active and passed guarded API/SPA smoke, sealed projection, and UI checks. A later read-only check found the installed release pointer differed from that checkpoint; see the CM-07 correction above. Authenticated public browser acceptance remained unverified. This historical note does not attest the currently installed payload.

2026-09-25 GUI production checkpoint (#100/#102/#103/#105/#108): Source checkpoints c3d7842, 4b18c2b, c2d31f8 and 0cfb2ce are pushed. The isolated GUI/API release 0cfb2ce is active at coinmaster24.com from the exact 32a runtime baseline plus the verified Stage-G UI/API overlay; only coinmaster-runtime restarted (PID 451884). A flat-state backup preceded the only Stage-G worker restart; the new PID 452805 has NRestarts=0, READY/PUBLIC_FEEDS_READY, sealed strategy SHA-256 3d79e50495de8b648045a43ecf4a6b320d4565bead3140e2455848a879c012f7, native Sandbox cash 10000 USDC, zero positions/orders, and live_order_capability=false. Paper PID 3653323 is unchanged. The worker rollback copy is /var/lib/coinmaster-hl-stageg-testnet/backups/gui-account-projection-20260925T0830Z/ (old worker SHA-256 89b72b519cfb92b0ac7e6040553f831b50417a3fdf51fb8a33faffcbfec805b6); installed minimal account-projection worker SHA-256 is 5d91445c6671b446d301647c08cafd3e603da0e7f01c39ae70a6a292b9ba5997. Focused projection tests 2 passed; TypeScript, generated API contract, UI invariant (41), and web/custom builds passed. GUI stage/activation smoke verified authentication, SPA, exact sealed READY projection and disabled controls with unchanged peer PIDs. Authenticated desktop/mobile browser acceptance remains unverified because the known client-to-VPS asset body timeout persists. No real orders, credentials, paper routing, Freqtrade route, or native control command were enabled; pause/resume/flatten remain visibly disabled pending the separate native lifecycle gate.

2026-09-25 Stage-G Sandbox maintenance deployment (#99): Scoped installed release uses exact 5bef600 maintenance source plus ead1329 lexical current-manifest path and 4dca174 status projection fix; installed sealed WaveOverlay SHA-256 remains 3d79e50495de8b648045a43ecf4a6b320d4565bead3140e2455848a879c012f7, Nautilus 1.231.0. Actual unit config points to /var/lib/coinmaster-hl-stageg-testnet/data/current/manifest.json; current immutable genuine Bybit artifact SHA-256 4b998fde471d7b7a80f310558a3bc434a96278ea7bf2118111f632787a679170 covers 1,483 BTC/SOL sessions through Sep23 completed UTC. Warmup timer first refresh succeeded Sep24 22:04 UTC, then remains active on 15-minute cadence. Stage-G PID 258525 had NRestarts=0 and five latest status cycles PUBLIC_FEEDS_READY, native node/strategy running, BTC/SOL feeds READY, orders_enabled and safe_for_increase true. Loopback /status HTTP 200 reports Sandbox-only, live_order_capability=false, virtual 10000 USDC, zero positions/orders. Latest flat journal read has no paper events/intents/funding or unwanted fills. Exact sealed-release isolated verification: 51 relevant tests passed, compile/diff clean. Rollback tar: /var/lib/coinmaster-hl-stageg-testnet/backups/stageg-maintenance-pre-5bef600-20260924T2153Z/installed-release-and-units.tar (SHA-256 f0a5d1df7e4342beb7c2c50c02c10e5534cbf85950b38ad3574d240239bcc248). Next UTC completed-session refresh remains to be observed; no virtual round trip yet. Native Sandbox open-exposure crash rehydration remains unsupported and fails closed. No real order capability or live recovery deployment.

2026-09-25 #110 isolated Nautilus 2.0.0rc5 qualification: official PyPI cp312-manylinux_2_34_x86_64 wheel SHA-256 eab45fafd2312deda1236554c49a9798bfc76bc8465af864878e2f70189ebebe and sdist SHA-256 d566bade9e8dc6ad35de0e2566658fbd1ae75a89ee2c45c8e48de5fcc6929399 verified; /tmp-only venv reports 2.0.0rc5. On read-only public account, native bulk order/position reports returned genuine empty, while fill reports raised Fill snapshot incomplete rather than a partial list. Loopback proxy refusal/429/500 raised ValueError for all three bulk report methods; canceled Future raised CancelledError. rc5 source uses ReportSweep.complete for malformed/unresolved venue rows and mass-status reports_complete=false when historical orders or fills hit the 2,000-row cap. Direct request_fill_reports does not itself carry cap completeness: takeover must consume validated mass-status window. Current #110 code is not rc5-compatible: model.data/model.currencies modules and BybitLiveDataClientFactory import are absent; native LiveNode/config/strategy state migration and fake transport crash tests remain. Mixed-row/cap wheel-level fixture acceptance and native restart proof are NOT complete. No pinned dependency/lockfile/runtime venv/service change; recovery gate closed. Prefer a separate deliberate rc5 migration qualification over an unbuilt private 1.231 Rust wheel on the 7.9GB-free production VPS. Upstream source: https://github.com/nautechsystems/nautilus_trader/tree/v2.0.0rc5/crates/adapters/hyperliquid/src .

2026-09-25 #110 read-only HL report/feed checkpoint: owner-supplied public master address queried through official POST /info only; clearinghouseState returned 0 positions, frontendOpenOrders 0, and userFillsByTime (last 7 days, unaggregated) 0. Association is not independently proven and empty replies do not validate nonempty parser/fee/cause behavior. Official Info docs cap userFillsByTime at 2,000 per page/10,000 recent; installed Nautilus 1.231 Python bulk report methods return [] on exception, and native Rust parsing can skip bad rows, so response completeness and manual-vs-liquidation close cause are NOT proven. Unexplained flat remains RECOVERY_REQUIRED, no synthetic close/lockout. Live recovery subclass now requires reconciliation-confirmed plus fresh BTC/SOL quotes and marks within explicit positive max_mark_age_ns before entries or any new reduce-only submit; restored state discards quote freshness. 60 relevant tests passed. One VPS only; no credentials, real orders, deploy, or recovery-gate opening. Next scoped blocker: version-pinned native adapter/report completeness semantics (including timeout, malformed row, history cap) and authoritative close cause before open-state takeover. Official API: https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint .

2026-09-25 #110 TP identity correction: zero-fill resting BTC TP recovery now checks intent side even without fill reports and requires the durably recorded native side/LIMIT/GTC/post-only/price plus open native status. Missing or altered shape fails closed before any episode/cursor commit; wrong side, price, type, TIF, status, and missing shape regressions preserve checkpoint bytes. 59 relevant tests passed with disposable loopback Redis; diff/compile checks pass. Architecture is one VPS only: local durable Redis/SQLite, native venue reconciliation, bounded systemd restart; no standby, second node, or offhost HA. The recovery gate remains closed pending full forced-close, native protective-management, fresh-feed, and sanitized adapter evidence. No service deploy, credentials, or real orders.

2026-09-25 #110 open-exposure ownership checkpoint: native report reconciliation now requires every checkpointed pending order to appear and match durable instrument/quantity/reduce-only fields plus native account, strategy, venue ID, side, fill set, position, and finite positive native USDC balance before any domain commit. A resting BTC TP reduce-only fixture proves owned-open-order validation; missing, foreign, altered reduce-only, and wrong-account reports leave episode/checkpoint unchanged. A position that vanishes without a coherent close fill/reason stays RECOVERY_REQUIRED; no synthetic liquidation/close. 53 relevant tests passed; compile/diff checks passed. This is still NOT autonomous takeover: sealed WaveOverlay has no standing downside protective stop (BTC TP is a reduction, not a stop), installed HL 1.231 adapter needs persisted Cache order for CLOID remap and returns empty reports on transport exceptions, and no sanitized real account/order fixture is available. Forced/venue-close reason, protective exit ownership, and fresh-public-feed management gate remain unproven; recovery_confirmed remains false. No credentials, real orders, deploy, or operational approval change.

2026-09-25 #110 acceptance-slice checkpoint (VPS UTC Sep24 21:04): fake native LiveExecutionClient durably accepts an order and hard-exits before ACK; restored TradingNode/Redis reclaims the exact generated ID with native partial fill/order/position reports and no duplicate submit. Independent uninterrupted native callback path and recovered path match native position, fill count, exact 0.27 USDC commission row, and USDC account cache total for identical fake-venue events. Cash is still an authoritative fake venue account event, not a second PnL calculation or proof of exchange cash correctness. Shared pure domain fill transition now replays SOL_HALF_EXIT cooldown and full-fill parent/pending/order mapping cleanup exactly once; entry gate remains closed. 48 relevant tests passed; compile/diff checks passed. Protective-order ownership/exit and forced/terminal close callback restoration remain acceptance blockers. No credentials, real orders, deploy, or approval change. Stage-G installed release remains old: at VPS UTC Sep24 21:04 PID 180775/NRestarts0, PUBLIC_FEEDS_READY, node/strategy running, BTC/SOL READY, orders_enabled/safe_for_increase true, Sandbox-only and live-order capability false. Genuine 1,483-session Bybit artifact ends at Sep24 00:00 UTC (latest completed Sep23); refresh timer is absent, so next UTC rollover will require maintenance rollout approval or fail-close.

2026-09-24 #110 P1 recovery correction: full scoped native account/order/strategy/side/trade-set/quantity and position parity are validated against a detached versioned episode before one atomic checkpoint-plus-trade-ID commit. Invalid reports leave durable bytes, cursor, and in-memory episode unchanged. Normal WaveOverlay fill callbacks now commit the same applied trade ID and updated episode atomically before follow-on decisions; a repeated historical report after restart cannot double the domain quantity. Real Redis/TradingNode and negative tests plus native strategy/config checks: 46 passed; compile/diff checks passed. The fake transport still supplies venue state instead of accepting a real submit, and its account cash is a supplied report, so uninterrupted-versus-recovered execution/fee/cash parity remains unproven. No deploy or live order authorization; Stage-G worker unchanged.

2026-09-24 #110 live-first proof: Nautilus 1.231 TradingNode with Redis and fake report-only execution transport recovered a pre-submit intent plus versioned WaveOverlay episode after hard crash, then native partial order/fill/position/account reports. Domain fill cursor applied one missing transition exactly once through two restarts with no duplicate submit or second PnL engine; 44 relevant tests passed. Fail-closed recovery gate remains unconfirmed, so this does not authorize live trading or claim complete autonomous takeover. Stage-G installed service remains on its prior release.

2026-09-24 Stage-G recovery scope: Nautilus 1.231.0 is the actual isolated worker, canonical venv, and lock pin. The installed Sandbox has no supported open matching-engine restoration. #110 in GitHub Project #2 therefore tracks an isolated live-ready recovery proof using native Nautilus strategy/risk and fake HL transport; no live order is authorized. The Stage-G reliability source checkpoint adds genuine incremental UTC warmup, fail-closed crash revision and flat cash/health preparation, but does not satisfy open-state takeover or 24/7 continuity. #99 stays In Progress.

# Coinmaster Project Truth

Updated: 2026-09-26 Europe/Moscow

## Sources of truth

- Backlog/status: GitHub Project #2 `https://github.com/users/VF78/projects/2`
- Canonical repo: `/root/.openclaw/workspace/coinmaster/coinmaster`
- Deploy mirror: `/opt/coinmaster` — never edit manually.
- Current restart snapshot: `.ops/ACTIVE_TASK.md`
- Stable rules only live here; backlog/current work does not.

## Objective

Coinmaster must autonomously trade within Trading Rules/Radar and move toward **≥50% monthly ROI**. Every task must directly improve profitability, execution quality, risk/loss control, or hypothesis-testing speed/quality. No refactor-for-refactor.

## Latest native research checkpoint

Stage G maximum-ROI reproducible research winner is EMA34, TP `(0.2125,0.2125,0.575)`, BTC `7.875`, SOL `(1.75,2.625,3.5)`, TOTAL `1978792.33053629` (+`50084.62324134` vs Stage F). Its completed G4 row and one clean from-genesis confirmation match exactly across TOTAL/ACTIVE/reserve, fee splits/notionals, 421 fills, zero liquidations, 2280 funding postings, and normalized fills/orders (only engine `init_id` is dropped). Prior G1 at `1945213.42678339` is runner-up. The prior G4 exclusion was procedural, not evidence of an invalid simulation; no new parameter/grid/extension or other native simulation ran. The confirmed report remains `NOT_FAITHFUL_DIAGNOSTIC` and non-live-ranking. Corrected isolated H1/H2 tie Stage D H0 exactly, strict TP fill-cycle H3 is `548601.24002896`, and H4 BTC-only is `89591.16982085`; their final sealed checkpoint SHA-256 remains `7d2de71d1b4a4c54fe0cf1ee036cf224f654c93d4fa474f0416b917853ad9d60`.

## Current #97 D3 attachment checkpoint

2026-09-23 07:52 UTC operational update: the dedicated `coinmaster-hl-stageg-testnet.service` is enabled and running on the user-confirmed VPS. Remote source/config hashes match committed `8537f69`; the Sep-23 warmup manifest SHA-256 matches `f397cb1cda1ba7d8279a85789317bf4456fe02c6b6fccff749d9fdc80d409c9d`. Remote attachment gate reports READY/`SEALED_APPROVAL_MATCH`, candidate `637762130c76396cd7c6e24644e31b28079a1683e4460d57f103dde42c603b6d`, public Bybit daily signals and public HL MAINNET BTC/SOL quote/mark/funding feeds. The running Nautilus node registers `WaveOverlayStrategy` and only `SandboxExecutionClient`; `live_order_capability=false`, `prime_error=null`, both feeds READY, service active with zero restarts, existing paper/runtime services active. This is `DRY_RUN_STARTED` for virtual-only D5 observation. At startup native portfolio reported zero open orders/positions; the first natural strategy signal and virtual round-trip have not yet occurred. Fixed public-base fee and local margin are virtual model policy; account-specific HL fees, settled funding cash, HL 24-month execution quality and ROI remain unverified. Refresh warmup after each UTC boundary before any restart; real-capital promotion is separately gated. Older same-day statements below describing no launch or transfer block are historical checkpoints, not current status.

2026-09-23 virtual-only final launch pass: the valid fresh source is the immutable ignored `runtime/var/data/stageg-bybit-warmup-20260923-verified3/manifest.json`, rebuilt from the verified Sep-21 predecessor and the explicitly preserved public Sep-22 tail whose raw manifest SHA-256 is `0974b9e0d2c39a0cdf6afa6a522540710ebbdb5b3ec0569659532b87da7fb7e0`. It verifies READY at the Sep-23 UTC boundary with 1,482 contiguous BTC/SOL Bybit kline+mark sessions each, zero gaps/duplicates; artifact manifest SHA-256 is `f397cb1cda1ba7d8279a85789317bf4456fe02c6b6fccff749d9fdc80d409c9d`. Strict attachment matches sealed candidate/strategy/execution-policy identities `637762130c76396cd7c6e24644e31b28079a1683e4460d57f103dde42c603b6d` / `1f9597c38f895bd64af48634c8fe5112b50df004a8a4b597dfb4fe5d6144c2da` / `eee44dde50781b6364923e6aad8df714860796b0ec7cd6121adef7c25406f687`. A credential-free local native proof attaches HL BTC/SOL IDs to the sealed configuration and public-profile sandbox margin policy, emits a Sandbox native BTC entry then reduce-only exit, reports native USDC fees/account, ends flat, and protects a durable HL position as `MANAGE_ONLY_DURABLE_OPEN_STATE` after restart; it is an execution-lifecycle proof, not economics parity. Public `next_funding_ns` is labelled `OBSERVED_MODELLED_UNPOSTED_NEXT_PAYMENT_NOT_CONFIRMED_SETTLEMENT` and is never posted early; no settlement cashflow/equity claim exists. Full split runtime verification is 205 passed (72 existing Pandas warnings). Read-only VPS inventory confirms existing paper/runtime active, dedicated Stage-G directory isolated, no dedicated env/warmup, and the Stage-G unit inactive. A requested source/config/artifact transfer was blocked by the safety system, hence no VPS file/service/existing-service state changed. Git commit/push is also unavailable locally until the owner accepts the host's Xcode license. #97 remains In Progress pending explicit approval of that specific verified-artifact/source transfer, remote gate verification, and isolated service start.

As of 2026-09-23, the sealed `WaveOverlayStrategy` is attached in code to source-preserving public Bybit daily signals, public Hyperliquid MAINNET marks, and exactly one local Nautilus Sandbox Hyperliquid/USDC execution account. Attachment is gated on the fresh verified Bybit warmup artifact and current public MAINNET profile; the configured provision path is `/var/lib/coinmaster-hl-stageg-testnet/data/stageg-bybit-warmup-20260923-verified3/manifest.json`. The strategy no longer imports research `TierMarginPolicy`: an injected public-profile policy uses explicit local Sandbox leverage (BTC 40x, SOL 20x) and fails closed on missing/stale marks or tier violations. Sandbox seed is 10,000 USDC; the comparison to 10,000 USDT research capital is an explicit nominal 1:1 assumption only. Fee policy fixes public base maker/taker rates at 0.015%/0.045%, without asserting an account tier, historical HL ROI, or 24-month HL execution-quality evidence.

Superseded same-day launch check: `stageg-bybit-warmup-20260923-verified2` used a different tail provenance and must not be provisioned. Its funding-oracle no-go is superseded only for credential-free virtual execution: future funding is observed/modelled and unposted, rather than a barrier to attached sandbox lifecycle testing. It remains a limitation on accounting/equity claims, not evidence of a settled cashflow. No VPS service was activated or changed.

The Sandbox simulated exchange supports native `adjust_account` cash postings. However, the public HL adapter's `FundingRateUpdate.next_funding_ns` means the next scheduled payment, not a confirmed settlement, and supplies no settlement oracle. The strategy therefore observes it but posts no funding until a separate confirmed-settlement event exists; this is an adapter-data limitation, not a Sandbox capability limitation. A running fresh Sandbox cache may not overwrite durable open/uncertain state after restart, so recovery stays manage-only. Isolated fixtures verify native lifecycle, fill/fee/margin/equity accounting, funding idempotency, pause/flatten, and process-restart manage-only behavior. A no-start local node build confirmed the READY gate and a `SimulatedExchange`; no public connection, process start, VPS/deploy, account/wallet/authentication, exchange order, or transfer occurred. #97 remains In Progress in GitHub Project #2.

No-go gaps retained deliberately: the artifact must be revalidated on every UTC-session boundary rather than treated as statically READY; Sandbox funding posting does not yet validate the resulting account-balance delta; recorded candidate/code identities are not external sealed-approval comparisons; and the generic native Sandbox/BacktestEngine fixtures are not an attached Stage-G test using HL BTC/SOL IDs. None may be presented as evidence of actual HL lifecycle parity.

## Immutable runtime configuration checkpoint

Issue #98 D4 (2026-09-23, P1 isolation/config correction plus inert VPS staging): trader/research process isolation is prepared without changing the native research engine. `coinmaster-hl-stageg-testnet.service` is the independent one-`TradingNode` testnet trader unit as unprivileged `coinmaster-hl`, with a `0700` StateDirectory, a root-only trader env contract, and no API/research dependency; `coinmaster-runtime.service` and its child research jobs run as separate `coinmaster-research`, with their own `0700` StateDirectory. Both consume only a read-only `/srv/coinmaster/runtime` release; the instance config fails closed unless its strategy and DB paths are the isolated release/state locations. Runtime now explicitly sets writable `COINMASTER_RESEARCH_DATA_ROOT=/var/lib/coinmaster-runtime/data`, preventing research jobs from attempting to write under the read-only release; historical public inputs are provisioned separately read-only, never from trader/paper/control SQLite. `ResearchJobManager` still launches the existing child backtest subprocesses but now removes Hyperliquid/Bybit credential names, trader/paper/control DB paths, and operator-token environment values; each run retains its private `runs/jobs/<id>` directory. Shared-VPS configuration requires one worker and preserves the established canonical lease behavior. The read-only `process_isolation_doctor.py` validates unit identity/path/credential contracts and has an explicit host metadata mode for root-owned env files plus separately owned trader/research StateDirectories; local fixture and real Popen-child sentinel tests cover these boundaries, and a local concurrent smoke ran research beside a public-only observation-trader stub. Focused D3/D4/API checks: 55 passed (2 existing Pandas warnings). Authorized inert VPS staging created only `coinmaster-hl:coinmaster-hl`, `/var/lib/coinmaster-hl-stageg-testnet` (`0700`), and root-owned `/srv/coinmaster/runtime` from local verified `5562fb5` (source-content SHA-256 `1055aa6715fbbd91bb630d92c2fcc702da0d920ab41a669aa021070f6569acce`, excluding generated bytecode, `.venv`, and `var/data`); pinned non-dev dependencies installed root-owned from `uv.lock` (~867 MiB), with ~9.5 GiB disk free. No secret env, D4 unit, enable/start/restart, node, wallet, account query, order, transfer, existing-unit/change, `/opt`, or canonical-checkout mutation occurred; paper/runtime stayed active (PIDs `2315259`/`2334804`). D4 is not operational acceptance: #97 remains open because account value is 0 and authenticated native lifecycle/concurrent VPS evidence are still absent.

Issue #97 D3 (2026-09-23, corrected local-only virtual-dry-run infrastructure): `hl-stageg-testnet` retains its isolated one-node/state identity and frozen Stage-G candidate, but its only route is native public Hyperliquid TESTNET data plus Nautilus 1.231 `SandboxExecutionClient` for the Hyperliquid venue/USDC. The exact execution allowlist is `SandboxLiveExecClientFactory`; no API wallet/address, Hyperliquid execution factory, account query, exchange order, transfer, or live activation exists in this path. The running node registers only `FeedObserver`, while D1 durable submit/cancel/reduce-only/post-only/taker/partial-fill lifecycle hooks remain injectable fixtures. Local state is sandbox-local (`SANDBOX_NO_REMOTE_RECONCILIATION`), and recovered durable exposure remains `MANAGE_ONLY`. Status explicitly separates observed public rates/marks from unposted modelled funding, native Sandbox commissions from unknown account fees, and Sandbox margin from unknown remote margin. Focused D3/D4/profile/isolation checks: 41 passed and doctor `OK`; broad split runtime verification: 194 passed, one unrelated research-child cancellation test blocked by this host's `os.killpg` permission policy. A bounded local native probe built and ran the one node with only `SandboxExecutionClient`, then stopped cleanly without orders; host DNS could not resolve the public testnet endpoint, so BTC/SOL feeds remained `DATA_STALE/PAUSED` and no public prices/funding, fills, or positions were observed. The node does not attach `WaveOverlayStrategy`: start requires a latest-24-month, real/provenanced/gap-checked warmup and executable parity with the sealed Stage-G winner. Existing Bybit signal history can satisfy that gate only if it is the exact sealed strategy's signal source and is recent through launch; it cannot be silently relabelled as HL data. Conversely, the empty/zero-volume HL boundary evidence in `var/venue-manifest.json` and any HL proxy rows cannot be used as tradable execution, mark, funding, or warmup evidence. The current data route is still TESTNET, so it is explicitly not the requested public-mainnet dry run and must not be started as one. No deploy, VPS change, GitHub mutation, push, wallet/account query, exchange order, or transfer occurred. The remaining blockers are local public-DNS reachability, public-mainnet data-route conversion, unverified recent signal warmup, and unproven live-feed strategy parity; the 24-month HL BBO/L2 gap remains non-substitutable for execution claims.

Preservation status (2026-09-22): D0 `ab0521d` and D1 `eb56033` are accepted and Done; D2 `5d60986` is pushed to `codex/nautilus-btc-sol-mvp` but remains In Progress. D2 contains a public MAINNET metadata/margin snapshot; the planned TESTNET node requires its own verified profile because Hyperliquid testnet tiers differ. Independent D2 audit was interrupted by a usage limit. GitHub Project #2 issue #96 records the blocker and is the current backlog/status source; local and origin HEAD both equal `5d60986341c6e4a153cef8d0185fd674d2ec55fd` before this preservation note.

D3 mainnet-route correction (2026-09-23): the public data route now pins Hyperliquid MAINNET while the only execution route remains local native Sandbox/USDC. This does not authorize a start: the available Bybit warmup ends at the 2026-09-01 completed session and the generic loader rejects it as stale; `WaveOverlayStrategy` still couples Bybit signal/execution IDs and imports research-only `TierMarginPolicy`, so executable HL parity is unproven. The next pass must prove the sealed winner's exact real, current signal source and replace that production-path margin/fee/funding coupling with venue-aware HL semantics before any strategy attachment.

D3 cross-venue gate (2026-09-23): `cross_venue_stage_g_gate` records immutable candidate and strategy-code identities while retaining Bybit BTC/SOL signal IDs and HL BTC/SOL Sandbox execution IDs as distinct provenance domains. It reads the current verified HL mainnet profile but is intentionally unattached because it detects the stale warmup and research-only margin-policy import. Public Bybit/HL tail retrieval is reachable from the VPS without credentials; any update must create a new isolated, provenance-checked artifact rather than mutate frozen research inputs.

Latest #97 D3 provenance/parity checkpoint (2026-09-23): the isolated artifact `runtime/var/data/stageg-bybit-warmup-20260922-verified2/manifest.json` was rebuilt from the verified base and Sep 2026 tail raw pages, retaining Bybit as the exact sealed signal source and recording HL MAINNET as execution market-data source. Base/tail source manifest SHA-256 values are `a3ece5d12a229a6bbb7d64d079f7b2c78e914eef46867d8df1e95935c9bcac74` / `d2e1ff66be91a81a9680a1483ea924836e765fca0a7903ee6f2e532377f836b7`; all raw page hashes and canonical page hashes were verified, then both daily signal datasets were independently rebuilt from raw and compared with their source normalized data. Each of BTC/SOL has 1,481 contiguous daily kline+mark sessions from 2022-09-02 through the 2026-09-21 completed UTC session. The dynamic loader is READY only through that session; it returns `WARMUP_LATEST_COMPLETED_SESSION_MISSING` after the 2026-09-22 UTC day closes until refreshed. Historical SOL funding off-grid timestamps are recorded and excluded from the warmup/funding claim. Runtime route config has public Bybit signal data + public HL MAINNET data and exactly one HL/USDC Nautilus Sandbox exec client; no strategy is attached or node started. Sandbox starts at 10,000 USDC versus the sealed 10,000 USDT research capital under an explicit 1:1 peg assumption, without asserting equivalence. Pinned candidate/code/declared fee-policy SHA-256 values: `637762130c76396cd7c6e24644e31b28079a1683e4460d57f103dde42c603b6d` / `02473b22d61aea1ede0dd93685ad2faf7133cb501564bf6194075d5979a63302` / `67bdfa1aea32e40202a6a50d7390afd311a59b92451129fd42d35c48d2c10472`. Gate remains `attachable=false`: the shared strategy still imports research-only Bybit `TierMarginPolicy`; HL account-specific margin/liquidation and fee tier/execution-quality parity are unproven; Sandbox funding cash posting is unsupported; and currency equivalence remains an assumption. Focused strategy/fixture/D3 suite: 39 passed (58 existing Pandas timestamp warnings). No node/service, strategy lifecycle, real orders, wallet/account/auth, VPS mutation, or live trading occurred; GitHub #97 remains In Progress.
Artifact manifest SHA-256: `2e6d111d044e77b33fce4aafe6db95d95442888be68d43df51a6aca499087d14`.

Issue #96 D2 testnet-profile correction is implemented locally on top of `ab48b7b`: a public, read-only testnet `metaAndAssetCtxs` snapshot (2026-09-22T21:01:26Z) supplies BTC `40→25→10x` at `0/10k/50k` from actual raw table `54`. SOL one-tier `10x` is explicitly separated as a documented `marginTableId < 50` derivation: raw `response.marginTables` has no invented table `10`, and the loader rejects one if added. Both retain Nautilus 1.231 canonical `*-USD-PERP.HYPERLIQUID` IDs. Profile/preflight now requires an explicit `mainnet` or `testnet` environment, rejects snapshot/mode mismatches, and scopes funding identity by environment. No wallet, account, execution/node, D3, VPS/deploy, order, optimizer/backtest, or push action occurred. Focused profile verification passed; earlier full runtime verification: 177 passed (70 existing Pandas warnings). The remaining D2/D3 blocker is separate authorization and a tested public-data/runtime integration; the 24-month HL BBO/L2 evidence gap remains non-substitutable.

Issue #96 D2 is implemented locally only. The production-facing Hyperliquid BTC/SOL perpetual profile stores a non-secret current public `metaAndAssetCtxs` provenance snapshot and source timestamp, USDC collateral/account currency, native IDs, venue size/price/minimum rules, configurable fee rates, current public margin table boundaries, and stable funding settlement identity. Production order/margin preflight is a standalone venue-profile abstraction with no research/native_fixture or Bybit tier-policy import. It fails closed when authenticated account collateral/mode/selected leverage is unknown and retains unavailable 24-month Hyperliquid BBO/L2 history as a non-substitutable blocker. No HL node/adapter activation, order, network, VPS, deploy, backtest, optimizer, or push occurred.

Issue #95 D1 execution-semantics parity is implemented locally on top of D0 and recorded in this local checkpoint. Planned SOL exits now use explicit native taker IOC control A (including zero-spread books); post-only GTC remains limited to explicit BTC TP targets. Regime/trail forced closes and SOL hard-timeout exits preempt queued/resting SOL work, cancel leaves, then reduce the reconciled remainder; a forced close clears timeout ownership before its quote loop and always uses actual native cache leaves. Every BTC entry/SOL add rechecks the runtime pause/stale/safe-for-increase gate immediately before submission; reductions bypass it. Confirmed partial BTC TP fills earn only the proportional cumulative SOL right and use their actual confirmed fill cycle for strict H3, while notional sizing uses buy ask/sell bid before precision/minimum validation. Funding journal isolation is unchanged. Focused unit/native tests plus broad runtime regression: 125 passed (68 existing Pandas warnings). No deploy, VPS/service, paper command/order, live action, GitHub mutation, optimizer, backtest, or push occurred.

Issue #94 D0 is implemented locally: `runtime/configs/stage-g-v1.json` pins the entire sealed Stage-G `Candidate` schema. The strict file loader canonicalizes and SHA-256 hashes Candidate content (never labels), rejects missing/extra/non-finite/economically invalid data, and returns a frozen Candidate. The non-secret `paper-stage-g-example.instance.json` documents the required instance envelope. Production paper startup now requires a strict instance config and loads it plus the strategy file once before `TradingNode` construction; it logs and reports `instance_id`, venue, mode, strategy/order identity, and exact candidate hash, while passing the latter IDs into the actual native strategy config. Missing or invalid production config fails closed; an in-process default exists only for isolated tests. Focused tests: 26 passed. No deploy/VPS/paper/live/optimizer action or push occurred. The explicitly addressed CLT Git binary is usable for the local checkpoint; D1 code parity is recorded above, while any operational activation remains separately scoped.

## Core invariants

- One active implementation task at a time.
- Route new CoinMaster tasks by `.ops/MODEL_ROUTING.md`; the current user-approved GPT-6 routing supersedes the older blanket `openai-codex/gpt-5.5` default.
- Non-trivial development uses subagent/coding-agent unless the change is trivial or Vladimir requests otherwise.
- GitHub Project status and `.ops/ACTIVE_TASK.md` must stay current.
- No secrets in repo, truth files, issue bodies, or chat.

## Deployment invariant

- All edits happen in canonical repo only.
- `/opt/coinmaster` is produced by deploy, not manual patches.
- Hyperliquid/Bybit/Telegram runtime connection settings live in persisted DB snapshot unless code explicitly says otherwise.
- Commit/deploy flow: post-commit hook or `scripts/deploy-prod-safe.sh` → typecheck/build → sync `src/`/`dist/` → restart `coinmaster.service` → write `/opt/coinmaster/.deploy-source-commit`.
- Before debugging/restarting, compare workspace `HEAD` with deployed commit; redeploy first if they differ.

## Protocol map

- Development: `.ops/SOFTWARE_DEVELOPMENT_PROTOCOL.md`
- Reset/handoff: `.ops/RESET_PREP_PROTOCOL.md`
- Task state/watchdog: `.ops/TASK_STATE_PROTOCOL.md`
- Current compact state: `.ops/ACTIVE_TASK.md`

2026-09-25 Stage-G native entry-control production checkpoint: commits `882cb75` and `1a94874` add only authenticated, instance-bound pause/resume-new-entries for `hl-stageg-testnet`; flatten and promotion remain unavailable. The worker patch was derived from the exact installed sealed 1.231 baseline, not the later #110 source surface: `paper.py` adds durable command/idempotency/latest-state access using existing `paper_commands` and `paper_command_audit`, and the strategy admission callback fails closed before non-reduce submissions. Existing reduce-only exits remain available. Exact-baseline isolated tests covered pause, duplicate replay, conflict, restart persistence, entry gating, and loopback HTTP 401/403/422/409/200 behavior. The sidecar requires normal GUI auth/CSRF, relays only to the loopback worker using a separate internal transport token, and requires native read-after-write; it keeps controls disabled when that transport is unavailable.

The backed-up production overlay is active. A root-owned 0600 shared internal-token EnvironmentFile and unit-scoped drop-ins are installed without recording the token. Only `coinmaster-hl-stageg-testnet.service` and then `coinmaster-runtime.service` were restarted. The GUI release `/srv/coinmaster-native-gui/releases/1a94874cb99dc116f6bc065c070c2fe925e4d67c` is active; its deployed index hash matches the canonical build. Guarded activation passed authenticated Pause -> native read-back -> Resume -> native read-back with no order submission. A further flat worker restart preserved `RUNNING` control state. Current status is READY, native_cash `10000`, zero positions/orders, `live_order_capability=false`, and sealed candidate/strategy/policy hashes `637762...b6d` / `3d79e...2f7` / `eee44...687`; GUI/paper peer PIDs stayed unchanged during worker restarts and all services have zero restarts afterward. The Stage-G warmup timer is active. Public browser acceptance remains limited by the earlier client-to-VPS asset-body timeout; no credential probing was performed. No real order, live execution, strategy-economic change, reseal, or `/opt` edit occurred.
