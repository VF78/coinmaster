# CM-04 isolated native replay — source-only, 2026-09-26

One Nautilus BacktestEngine/TestClock runs the unmodified WaveOverlayStrategy, its actual domain decisions, order submission, and native fill/position callbacks. The 800 DailyBar rows are feature-only seed history needed by the approved 730-day wave lookback; only 22 subsequent UTC sessions are executed. The deterministic one-day SOL residual at session 808 is market input, not an injected signal or order. No natural Stage-G worker state is read or changed.

| UTC session | Native evidence | Quantity / price |
| --- | --- | --- |
| 801 | BTC_ENTRY taker fill, short episode | 714.285 @ 126 |
| 806 | Three owned, post-only maker BTC_TP fills | 107.142 + 178.571 + 249.999 @ 126 |
| 808 | Confirmed TP right permits SOL_ADD | 1000 @ 51.33 |
| 809 | SOL_Z_HALF_EXIT | 500 @ 57.40 |
| 810 | SOL_Z_FULL_EXIT | 500 @ 57.56 |
| 821 | REGIME forced close of exact remaining BTC leaves; native account flat | 178.573 @ 138.90 |
| 822 | Distinct new BTC_ENTRY episode | 877.68 @ 139 |

The regression in runtime/tests/test_wave_native_integration.py checks exact action/session sequence, distinct episode identities, TP fractions and ownership, SOL and BTC quantity conservation, nonzero native commissions, nine matching native fill reports without duplicate executions, and the native flat checkpoint (total equals free, locked zero, changed cash). The strategy source, candidate, fees, margin policy, and execution routes are unchanged. Functional lifecycle proof is not a profitability claim.

The single replay fills its first IOC entry completely because fixture quote depth exceeds order size. A separate connected partial-BTC callback test proves partial fill/cancel replacement quantities via the same strategy callbacks; zero-fill terminal/retry tests also pass. A genuine partial fill generated by the native matcher in this single chain remains a coverage gap, not a demonstrated production fault. This replay uses research fixture USDT; CM-03's distinct isolated HL Sandbox money tests prove USDC fee/cash behavior but their exact attachment checks are blocked by the unchanged strategy seal. No combined HL-USDC full-cycle or post-restart open-state claim is made.

Focused replay: 1 passed. Connected domain/native/Stage-G/fee-model suite: 63 passed, 4 deselected (two exact-seal assertions and two HL Sandbox lifecycle cases gated by that seal), 22 existing Pandas deprecation warnings. Release stays blocked on exact new strategy approval and position-preserving native recovery. No deployment, restart, worker orders, approval reseal, or real order capability change.
